---
phase: 02-vizualnaya-sistema
plan: 05
type: execute
wave: 4
depends_on: [02-02, 02-04]
files_modified:
  - src/pages/work.astro
  - src/pages/index.astro
  - docs/hero-concept.md
autonomous: true
requirements: [REQ-design-implications, R3, R9]
user_setup:
  - service: figma
    why: "Артефакты концептов первого экрана (R9) — только если решение 02-02 выбрало вариант hero-figma; при дефолте (hero-default) не требуется"
must_haves:
  truths:
    - "work.astro содержит SYSTEM DEMO блок: SectionHeading (meta «SYSTEM DEMO», title «Системная демонстрация») + фикстурные ProjectCard (с Media-слотом и Tag), заменяемый в фазе 3 (UI-SPEC Page structure, grep-usage fallback)"
    - "Grep-контроль использования компонентов зелёный: все 8 компонентов (Button, Link, SectionHeading, ProjectCard, Media, Tag, Nav, Footer) импортированы в src/pages или src/layouts (R3)"
    - "docs/hero-concept.md существует и фиксирует выбор первого экрана по решению 02-02: дефолт D-12 (контрактная типографика, вариант не генерировался) или 2–3 артефакта концептов (ссылки/скриншоты) с выбором (R9, edge R9, AC#12)"
    - "Обоснование выбора в docs/hero-concept.md отвечает критериям D-14: реализуемость в системе (токены/сетка/CSS-only), соответствие «Calm Interface, Active Work», задел под контент фазы 3 без перекомпоновки"
    - "Невыбранные варианты (если были) архивируются в документе как референс; макет — не источник правды (D-15)"
    - "index.astro рендерит оболочку по выбранному концепту в рамках системы: при выборе варианта сложнее D-13-минимума — уточнение в рамках токенов и CSS-only; при дефолте — hero-shell 02-03 без изменений (D-12, D-13)"
    - "Проверка документа: hero-concept.md содержит строку-вердикт о выборе; отсутствие Figma MCP не блокировало фазу (D-12)"
    - "Полный npm run verify зелёный: build + check-seo + check-tokens (включая W1-ассерт, grep-правила и grep-контроль использования) + check-contrast + check-collections + check-prohibitions (R8 AC)"
  artifacts:
    - src/pages/work.astro
    - src/pages/index.astro
    - docs/hero-concept.md
  key_links:
    - "SYSTEM DEMO потребляет SectionHeading/ProjectCard/Media/Tag/IconExternalLink из 02-04 — последний недостающий узел grep-контроля использования (check-tokens) становится зелёным"
    - "docs/hero-concept.md потребляет решение 02-02 (выбор концепта); критерии D-14/D-15 — из 02-CONTEXT.md"
    - "Фикстурные пропы карточек — только для фазы 2 (AC#7); реальные данные — фаза 3; content.config.ts не изменяется"
    - "index.astro меняется только при выборе hero-figma и только в рамках системы (токены, CSS-only, scoped-стили)"
  prohibitions:
    - statement: "Figma-макет после переноса НЕ является источником правок: все изменения вносятся в код (Git), макет архивируется как референс"
      status: resolved
      verification: "judgment — код без расхождения с референсом; ADR «единый источник истины — файлы Astro в Git»; docs/hero-concept.md фиксирует статус макета (секция «Статус макета», D-15)"
    - statement: "Внешние изображения НЕ добавляются в репозиторий без зафиксированного источника и лицензионной пригодности для коммерческого использования; источник фиксируется в docs/asset-license.md"
      status: resolved
      verification: "judgment — фаза 2 не добавляет изображений (контракт Media только, превью кейсов — реальные скриншоты фазы 3); docs/asset-license.md создаётся при появлении ассетов"
---

<objective>
Завершить страничную композицию: work.astro получает SYSTEM DEMO-блок (фикстурные ProjectCard с Media и Tag — последний узел grep-контроля использования компонентов, после которого полный check-tokens зелёный), и зафиксировать выбор концепта первого экрана в docs/hero-concept.md по решению 02-02 (R9, D-12/D-14/D-15) с уточнением index.astro при выборе варианта сложнее D-13-минимума. Завершение плана — первый полный зелёный прогон npm run verify фазы.

Purpose: R3 требует, чтобы все компоненты использовались на страницах (grep-контроль); UI-SPEC определяет SYSTEM DEMO как детерминированный fallback-узел использования ProjectCard/Media/Tag (не зависит от выбора концепта). R9 требует артефакты концептов или зафиксированный дефолт.

Output: work.astro с системной демонстрацией; docs/hero-concept.md с выбором и обоснованием; index.astro по выбору; полный verify зелёный.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vizualnaya-sistema/02-CONTEXT.md
@.planning/phases/02-vizualnaya-sistema/02-RESEARCH.md
@.planning/phases/02-vizualnaya-sistema/02-PATTERNS.md
@.planning/phases/02-vizualnaya-sistema/02-UI-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: work.astro — SYSTEM DEMO блок (фикстурные ProjectCard с Media и Tag)</name>
  <files>src/pages/work.astro</files>
  <read_first>
    - 02-UI-SPEC.md (Page structure — строки 179-183: SYSTEM DEMO fallback; Component Contracts — ProjectCard/Media/Tag; Copywriting Contract — «SYSTEM DEMO / системная демонстрация»)
    - src/pages/work.astro (текущее состояние: pageLabel «02 / WORK» + empty-state из 02-03 Task 2)
    - 02-CONTEXT.md (D-04 — честный статус, замена в фазе 3; Claude's Discretion — фикстурные формулировки)
  </read_first>
  <action>
    Переработать src/pages/work.astro: сохранить title/description (уникальная пара check-seo) и pageLabel «02 / WORK»; ЗАМЕНИТЬ empty-state (из 02-03 Task 2) на SYSTEM DEMO-блок (UI-SPEC Page structure: «If the selected concept excludes the strip, the planner places a mono-labeled system-demo block on work.astro» — выбран детерминированный вариант, не зависящий от концепта): SectionHeading (meta="SYSTEM DEMO", title="Системная демонстрация", layout="split") + два фикстурных ProjectCard (только пропы-фикстуры, AC#7 — реальные данные фазы 3; формулировки фикстур — рабочая версия, помеченная комментарием // fixture: replaced in phase 3):
    1. Карточка 1: title="Проект «Терракота»" (вариант), meta="дизайн · 2026" (моно-строка роль · год, показывается при showMetrics), theme="terracotta", showMetrics=true, layout="stacked"; в слот media — <Media ratio="4:3" caption="Превью проекта — фаза 3" />; в дефолтный слот — <Tag label="Web" theme="terracotta" /> + <Tag label="CMS" /> (один с theme, один без — визуальная демонстрация нейтрального покоя D-08).
    2. Карточка 2: title="Проект «Олива»" (вариант), meta="разработка · 2026", theme="olive", showMetrics=false (мета-строка НЕ рендерится — проверка пропа), layout="split"; слот media — <Media ratio="3:2" /> (без caption); дефолтный слот — <Tag label="Astro" />.
    3. Импорты: SectionHeading, ProjectCard, Media, Tag из '../components/'; при желании Link/IconExternalLink не обязательны (Link уже используется в empty-state lab/about/contact).
    4. Scoped-стили страницы — минимальны: вертикальный ритм блока (gap var(--space-2xl) между SectionHeading и списком карточек, list gap var(--space-lg)), сетка карточек: базово — 1 колонка, @media (min-width: 768px) — 2 колонки (grid-template-columns: repeat(2, 1fr), gap var(--space-lg)); числа 768 = --bp-md (check-tokens). Всё — через токены; без <script>.
    5. Прогнать npm run build; затем ПОЛНЫЙ npm run verify — теперь все узлы зелёные (grep-контроль использования закрыт; W1-ассерт; check-contrast; check-seo; check-collections; check-prohibitions). Зафиксировать результат в SUMMARY (первый полный зелёный verify фазы).
  </action>
  <verify>
    <automated>npm run verify && node -e "const h=require('fs').readFileSync('dist/work/index.html','utf8');if(!h.includes('Системная демонстрация')||!h.includes('SYSTEM DEMO')){console.error('SYSTEM DEMO missing');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/work.astro содержит импорты SectionHeading, ProjectCard, Media, Tag и рендерит SectionHeading (meta «SYSTEM DEMO») + 2 ProjectCard с Media и Tag в слотах
    - Пропы-фикстуры: одна карточка с showMetrics=true (мета-строка в HTML), одна с showMetrics=false (мета-строка отсутствует в dist/work/index.html)
    - dist/work/index.html содержит «Системная демонстрация» и «SYSTEM DEMO»
    - Полный npm run verify exit 0 (build + check-seo + check-tokens + check-contrast + check-collections + check-prohibitions) — первый полный зелёный прогон фазы
    - content.config.ts не изменён; реальные данные/тексты не добавлялись (фаза 3)
  </acceptance_criteria>
  <done>work.astro рендерит SYSTEM DEMO с фикстурными карточками; grep-контроль использования закрыт; полный npm run verify зелёный</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: docs/hero-concept.md — фиксация выбора концепта первого экрана (R9, D-12/D-14/D-15)</name>
  <files>docs/hero-concept.md, src/pages/index.astro</files>
  <read_first>
    - 02-CONTEXT.md (D-12 — D-15, строки 63-68; реестр решений 02-02 — выбор концепта)
    - 02-SPEC.md (Requirement 9 + AC#12)
    - 02-UI-SPEC.md (Copywriting Contract — hero; Page structure — index)
    - src/pages/index.astro (hero-shell из 02-03 Task 1 — D-13 минимум)
    - docs/design_direction_portfolio_ivan_shivarshinov.md (прецедент формата документа-решения)
  </read_first>
  <action>
    Создать docs/hero-concept.md по решению 02-02 (choice из STATE.md). Обязательные секции:
    1. «Выбор» — вариант: дефолт D-12 (контрактная типографика: метка «01 / HOME» + имя Unbounded display + primary CTA «Смотреть работы»; Figma-варианты не генерировались — MCP на планировании недоступен) ЛИБО выбранный вариант из 2–3 концептов.
    2. «Варианты» — если решение hero-figma: 2–3 артефакта (ссылки/скриншоты Figma, предоставленные пользователем); если hero-default: запись «варианты не генерировались (D-12)».
    3. «Обоснование» — по критериям D-14: (a) реализуемость в системе (токены/сетка/CSS-only, без JS и новых зависимостей); (b) соответствие «Calm Interface, Active Work» (нейтральный покой, моно-слой, акценты во взаимодействии); (c) задел под фазу 3 (контент ложится без перекомпоновки оболочки).
    4. «Статус макета» — макет/концепты — референс, не источник правды; источник правды — код в Git (D-15, ADR).
    Дальше по решению: (а) hero-default — index.astro НЕ меняется (hero-shell 02-03 уже рендерит D-13 минимум); (б) hero-figma с выбором варианта сложнее минимума — уточнить index.astro в рамках системы (только токены var(), scoped-стили, CSS-only интерактивность, без JS и новых компонентов; например — расположение/размеры hero-блока или добавить секцию Selected Work по UI-SPEC Page structure, если выбранный концепт её содержит; НЕ добавлять реальный контент — фаза 3). После правок index.astro — npm run verify (зелёный).
    Проверка документа (R9 AC): hero-concept.md содержит строку-вердикт о выборе и обоснование по D-14; отсутствие выбора не заблокировало фазу.
  </action>
  <verify>
    <automated>test -f docs/hero-concept.md && grep -q 'Выбор' docs/hero-concept.md && grep -q 'D-14\|реализуем' docs/hero-concept.md && npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - docs/hero-concept.md существует и содержит секции «Выбор», «Варианты», «Обоснование» (критерии D-14), «Статус макета» (D-15)
    - Содержимое соответствует решению 02-02: дефолт D-12 (запись «варианты не генерировались») или 2–3 артефакта концептов с выбором
    - index.astro: при hero-default — без изменений (hero-shell 02-03); при hero-figma — уточнён строго в рамках системы (токены, CSS-only), без реального контента
    - npm run verify exit 0 после Task 2
  </acceptance_criteria>
  <done>docs/hero-concept.md зафиксировал выбор (дефолт или концепт) с обоснованием D-14 и архивацией D-15; index.astro соответствует выбору в рамках системы; verify зелёный</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| фикстурные пропы (build-time) → собранный HTML | Карточки SYSTEM DEMO рендерят фикстурные строки; реальные данные — фаза 3 через content collections (zod) |
| docs/hero-concept.md → репозиторий (публичный) | Документ с ссылками/скриншотами Figma становится публичным (repo public, 01-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | Фикстурный контент SYSTEM DEMO | low | mitigate | Статичные строки на сборке; типизированные пропы; заменяются реальными данными в фазе 3 — схема projects (content.config.ts) не меняется в фазе 2 (AC R5) |
| T-02-05 | Information Disclosure | docs/hero-concept.md (публичный репозиторий) | low | accept | Видимость репозитория — осознанное решение пользователя (01-01 T3); артефакты предоставлены самим пользователем (решение 02-02, вариант b); документ не содержит секретов |
| T-02-05 | Spoofing | Внешние ссылки/скриншоты Figma в документе | low | accept | Документ — референс (D-15), не исполняемый код; ссылки не попадают в собранный HTML |
</threat_model>

<verification>
- [ ] npm run verify exit 0 после Task 1 (первый полный зелёный прогон фазы — grep-контроль использования закрыт)
- [ ] dist/work/index.html содержит «Системная демонстрация»/«SYSTEM DEMO»; карточка с showMetrics=false не содержит мета-строку в HTML
- [ ] docs/hero-concept.md существует, содержит выбор + обоснование D-14 + статус макета D-15
- [ ] index.astro соответствует решению 02-02; content.config.ts не изменён
</verification>

<success_criteria>
- [ ] Все 8 компонентов используются на страницах (grep-контроль check-tokens зелёный)
- [ ] docs/hero-concept.md фиксирует выбор первого экрана (дефолт D-12 или 2–3 Figma-концепта) с обоснованием
- [ ] Главная рендерит оболочку по выбору в рамках системы; отсутствие выбора не блокировало фазу (edge R9)
- [ ] Полный npm run verify зелёный (все скрипты фазы 2)
</success_criteria>

<output>
Create `.planning/phases/02-vizualnaya-sistema/02-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/pages/work.astro` — SYSTEM DEMO-блок: `SectionHeading` (meta «SYSTEM DEMO», title «Системная демонстрация», layout split) + 2 фикстурных `ProjectCard` (theme terracotta/olive, showMetrics true/false, layout stacked/split) со слотами: `Media` (ratio 4:3 c caption / 3:2 без) + `Tag` (с theme и без); scoped-сетка карточек 1 → 2 колонки на ≥768px
- `docs/hero-concept.md` — секции «Выбор», «Варианты», «Обоснование» (критерии D-14), «Статус макета» (D-15); содержит вердикт о выборе (дефолт D-12 или 2–3 артефакта концептов)
- `src/pages/index.astro` — без изменений при hero-default; уточнение в рамках системы при hero-figma (вариант сложнее D-13-минимума)
