---
phase: 02-vizualnaya-sistema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [REQ-design-implications, R5, R9]
user_setup:
  - service: figma
    why: "Генерация 2–3 концептов первого экрана (R9) — вариант b решения 2; опционально, вариант a (дефолт D-12) не требует Figma"
must_haves:
  truths:
    - "Зафиксированы hex палитры проектов (D-07): приняты исследовательские значения (clay #8A5A44, olive #6B6B3F, slate #55606E, plum #6E4A5C) или переопределены пользователем — 4 значения приглушённые, каждая пара с bg/surface ≥ 4.5:1 (проверяется check-contrast в 02-03)"
    - "Зафиксирован выбор концепта первого экрана (R9): дефолт D-12 (контрактная типографика) или 2–3 концепта, сгенерированных в Figma, с артефактами (ссылки/скриншоты)"
    - "Решения записаны в STATE.md и читаемы планами-потребителями без повторного вопроса: 02-03 (значения --project-* в tokens.css), 02-05 (docs/hero-concept.md)"
  artifacts:
    - .planning/STATE.md
  key_links:
    - "Выбор палитры (решение 1) → 02-03 Task 1: hex в tokens.css; enum-имена (terracotta/clay/olive/slate/plum) НЕ пересматриваются (D-07: costly-контракт, потребляется схемой projects фазы 3)"
    - "Выбор концепта (решение 2) → 02-05 Task 2: docs/hero-concept.md (критерии D-14, архивация D-15); дефолт D-13-минимум (метка + имя + CTA) рендерится в 02-03 независимо от выбора"
---

<objective>
Зафиксировать 2 пользовательских решения, которые блокируют волну 2 (02-03 tracer — значения проектных цветов в tokens.css) и финальную фиксацию выбора первого экрана (02-05 — docs/hero-concept.md). Это единственные точки фазы, где требуется ввод пользователя ДО реализации; оба выявлены на этапах discuss/research (D-07: «пользователь может переопределить на чекпоинте планирования»; D-12: момент выбора — этап планирования).

Purpose: UI-SPEC Color явно резервирует за пользователем право переопределить 4 дискреционных hex на чекпоинте планирования — после фиксации в tokens.css значение меняется тривиально, но переопределение сейчас исключает повторный цикл «изменить → перепроверить контраст». Концепт первого экрана (R9) требует генерации в Figma (MCP в этой сессии недоступен) — решение фиксирует, чем руководствуется главная: дефолтом D-12 или выбранным вариантом.

Output: 2 зафиксированных решения, записанных в STATE.md. Код фаза не создаёт — чистый блок решений (паттерн 01-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vizualnaya-sistema/02-CONTEXT.md
@.planning/phases/02-vizualnaya-sistema/02-RESEARCH.md
@.planning/phases/02-vizualnaya-sistema/02-UI-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-SPEC.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Решение 1: hex палитры проектов (theme-проп, D-07/D-08)</name>
  <read_first>
    - 02-CONTEXT.md (D-07, D-08 — строки 56-58)
    - 02-UI-SPEC.md (Project accent palette, строки 105-117; Contrast pair list, строки 119-121)
    - 02-RESEARCH.md (строки 471 — численно верифицированные пары; Don't Hand-Roll «Контраст текста»)
  </read_first>
  <decision>4 дискреционных hex палитры проектов (5-е значение — терракота = системный accent #A84B32, D-07): принять исследовательские значения или переопределить</decision>
  <context>Enum-имена terracotta/clay/olive/slate/plum зафиксированы (D-07, costly-контракт: потребляются схемой projects фазы 3 и вариантами Stacki — имена не пересматриваются). Значения — Claude's Discretion, и UI-SPEC резервирует право пользователя переопределить их на чекпоинте планирования. Исследовательские значения верифицированы численно (RESEARCH строка 471): худшая пара — olive×surface 4.84:1, все ≥ 4.5:1 (WCAG AA). Требования к любым альтернативам: приглушённые (D-08, граница «неоновой AI-эстетики»), каждая пара текст/фон на bg #FAFAF7 и surface #F1F0EB ≥ 4.5:1 — после фиксации значений они проверяются check-contrast (02-03).</context>
  <options>
    <option id="palette-research">
      <name>Принять исследовательские значения (рекомендация): clay #8A5A44, olive #6B6B3F, slate #55606E, plum #6E4A5C</name>
      <pros>Численно верифицированы (olive×surface 4.84:1 — худшая, все ≥ 4.5:1); приглушённые, соответствуют D-08; значения уже согласованы с UI-SPEC Color</pros>
      <cons>Не выбран пользователем вручную (при необходимости заменяются позже в tokens.css — правка одного файла, enum не меняется)</cons>
    </option>
    <option id="palette-custom">
      <name>Переопределить: пользователь указывает 4 hex (или несколько)</name>
      <pros>Персональный выбор тона; переопределение сейчас дешевле, чем после фиксации</pros>
      <cons>Значения должны пройти проверку ≥ 4.5:1 на bg/surface (check-contrast в 02-03 может вернуть красным — потребуется корректировка)</cons>
    </option>
  </options>
  <reversibility rating="reversible">Изменение значений позже = правка hex в tokens.css (один файл); enum-имена НЕ меняются (costly-контракт D-07)</reversibility>
  <resume-signal>palette-research / palette-custom (перечислить hex)</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Решение 2: концепт первого экрана (R9, D-12/D-14/D-15)</name>
  <read_first>
    - 02-CONTEXT.md (D-12 — D-15, строки 63-68)
    - 02-SPEC.md (Requirement 9 и его Acceptance Criteria)
    - 02-RESEARCH.md (Open Question 1, строки 516-518; A2, строка 508)
  </read_first>
  <decision>Чем руководствуется главная (index.astro) и что фиксируется в docs/hero-concept.md: дефолт D-12 (контрактная типографика) или 2–3 концепта из Figma с выбором</decision>
  <context>SPEC R9 требует 2–3 варианта концепта первого экрана, сгенерированных в Figma через MCP (remote-сервер подключается на планировании). В этой сессии планирования Figma MCP недоступен (RESEARCH A2). D-12: отсутствие выбора не блокирует фазу — дефолт — контрактная типографика (метка «01 / HOME» + имя Unbounded display + primary CTA Button «Смотреть работы», рендерится в 02-03 Task 1). Выбор пользователя фиксируется в docs/hero-concept.md (02-05) с обоснованием по критериям D-14 (реализуемость в системе, «Calm Interface, Active Work», задел под фазу 3) и архивацией невыбранных вариантов как референса (D-15; макет — не источник правды).</context>
  <options>
    <option id="hero-default">
      <name>Дефолт D-12: контрактная типографика (рекомендация при недоступности Figma)</name>
      <pros>Фаза не блокируется; главная детерминирована (D-13-минимум); docs/hero-concept.md фиксирует дефолт с обоснованием по D-14; концепты могут появиться в фазе 3 без перекомпоновки</pros>
      <cons>Первый экран не имеет Figma-концепта-референса до фазы 3</cons>
    </option>
    <option id="hero-figma">
      <name>Пользователь генерирует 2–3 концепта в Figma самостоятельно (вне агентской сессии) и передаёт ссылки/скриншоты</name>
      <pros>Референс-выбор для главной (R9 исполнен полностью: 2–3 артефакта + выбор); варианты архивируются в docs/hero-concept.md</pros>
      <cons>Требует ручной работы пользователя в Figma; выбор должен быть передан в сессию выполнения; при выборе варианта сложнее D-13-минимума — index.astro уточняется в 02-05 Task 2 в рамках системы (токены, CSS-only)</cons>
    </option>
  </options>
  <reversibility rating="reversible">Дефолт рендерится в любом случае (D-13-минимум); смена концепта в фазе 3 не ломает shell — D-12 «задел под контент без перекомпоновки»</reversibility>
  <resume-signal>hero-default / hero-figma (передать ссылки/скриншоты)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Фаза решений не создаёт кода и не пересекает границ доверия. Единственный актив — данные пользователя (выбор палитры, выбор концепта).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | docs/hero-concept.md (ссылки/скриншоты Figma в публичном репозитории) | low | accept | Видимость репозитория public — осознанное решение пользователя (01-01 T3); артефакты предоставляет сам пользователь (вариант b); макет — референс, не источник правды (D-15) |
| T-02-02 | Tampering | Записи решений в STATE.md | low | accept | Внутренний артефакт планирования; потеря данных покрывается git-историей |
</threat_model>

<verification>
- [ ] Оба чекпоинта закрыты: в STATE.md зафиксированы hex палитры (или факт принятия исследовательских значений) и выбор концепта (дефолт или Figma-артефакты)
- [ ] Решения записаны так, что планы 02-03 и 02-05 читают их без повторного вопроса
</verification>

<success_criteria>
- [ ] Пользователь ответил на 2 вопроса с обоснованием выбора
- [ ] Ни один hex --project-* не записан в tokens.css до решения 1
- [ ] docs/hero-concept.md не создан до решения 2
</success_criteria>

<output>
Create `.planning/phases/02-vizualnaya-sistema/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

План не создаёт символов кода — это блок решений (паттерн 01-01). Единственный артефакт — записи в `.planning/STATE.md`: (1) hex палитры проектов — приняты исследовательские (clay #8A5A44, olive #6B6B3F, slate #55606E, plum #6E4A5C) или переопределены пользователем, enum-имена terracotta/clay/olive/slate/plum не пересматриваются (D-07); (2) выбор концепта первого экрана — дефолт D-12 или 2–3 Figma-концепта с артефактами (D-12/D-14/D-15). Потребители: 02-03 Task 1 (значения в tokens.css), 02-05 Task 2 (docs/hero-concept.md), 02-05 Task 1 (влияние на index.astro при выборе варианта сложнее D-13-минимума).
