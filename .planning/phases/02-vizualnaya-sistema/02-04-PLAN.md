---
phase: 02-vizualnaya-sistema
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - src/components/SectionHeading.astro
  - src/components/Tag.astro
  - src/components/Media.astro
  - src/components/ProjectCard.astro
  - src/components/IconExternalLink.astro
  - src/components/IconArrowDown.astro
  - src/components/IconMail.astro
  - src/components/IconCopy.astro
  - src/components/IconGithub.astro
  - src/components/IconTelegram.astro
  - src/components/IconLinkedin.astro
  - src/components/IconCheck.astro
  - src/components/IconClose.astro
autonomous: true
requirements: [REQ-design-implications, R3, R5, R7, R8]
user_setup: []
must_haves:
  truths:
    - "Каждый компонент рендерится без пропов / с дефолтами без ошибки сборки — npm run build exit 0 (R3, edge R3, AC#7)"
    - "ProjectCard и Tag принимают типизированный theme-проп (5-enum terracotta/clay/olive/slate/plum); при theme на корне выставляется --project-accent через inline style; без theme — нейтральный покой (R5, edge R5)"
    - "Невалидное значение theme → astro check exit 1 (negative-фикстура scripts/check-theme.mjs, TS strict), фикстура удаляется, дерево чисто (R5)"
    - "Значения hex палитры живут ТОЛЬКО в tokens.css (--project-*); компоненты содержат только маппинг enum → var(--project-*) — без hex-литералов (R5, check-tokens)"
    - "Media без изображения рендерит рамку с ratio (aspect-ratio через var(--ratio), overflow hidden, min-height 0); содержимое слота обрезается рамкой; caption — моно (R7, edge R7, UI-SPEC overflow media)"
    - "SectionHeading layout='split': ниже bp-md складывается в stacked (meta над title), ≥768px — row + space-between; title переносится без обрезания (UI-SPEC overflow/long-text SectionHeading)"
    - "ProjectCard: title 20px Unbounded переносится без обрезания; media-слот обрезается рамкой; мета-строка рендерится только при showMetrics (UI-SPEC overflow/long-text list-collection, empty/populated list-collection)"
    - "Tag: mono-лейбл 14px, рамка 1px var(--color-line), fill var(--color-surface); с theme — текст+рамка var(--project-accent); hover рамка→ink через motion-токены (R3)"
    - "10 Icon*.astro (IconArrowRight — 02-03, остальные 9 здесь): self-closing, отдельные файлы без switch-контейнеров и спрайтов, size 16|24 (дефолт 24), stroke 2px, currentColor, aria-hidden (D-09, D-10, D-11)"
    - "Переходы во всех компонентах — только var(--motion-*)/var(--ease-*) (grep-правило check-tokens, R4)"
  artifacts:
    - src/components/SectionHeading.astro
    - src/components/Tag.astro
    - src/components/Media.astro
    - src/components/ProjectCard.astro
    - src/components/IconExternalLink.astro
    - src/components/IconArrowDown.astro
    - src/components/IconMail.astro
    - src/components/IconCopy.astro
    - src/components/IconGithub.astro
    - src/components/IconTelegram.astro
    - src/components/IconLinkedin.astro
    - src/components/IconCheck.astro
    - src/components/IconClose.astro
  key_links:
    - "theme-маппинг ProjectCard/Tag идентичен: ACCENTS = { terracotta: 'var(--color-accent)', clay: 'var(--project-clay)', olive: 'var(--project-olive)', slate: 'var(--project-slate)', plum: 'var(--project-plum)' } — значения в tokens.css (02-03), enum-имена из D-07"
    - "Inline style с кавыченными ключами ('--project-accent': ...) — не ловит check-tokens VAR_DEF_RE (паттерн RESEARCH Pattern 2)"
    - "Грей-использование SectionHeading/ProjectCard/Media/Tag на страницах — 02-05 (SYSTEM DEMO на work.astro); до этого полный check-tokens по-прежнему known-red по grep-контролю"
    - "Утилизация Media/ProjectCard/Tag на страницах без реальных данных: фикстурные пропы (AC#7) — реальные кейсы в фазе 3"
---

<objective>
Собрать горизонтальный слой компонентной библиотеки: SectionHeading, Tag, Media (Task 1), ProjectCard с theme-контрактом и negative-фикстурой (Task 2), 9 оставшихся Icon*.astro (Task 3). Все компоненты следуют форме «interface Props + деструктуризация с дефолтами + scoped style через var()» (паттерн 02-03, Seo.astro/Button.astro) и потребляют токены из 02-03. Адаптивность живёт внутри компонентов (ADR §5.4, min-width inclusive на bp-значениях).

Purpose: закрыть R3 (компоненты с семантическими пропами), R5 (theme-контракт --project-accent с нейтральным покоем), R7 (медиа-оболочка как контракт фазы 3) — библиотека, на которую страницы 02-05 будут ссылаться (grep-контроль использования завершится там).

Output: 5 компонентов + 9 иконок в src/components; build зелёный; negative-фикстура theme подтверждает TS strict; check-tokens по src без новых нарушений.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vizualnaya-sistema/02-CONTEXT.md
@.planning/phases/02-vizualnaya-sistema/02-RESEARCH.md
@.planning/phases/02-vizualnaya-sistema/02-PATTERNS.md
@.planning/phases/02-vizualnaya-sistema/02-UI-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SectionHeading.astro + Tag.astro + Media.astro</name>
  <files>src/components/SectionHeading.astro, src/components/Tag.astro, src/components/Media.astro</files>
  <read_first>
    - 02-PATTERNS.md (SectionHeading — строки 207-218; Tag — строки 290-295; Media — строки 256-285; Shared Patterns — строки 415-461)
    - 02-RESEARCH.md (Pattern 3 — адаптивность внутри компонентов, строки 256-273; Code Example 3 — Media-рамка, строки 417-445; Pitfall 3 — aspect-ratio blow-out)
    - 02-UI-SPEC.md (Component Contracts — SectionHeading/Media/Tag; Color — theme палитра; Containers & Breakpoints; Motion)
  </read_first>
  <action>
    Три компонента по паттерну «Props + scoped style + var()» (эталон — Button.astro из 02-03). Только токены; переходы — var(--motion-*)/var(--ease-*); числа media-запросов равны bp-значениям (768/1200, min-width inclusive); ноль <script>.

    1. src/components/SectionHeading.astro: interface Props { meta?: string; title: string; layout?: 'stacked' | 'split' } дефолт layout='stacked'. Разметка: {meta && <p class="sh-meta">{meta}</p>} + <h2 class="sh-title">{title}</h2> в обёртке .section-heading (класс-модификатор .section-heading--split при layout='split'). Scoped-стили: .sh-meta — моно (--font-mono, --text-label 14px, --color-ink-muted) — «номера и подписи» (D-05-язык); .sh-title — font-family var(--font-display), font-size var(--text-heading), font-weight 600, line-height 1.2, перенос без обрезания (overflow-wrap). layout split: базовое состояние — flex-direction column (meta над title); @media (min-width: 768px) { flex-direction: row; justify-content: space-between; align-items: baseline; gap: var(--space-lg) } — числа 768 = --bp-md (check-tokens).
    2. src/components/Tag.astro: interface Props { label: string; theme?: 'terracotta' | 'clay' | 'olive' | 'slate' | 'plum'; size?: 'sm' | 'md' } дефолт size='sm'. Маппинг ACCENTS (см. key_links) в frontmatter; на корень <span class="tag" style={theme ? { '--project-accent': ACCENTS[theme] } : undefined}>. Scoped-стили: mono-лейбл (--font-mono, --text-label 14px); без theme: color var(--color-ink-muted), background var(--color-surface), border 1px solid var(--color-line); с theme: color/рамка var(--project-accent, fallback var(--color-ink-muted)) — свойство выставляется только при theme (нейтральный покой D-08: без theme ноль акцента); hover: border-color var(--color-ink); size sm: padding var(--space-xs) var(--space-sm); md: padding var(--space-sm) var(--space-md); transition: color var(--motion-fast) var(--ease-standard), border-color var(--motion-fast) var(--ease-standard), background-color var(--motion-fast) var(--ease-standard).
    3. src/components/Media.astro (R7, RESEARCH Code Example 3): interface Props { ratio?: '16:9' | '4:3' | '3:2' | '1:1'; caption?: string } дефолт ratio='4:3'. Разметка: <figure class="media"> <div class="media__frame" style={{ '--ratio': RATIOS[ratio] }}><slot /></div> {caption && <figcaption class="media__caption">{caption}</figcaption>} </figure>; RATIOS = { '16:9': '16 / 9', '4:3': '4 / 3', '3:2': '3 / 2', '1:1': '1 / 1' }. Scoped-стили: .media__frame — aspect-ratio: var(--ratio), overflow: hidden, min-height: 0 (Pitfall 3: контент не раздувает ratio), border: 1px solid var(--color-line), width: 100%; hover рамки (когда интерактивна): border-color var(--color-ink-muted), transition: border-color var(--motion-base) var(--ease-standard); .media__caption — моно: font-family var(--font-mono), font-size var(--text-caption), color var(--color-ink-muted), margin-top var(--space-xs). Без изображения рендерится пустая рамка с ratio (edge R7 — контракт для <Image /> фазы 3).
    4. Прогнать npm run build — компоненты без использования на страницах всё равно типизируются и собираются (astro check покрывает src/components).
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');for(const f of ['SectionHeading','Tag','Media']){const s=fs.readFileSync('src/components/'+f+'.astro','utf8');if(!s.includes('interface Props')){console.error('Props missing '+f);process.exit(1)}}const m=fs.readFileSync('src/components/Media.astro','utf8');if(!m.includes('aspect-ratio: var(--ratio)')||!m.includes('overflow: hidden')||!m.includes('min-height: 0')){console.error('Media frame contract broken');process.exit(1)}const t=fs.readFileSync('src/components/Tag.astro','utf8');if(!t.includes('--project-accent')){console.error('Tag theme contract missing');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - SectionHeading.astro содержит props meta?/title/layout? с enum 'stacked'|'split' и @media (min-width: 768px) для split-варианта (число = --bp-md)
    - Tag.astro содержит props label/theme?/size? с enum theme 5-значений, inline style '--project-accent' при theme, hover рамка→ink, размеры sm/md через var(--space-*)
    - Media.astro содержит props ratio?/caption?, RATIOS-маппинг, .media__frame с aspect-ratio/overflow hidden/min-height 0/1px var(--color-line), моно-caption
    - npm run build exit 0 (пустые пропы — дефолты, edge R3)
  </acceptance_criteria>
  <done>SectionHeading, Tag, Media созданы с семантическими пропами, адаптивностью внутри компонентов и полным потреблением токенов; build зелёный</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ProjectCard.astro (theme-контракт R5) + реальный прогон negative-фикстуры check-theme</name>
  <files>src/components/ProjectCard.astro</files>
  <read_first>
    - 02-RESEARCH.md (Pattern 2 — семантический проп → CSS-переменная, строки 213-252; Pitfall 6 — scoped-стили и дочерние компоненты)
    - 02-PATTERNS.md (ProjectCard — строки 222-252; Tag/theme-паттерн)
    - 02-UI-SPEC.md (Component Contracts — ProjectCard; Color — project palette; Motion — card hover)
    - scripts/check-theme.mjs (создан в 02-01 — как запускается реальная фикстура)
  </read_first>
  <action>
    1. src/components/ProjectCard.astro (R5): interface Props { title: string; meta?: string; theme?: 'terracotta' | 'clay' | 'olive' | 'slate' | 'plum'; showMetrics?: boolean; layout?: 'stacked' | 'split' } дефолты showMetrics=false, layout='stacked'. ACCENTS-маппинг как в Tag (см. key_links). Разметка: <article class="card" style={theme ? { '--project-accent': ACCENTS[theme] } : undefined}> <div class="card__media"><slot name="media" /></div> <h3 class="card__title">{title}</h3> {showMetrics && meta && <p class="card__meta">{meta}</p>} <div class="card__footer"><slot /></div> </article> — слоты: именованный 'media' (для Media.astro) и дефолтный (для Tag и прочих). Scoped-стили: .card — border 1px solid var(--color-line), padding var(--space-lg), display flex column, gap var(--space-md); .card__title — font-family var(--font-display), font-size var(--text-heading), font-weight 600, line-height 1.2, перенос без обрезания; .card__meta — моно (--font-mono, --text-caption, --color-ink-muted); layout='split': базовое — column; @media (min-width: 768px) { flex-direction: row; .card__media { flex: 1 }; текстовая часть — flex: 1 } (число 768 = --bp-md). Состояния (SC3, нейтральный покой): в покое карточка использует ink/line — ноль акцента; hover: border-color var(--color-ink-muted) + .card__title color var(--project-accent, var(--color-ink)); transition: border-color var(--motion-base) var(--ease-standard), color var(--motion-fast) var(--ease-standard). Ключевое свойство --project-accent выставляется на корне article через inline style с КАВЫЧЕННЫМ ключом ('--project-accent') — custom property наследуется в слоты (Media/Tag внутри карточки получают accent без дополнительных правил; Pitfall 6: scoped-селекторы не пересекают компоненты, наследование — runtime-каскад). Нейтральный покой обязателен: accent появляется только в hover/focus (D-08).
    2. Прогнать реальную negative-фикстуру: node scripts/check-theme.mjs — скрипт создаёт src/pages/zz-check-theme.astro с theme="bad", ожидает exit != 0 от npm run check (TS strict: 'bad' не входит в union), удаляет фикстуру и проверяет чистоту дерева. Убедиться, что после прогона git status не содержит zz-check-* файлов.
    3. Прогнать npm run build (ProjectCard с дефолтами — пустой слот-медиа и без theme: сборка обязана проходить, edge R3/AC#7).
  </action>
  <verify>
    <automated>node scripts/check-theme.mjs && npm run build && node -e "const s=require('fs').readFileSync('src/components/ProjectCard.astro','utf8');if(!s.includes('--project-accent')||!s.includes(\"'terracotta': 'var(--color-accent)'\")){console.error('ProjectCard theme contract broken');process.exit(1)}" && test -z "$(git status --porcelain | grep zz-check)"</automated>
  </verify>
  <acceptance_criteria>
    - ProjectCard.astro содержит props title/meta?/theme?/showMetrics?/layout?, ACCENTS-маппинг 5 значений, inline style '--project-accent' на корне при theme, hover title→accent (var(--project-accent, var(--color-ink))), border line→ink-muted
    - node scripts/check-theme.mjs exit 0: theme="bad" падает на astro check (exit != 0), фикстура удалена, zz-check-* файлов в рабочем дереве нет
    - npm run build exit 0: ProjectCard без пропов/с дефолтами собирается (edge R3); content.config.ts НЕ изменён (R5 AC)
    - Ни одного hex-литерала в ProjectCard.astro (только var()/маппинг на var()) — check-tokens hex-правило
  </acceptance_criteria>
  <done>ProjectCard с theme-контрактом создан; нейтральный покой подтверждён контрактом стилей; negative-фикстура check-theme зелёная (TS strict ловит невалидный theme); build зелёный</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 9 оставшихся Icon*.astro (D-09/D-10/D-11)</name>
  <files>src/components/IconExternalLink.astro, src/components/IconArrowDown.astro, src/components/IconMail.astro, src/components/IconCopy.astro, src/components/IconGithub.astro, src/components/IconTelegram.astro, src/components/IconLinkedin.astro, src/components/IconCheck.astro, src/components/IconClose.astro</files>
  <read_first>
    - 02-CONTEXT.md (D-09 — список, D-10 — отдельные self-closing файлы, D-11 — контракт стиля)
    - 02-PATTERNS.md (Icon*.astro — строки 298-314; эталон — IconArrowRight.astro из 02-03)
    - 02-UI-SPEC.md (Component Contracts — Icons (10))
  </read_first>
  <action>
    Создать 9 компонентов-иконок по точному образцу IconArrowRight.astro (02-03): interface Props { size?: number } дефолт 24; <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="square" vector-effect="non-scaling-stroke" aria-hidden="true"> + кастомный геометрический path (прямые линии и прямоугольники — язык «прямоугольные плоскости, тонкие линии», D-11). Без <style>, без <script>. Геометрия (квадратные углы, stroke 2, вписанность в 24×24):
    - IconExternalLink: квадрат-рамка (прямоугольник по центру) + стрелка из угла (два отрезка: вертикальный и горизонтальный, выходящие из верхнего правого угла) — язык «рамка + выноска»;
    - IconArrowDown: вертикальный отрезок сверху вниз + стрелка с квадратным углом внизу (зеркально IconArrowRight);
    - IconMail: прямоугольник-конверт (рамка) + V-образная ломаная (клапан) из нижних углов к верхнему центру;
    - IconCopy: два прямоугольника (оригинал + копия со сдвигом);
    - IconGithub: прямоугольник-рамка (по контуру) + внутренняя линия-горизонталь (стилизованная «канавка») — без заливки, stroke-геометрия;
    - IconTelegram: прямоугольный контур + наклонная ломаная (стилизованная «ракета» прямыми отрезками);
    - IconLinkedin: прямоугольник-рамка + три горизонтальных отрезка внутри (стилизованные «строки») + квадрат-точка;
    - IconCheck: две ломаные прямых (галочка) с квадратным углом;
    - IconClose: два диагональных отрезка (крест) с квадратными концами.
    Точные координаты — на усмотрение исполнителя в рамках контракта: все отрезки прямые, углы квадратные (linecap square), геометрия не выходит за viewBox. Файлы self-closing (D-10): каждый — собственный файл, никаких switch-контейнеров и спрайтов. Иконки не обязаны быть использованы на страницах в этой фазе (библиотечный контракт; в рендере участвуют IconArrowRight — CTA 02-03 и IconExternalLink — SYSTEM DEMO 02-05).
    Прогнать npm run build.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const names=['ExternalLink','ArrowDown','Mail','Copy','Github','Telegram','Linkedin','Check','Close'];for(const n of names){const p='src/components/Icon'+n+'.astro';const s=fs.readFileSync(p,'utf8');if(!s.includes('viewBox=\"0 0 24 24\"')||!s.includes('stroke=\"currentColor\"')||!s.includes('stroke-width=\"2\"')||!s.includes('aria-hidden=\"true\"')){console.error('icon contract broken: '+p);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - Существуют 9 файлов src/components/Icon{ExternalLink,ArrowDown,Mail,Copy,Github,Telegram,Linkedin,Check,Close}.astro, каждый содержит viewBox="0 0 24 24", stroke="currentColor", stroke-width="2", stroke-linecap="square", aria-hidden="true", props size (дефолт 24)
    - Ни один файл не содержит <style> или <script>
    - npm run build exit 0
    - В src/components нет switch-контейнеров и спрайтов (только отдельные self-closing файлы, D-10)
  </acceptance_criteria>
  <done>Набор из 10 иконок (1 из 02-03 + 9 здесь) существует по контракту D-09/D-10/D-11; build зелёный</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| пропы компонентов (build-time) → HTML | theme-проп enum-типизирован; label/title — строки из статичных страниц, не пользовательский ввод |
| inline style (--project-accent) → наследование в слоты | Custom property наследуется в дочерние компоненты (Media/Tag внутри ProjectCard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | theme-проп (невалидное значение) | low | mitigate | TS union-тип + astro check в build; negative-фикстура check-theme.mjs (exit 1 при theme='bad') — невалидное значение падает сборкой, а не молчит в проде (AC R5) |
| T-02-04 | Spoofing | Inline-SVG иконки (9 новых) | low | mitigate | Markup-only (без <script>/<style>), aria-hidden="true", stroke/fill currentColor; внешних ресурсов нет |
| T-02-04 | Tampering | Слоты ProjectCard (контент slot name='media') | low | mitigate | Статический рендер; фаза 3 добавляет <Image /> через слот — контракт Media; Astro экранирует HTML |
| T-02-04 | Tampering | Хардкод-значения (hex/px) в новых компонентах | medium | mitigate | check-tokens hex/px-запрет (значения — только tokens.css); transition-токены grep-правилом |
</threat_model>

<verification>
- [ ] npm run build exit 0 после каждого Task (включая astro check — компоненты типизируются)
- [ ] node scripts/check-theme.mjs exit 0 (negative-фикстура theme='bad') — Task 2
- [ ] git status не содержит zz-check-* файлов после Task 2
- [ ] Спот-grep: ни в одном новом компоненте нет hex-литералов (#[0-9a-f]{3,6}) и переходов с литеральными длительностями
- Объём 13 файлов / 3 задачи осознан: Task 3 — 9 файлов иконок (~15 строк каждый, один шаблон контракта D-09/D-10/D-11), механический батч, верифицируется контракт-грepом в verify Task 3 (viewBox/stroke/aria-hidden); счётчик файлов завышает реальную контекст-стоимость
</verification>

<success_criteria>
- [ ] 5 компонентов (SectionHeading, Tag, Media, ProjectCard, Link — Link из 02-03) и 10 иконок существуют в src/components
- [ ] theme-контракт работает: enum 5 значений → --project-accent на корне; без theme — нейтральный покой (SC3)
- [ ] Media-оболочка (figure + ratio + рамка + caption + слот) рендерится без изображения
- [ ] Каждый компонент рендерится без пропов/с дефолтами без ошибки сборки (edge R3)
- [ ] Невалидный theme падает на astro check (negative-фикстура)
</success_criteria>

<output>
Create `.planning/phases/02-vizualnaya-sistema/02-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/components/SectionHeading.astro` — Props `{meta?, title, layout?: 'stacked'|'split'}`; .sh-meta моно-надзаголовок; split ≥768px (row, space-between)
- `src/components/Tag.astro` — Props `{label, theme?: 'terracotta'|'clay'|'olive'|'slate'|'plum', size?: 'sm'|'md'}`; inline style `--project-accent` при theme; hover border→ink
- `src/components/Media.astro` — Props `{ratio?: '16:9'|'4:3'|'3:2'|'1:1', caption?}`; RATIOS-маппинг; .media__frame (aspect-ratio/overflow hidden/min-height 0/1px var(--color-line)); слот; моно-figcaption
- `src/components/ProjectCard.astro` — Props `{title, meta?, theme?, showMetrics?: boolean, layout?: 'stacked'|'split'}`; ACCENTS-маппинг 5 значений → inline style `--project-accent` на корне article; слоты `name="media"` и дефолтный; hover border→ink-muted + title→accent; нейтральный покой без theme
- `src/components/Icon{ExternalLink,ArrowDown,Mail,Copy,Github,Telegram,Linkedin,Check,Close}.astro` — Props `{size?: number}` (24); svg 24×24, stroke 2, linecap square, currentColor, aria-hidden; геометрические paths из прямых отрезков (D-11)
