---
phase: 02-vizualnaya-sistema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-tokens.mjs
  - scripts/check-contrast.mjs
  - scripts/check-theme.mjs
  - package.json
autonomous: true
requirements: [REQ-design-implications, R1, R2, R3, R4, R5, R8]
user_setup: []
must_haves:
  truths:
    - "Расширенный check-tokens.mjs проходит node --check и --self-test с кодом 0 (все новые правила покрыты фикстурами)"
    - "check-tokens требует bp-группу (--bp-md, --bp-lg) в tokens.css и наличие --text-lead/--text-caption/--ease-enter/--ease-exit/--color-line/--project-clay/--project-olive/--project-slate/--project-plum (R1)"
    - "check-tokens сверяет числа media-запросов min-width в src/components, src/layouts, src/pages со значениями bp-токенов из tokens.css — любое другое число (например, 767px) → нарушение (R1, R6, edge R1)"
    - "check-tokens содержит grep-правило: transition-декларации в src/components задаются только через var(--motion-*)/var(--ease-*); литералы (150ms, cubic-bezier) вне var() → нарушение; global.css (kill-switch 0ms) не сканируется (R4)"
    - "check-tokens содержит W1-ассерт по dist: на каждой из 5 собранных страниц ровно один aria-current='page', и href элемента соответствует маршруту файла; при отсутствии dist — предупреждение, не fail (R2)"
    - "check-tokens содержит grep-контроль использования: каждый из 8 компонентов (Button, Link, SectionHeading, ProjectCard, Media, Tag, Nav, Footer) импортирован хотя бы в одном файле src/pages или src/layouts (R3)"
    - "check-tokens содержит проверку: в собранных страницах dist 0 тегов <script> (R4)"
    - "Новый check-contrast.mjs проходит node --check и --self-test с кодом 0; формула WCAG; self-test эталоны: чёрный×белый = 21:1, #A84B32×#FAFAF7 = 5.40:1 (pass), #DAD9D3×#FAFAF7 = 1.35 (fail-path) (R8)"
    - "check-contrast проверяет 20 пар из UI-SPEC Color (ink×bg, ink×surface, ink-muted×bg, ink-muted×surface, accent×bg, accent×surface, accent-ink×accent, destructive×bg, 4 проектных акцента×bg, ×surface, accent-ink×каждый проектный акцент) на ≥4.5:1; --color-line исключён; отсутствующий токен → нарушение (R5, R8)"
    - "Новый check-theme.mjs: временная страница с theme='bad' → npm run check exit != 0 → фикстура удаляется в finally; рабочее дерево чисто от фикстур (R5)"
    - "package.json содержит скрипт check-contrast и включает check-contrast в цепочку verify после check-tokens (R8)"
  artifacts:
    - scripts/check-tokens.mjs
    - scripts/check-contrast.mjs
    - scripts/check-theme.mjs
    - package.json
  key_links:
    - "verify-цепочка: build → check-seo → check-tokens (включая W1-ассерт и grep-правила) → check-contrast → check-collections → check-prohibitions"
    - "Полный npm run verify зелёный только после 02-05: grep-контроль использования и bp-группа требуют кода фаз 02-03/02-04/02-05 (частичный прогон допустим, паттерн фазы 1: 'полный verify зелёный только после 01-04/01-05')"
    - "W1-ассерт работает по dist — порядок build → check-tokens в verify уже гарантирует наличие dist (RESEARCH A3)"
---

<objective>
Создать валидационный каркас фазы 2 — расширение scripts/check-tokens.mjs (bp-группа, наличие новых токенов, сверка чисел media-запросов, grep-правило transition-токенов, W1-ассерт, grep-контроль использования компонентов, 0 <script> в dist) и два новых скрипта: check-contrast.mjs (WCAG-контраст пар UI-SPEC) и check-theme.mjs (negative-фикстура невалидного theme-пропа). Скрипты создаются ДО кода, который они проверяют (Nyquist: verify-команды планов 02-03/02-04/02-05 ссылаются на них).

Purpose: фаза не содержит юнит-логики; валидация — на уровне src (токен-дисциплина, числа media-запросов, transition-токены) и собранного dist (W1, 0 script-тегов). Скрипты — plain Node.js ESM без зависимостей, с self-tests на временных фикстурах (паттерн фазы 1: 6 скриптов).

Покрытие Wave 0 gaps (02-VALIDATION.md): gap 1 (check-contrast) — здесь; gap 2 (расширение check-tokens) — здесь; gap 3 (W1-ассерт) — здесь (внутри check-tokens); gap 4 (negative-фикстура theme) — здесь (check-theme.mjs, реальный прогон — в 02-04); gap 5 (grep-контроль использования) — здесь (внутри check-tokens); gap 6 (npm verify) — здесь.

Output: 3 скрипта в scripts/ (+ package.json verify-цепочка), каждый с режимом --self-test для проверки логики без собранного проекта.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vizualnaya-sistema/02-VALIDATION.md
@.planning/phases/02-vizualnaya-sistema/02-RESEARCH.md
@.planning/phases/02-vizualnaya-sistema/02-PATTERNS.md
@.planning/phases/02-vizualnaya-sistema/02-UI-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: расширение scripts/check-tokens.mjs — bp-группа, токены, media-query сверка, transition-grep, W1-ассерт, использование компонентов, 0 script</name>
  <files>scripts/check-tokens.mjs</files>
  <read_first>
    - scripts/check-tokens.mjs (текущее состояние: GROUPS строки 38-44, audit строки 71-122, self-test строки 134-253 — точка расширения)
    - 02-RESEARCH.md (Pattern 5 «Верификация», строки 297-312; Code Example 4 — W1-ассерт, строки 447-454; Code Example 5 — формула WCAG, строки 456-471)
    - 02-PATTERNS.md (раздел scripts/check-tokens.mjs, строки 339-365 — константы GROUPS и точка расширения)
    - 02-VALIDATION.md (Wave 0 Gaps, строки 58-66; Per-Task Verification Map R1/R2/R3/R4/R8)
    - 02-SPEC.md (Requirements 1, 2, 3, 4, 8 и их Acceptance Criteria)
  </read_first>
  <action>
    Расширить scripts/check-tokens.mjs, сохранив существующие правила (единый файл токенов, группы, запрет hex/px-шкалы вне tokens.css) и их self-tests. ВАЖНО: существующая фикстура GOOD_TOKENS в runSelfTest содержит 5 групп — после добавления 6-й обязательной группы bp фикстуру надо дополнить группой bp, иначе старый self-test начнёт падать по ложной причине.

    1. GROUPS (строки 38-44): добавить 6-й элемент { name: 'bp (--bp-*)', re: /--bp-[a-z0-9-]+\s*:/i }. Обновить сообщения render() и self-test: «5 групп» → «6 групп».
    2. Новые проверки наличия токенов в tokens.css (после проверки групп): обязательны --text-lead, --text-caption, --ease-enter, --ease-exit, --color-line, --project-clay, --project-olive, --project-slate, --project-plum; --text-display обязан содержать подстроку clamp( (fluid, R1). Отсутствие любого → violation с именем токена.
    3. Сверка чисел media-запросов: регулярное выражение /@media\s*\(min-width:\s*(\d+)px\)/gi по файлам src/components, src/layouts, src/pages (walk с SCAN_EXTS). Значения bp-токенов считываются из tokens.css (--bp-md, --bp-lg). Каждое найденное число обязано совпасть с одним из значений bp-токенов; min-width — единственный допустимый вариант (Pitfall 2: max-width запрещён контрактом AC R6 — max-width-медиазапрос тоже нарушение). Любое другое число (например, 767px) или max-width → violation с именем файла и числом.
    4. Grep-правило transition-токенов (R4): по src/components (НЕ src/styles — global.css владеет kill-switch 0ms, RESEARCH Pattern 4/6). Для каждой декларации, содержащей свойство transition (transition, transition-property, transition-duration, transition-timing-function), значение обязано состоять только из var(--motion-*) и/или var(--ease-*) (допустимы также ключевые слова-свойства перед var(), например transition: color var(--motion-fast) var(--ease-standard)). Наличие литерала \d+ms или cubic-bezier( или ease-слов (ease, ease-in, ease-out, ease-in-out, linear) вне var() → violation с именем файла и найденным литералом.
    5. W1-ассерт по dist (R2, RESEARCH Code Example 4, паттерн walk+regex check-seo.mjs): если dist/ отсутствует — предупреждение в консоль, НЕ fail (паттерн guard auditDist, PATTERNS строка 360). Если dist присутствует: walk dist/**/*.html; для каждой страницы ровно одно вхождение aria-current="page" → иначе violation; у элемента с aria-current извлечь атрибут href и сверить с маршрутом файла: index.html → '/', {name}/index.html → '/{name}' (без завершающего слэша — Astro сохраняет href как написан).
    6. Grep-контроль использования компонентов (R3): по файлам src/pages/** и src/layouts/** искать импорты вида import X from '../components/X.astro'; каждый из 8 компонентов (Button, Link, SectionHeading, ProjectCard, Media, Tag, Nav, Footer) обязан быть импортирован хотя бы в одном файле; отсутствие → violation с именем компонента. (Seo и Icon* в список не входят.)
    7. Проверка 0 тегов <script> в dist (R4): для каждого собранного dist/**/*.html — любое вхождение '<script' (без учёта регистра) → violation с именем файла.
    8. Self-tests: добавить фикстуры для каждого нового правила (временная директория mkdtempSync, паттерн runSelfTest): (a) good — токены с bp-группой и всеми новыми токенами, компонент с var()-переходом, media-запрос 768px → 0 нарушений; (b) bad-bp — tokens без bp-группы → нарушение 'bp (--bp-*)'; (c) bad-media — компонент с @media (min-width: 767px) → нарушение; (d) bad-transition — компонент с transition: color 150ms ease → нарушение, и отдельно файл с transition: color var(--motion-fast) var(--ease-standard) → чисто; (e) bad-dist — временный dist/index.html с 2 aria-current и dist/work/index.html с href="/lab" → нарушения W1; good-dist — по одному aria-current с верным href → чисто; dist-отсутствие → не fail; (f) bad-usage — страница без импорта одного из 8 компонентов → нарушение; (g) bad-script — dist-страница с <script> → нарушение.
  </action>
  <verify>
    <automated>node --check scripts/check-tokens.mjs && node scripts/check-tokens.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-tokens.mjs содержит GROUPS с 6-й группой 'bp (--bp-*)' и render-сообщение с «6 групп»
    - scripts/check-tokens.mjs содержит проверки наличия --text-lead, --text-caption, --ease-enter, --ease-exit, --color-line, --project-clay, --project-olive, --project-slate, --project-plum и clamp( в --text-display
    - scripts/check-tokens.mjs содержит MEDIA_PX_RE (/@media\s*\(min-width:\s*(\d+)px\)/gi) и чтение значений --bp-md/--bp-lg из tokens.css
    - scripts/check-tokens.mjs содержит grep-правило transition по src/components с детекцией литералов \d+ms и cubic-bezier(
    - scripts/check-tokens.mjs содержит W1-ассерт: ровно одно aria-current="page" на dist-страницу + сверка href с маршрутом; guard при отсутствии dist (предупреждение, не fail)
    - scripts/check-tokens.mjs содержит grep-контроль импорта 8 компонентов в src/pages и src/layouts
    - scripts/check-tokens.mjs содержит проверку отсутствия '<script' в dist/**/*.html
    - runSelfTest обновлён: GOOD_TOKENS содержит bp-группу; новые фикстуры (b)-(g) классифицируются верно
    - node --check и --self-test завершаются кодом 0
  </acceptance_criteria>
  <done>check-tokens.mjs расширен всеми 7 новыми правилами с self-tests; node --check и --self-test зелёные; контракты R1/R2/R3/R4/R8 реализованы в логике скрипта</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: scripts/check-contrast.mjs (новый) + scripts/check-theme.mjs (новый) + wiring npm verify</name>
  <files>scripts/check-contrast.mjs, scripts/check-theme.mjs, package.json</files>
  <read_first>
    - 02-RESEARCH.md (Pattern 5, строки 297-312; Code Example 5 — формула WCAG + эталоны, строки 456-471; Don't Hand-Roll «Контраст текста», строка 327-328)
    - 02-PATTERNS.md (раздел scripts/check-contrast.mjs, строки 369-395; Shared Pattern «Скрипт верификации», строки 442-448)
    - 02-UI-SPEC.md (Color — перечень пар, строки 119-121; Project accent palette, строки 105-117)
    - 02-SPEC.md (Requirement 5 — AC про невалидный theme; Requirement 8)
    - .planning/STATE.md (решение 01-02 T1 — Windows-спавн npm через cmd.exe /d /s /c + windowsVerbatimArguments)
  </read_first>
  <action>
    Создать scripts/check-contrast.mjs: каркас копируется из check-tokens.mjs (импорты node:fs/node:path, ROOT через fileURLToPath, walk, render(), runSelfTest() с mkdtempSync-фикстурами, CLI-диспетчер с флагом --self-test). Логика: (1) чтение src/styles/tokens.css; извлечение значений по именам токенов (регулярное выражение /--([a-z0-9-]+):\s*(#[0-9a-fA-F]{6})\b/g); (2) фиксированный перечень из 20 пар (по UI-SPEC Color, строки 119-121): ink×bg, ink×surface, ink-muted×bg, ink-muted×surface, accent×bg, accent×surface, accent-ink×accent, destructive×bg, project-clay×bg, project-clay×surface, project-olive×bg, project-olive×surface, project-slate×bg, project-slate×surface, project-plum×bg, project-plum×surface, accent-ink×project-clay, accent-ink×project-olive, accent-ink×project-slate, accent-ink×project-plum (имена токенов с префиксом --color-/--project-); (3) формула WCAG 2.x: относительная яркость через линеаризацию sRGB (v <= 0.03928 ? v/12.92 : ((v+0.055)/1.055)^2.4), ratio = (hi+0.05)/(lo+0.05) — код из RESEARCH Code Example 5; (4) порог 4.5:1 для обычного текста (AA); --color-line исключён из перечня (декоративный, RESEARCH строка 471); отсутствующий в tokens.css токен пары → violation «токен не найден»; (5) вывод: таблица пар с ratio, exit 1 при любом нарушении. Self-tests: эталоны RESEARCH строки 471 — #000000×#FFFFFF = 21:1 (pass), #A84B32×#FAFAF7 = 5.40:1 (pass), #DAD9D3×#FAFAF7 = 1.35:1 (fail-path — проверка, что нарушение детектируется); фикстура tokens.css с полным набором токенов (включая 4 --project-*) → 0 нарушений; фикстура без --project-plum → violation «токен не найден: --project-plum».
    Создать scripts/check-theme.mjs (R5, negative-фикстура, паттерн check-collections): (1) записывает временный файл src/pages/zz-check-theme.astro с импортом ProjectCard из '../components/ProjectCard.astro' и рендером <ProjectCard title="zz-check" theme="bad" />; (2) спавнит npm run check (на Windows — cmd.exe /d /s /c + windowsVerbatimArguments, паттерн verify-preview.mjs, STATE 01-02 T1) и ожидает exit code != 0 (TS strict: theme не входит в union); (3) если exit == 0 → violation (невалидное значение не поймано); (4) удаляет файл в finally и проверяет, что в src/pages не осталось файлов zz-check-*; (5) повторный npm run check после удаления должен быть exit 0 (необязательная проверка, при недоступности — предупреждение). Exit 1 при любой ошибке. --self-test: логика создания/удаления фикстуры на временной директории без запуска astro check (паттерн check-collections self-test). Реальный прогон против ProjectCard — в плане 02-04 Task 2 (до этого компонента не существует, прогон дал бы ложное срабатывание).
    Обновить package.json: добавить скрипт "check-contrast": "node scripts/check-contrast.mjs" и "check-theme": "node scripts/check-theme.mjs"; расширить verify до: "npm run build && npm run check-seo && npm run check-tokens && npm run check-contrast && npm run check-collections && node scripts/check-prohibitions.mjs" (check-theme не включается в verify — требует собранного ProjectCard и дублирует astro check; прогоняется в 02-04 Task 2). Новых пакетов НЕ устанавливать (RESEARCH Standard Stack: «npm install не запускается»).
  </action>
  <verify>
    <automated>node --check scripts/check-contrast.mjs && node scripts/check-contrast.mjs --self-test && node --check scripts/check-theme.mjs && node scripts/check-theme.mjs --self-test && node -e "const p=require('./package.json');if(!p.scripts['check-contrast'])process.exit(1);if(!p.scripts.verify.includes('check-contrast'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-contrast.mjs содержит luminance()/ratio() по формуле WCAG (порог 4.5:1), перечень из 20 пар UI-SPEC, детекцию «токен не найден», исключение --color-line
    - node scripts/check-contrast.mjs --self-test exit 0: эталоны 21:1 и 5.40:1 проходят, пара 1.35:1 детектируется как нарушение
    - scripts/check-theme.mjs создаёт src/pages/zz-check-theme.astro с theme="bad", ожидает exit != 0 от npm run check, удаляет фикстуру в finally, проверяет чистоту дерева от zz-check-* файлов
    - node scripts/check-theme.mjs --self-test exit 0
    - package.json содержит скрипты check-contrast и check-theme; verify включает check-contrast после check-tokens
  </acceptance_criteria>
  <done>check-contrast.mjs (20 пар, WCAG, self-tests с эталонами) и check-theme.mjs (negative-фикстура theme) созданы; verify-цепочка дополнена check-contrast; все self-tests зелёные</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| локальная ФС → скрипты | Скрипты читают src/, dist/, package.json и спавнят npm run check/build (check-theme.mjs) |
| CI → скрипты | Скрипты выполняются в GitHub Actions (lint → check → build); в CI verify-цепочка не запускается, но скрипты доступны |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | scripts/*.mjs (исполнение node) | low | mitigate | Чистые чтения файлов + spawn npm run check для фикстур; фикстуры — во временных директориях (mkdtempSync) и удаляются в finally; сетевых вызовов и postinstall-логики нет |
| T-02-01 | Tampering | check-theme.mjs фикстура src/pages/zz-check-theme.astro | low | mitigate | Файл удаляется в finally; после self-test и прогона рабочее дерево проверяется на отсутствие zz-check-* файлов; файл не коммитится |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Фаза НЕ устанавливает пакетов (RESEARCH Package Legitimacy Audit: gate не применим, стандартный стек фазы 1 неизменен); npm install не запускается ни в одном плане — поверхность цепочки поставки не расширяется |
| T-02-01 | Information Disclosure | Вывод скриптов (пути, значения токенов, dist-контент) | low | accept | Локальный вывод и CI-логи; чувствительных данных в проекте нет (статический SSG) |
</threat_model>

<verification>
- [ ] node --check и --self-test всех трёх скриптов зелёные (проверки выше)
- [ ] Скрипты не оставляют артефактов: git status не содержит zz-check-* файлов после self-test
- [ ] Полный прогон против реального проекта — в планах-потребителях: bp-группа и токены — 02-03 (check-tokens/check-contrast реальный прогон), использование компонентов — 02-05, negative-фикстура theme — 02-04 Task 2, W1-ассерт — 02-03 Task 1 (после build)
</verification>

<success_criteria>
- [ ] Все 3 скрипта существуют, синтаксически валидны, self-test зелёный
- [ ] Проверяемое поведение каждого скрипта соответствует строке Per-Task Verification Map в 02-VALIDATION.md (R1/R2/R3/R4/R5/R8)
- [ ] Wave 0 gaps 1-6 закрыты: 1-3 и 5-6 здесь, gap 4 (реальный прогон negative-фикстуры) — 02-04
</success_criteria>

<output>
Create `.planning/phases/02-vizualnaya-sistema/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `scripts/check-tokens.mjs` — расширен: CLI `node scripts/check-tokens.mjs [--self-test]`; GROUPS теперь 6 (добавлена `bp (--bp-*)`); новые audit-правила: (1) наличие `--text-lead`/`--text-caption`/`--ease-enter`/`--ease-exit`/`--color-line`/`--project-clay`/`--project-olive`/`--project-slate`/`--project-plum` + `clamp(` в `--text-display`; (2) сверка чисел `@media (min-width: Npx)` (только min-width) в `src/components|src/layouts|src/pages` со значениями `--bp-md`/`--bp-lg`; (3) grep-правило: transition в `src/components` только через `var(--motion-*)`/`var(--ease-*)`, литералы `\d+ms`/`cubic-bezier(` → нарушение; (4) W1-ассерт по `dist/**/*.html`: ровно один `aria-current="page"`, href == маршруту файла (`index.html → '/', {name}/index.html → '/{name}'`), guard при отсутствии dist; (5) grep-контроль импорта 8 компонентов (Button, Link, SectionHeading, ProjectCard, Media, Tag, Nav, Footer) в `src/pages`/`src/layouts`; (6) 0 вхождений `<script` в dist
- `scripts/check-contrast.mjs` — НОВЫЙ: CLI `node scripts/check-contrast.mjs [--self-test]`; функции `luminance(hex)`/`ratio(a,b)` (WCAG 2.x, линеаризация sRGB); константа PAIRS — 20 пар токенов UI-SPEC; порог 4.5:1; детекция «токен не найден»; self-tests: 21:1, 5.40:1, 1.35:1 (fail-path)
- `scripts/check-theme.mjs` — НОВЫЙ: CLI `node scripts/check-theme.mjs [--self-test]`; создаёт `src/pages/zz-check-theme.astro` с `theme="bad"`, ожидает exit != 0 от `npm run check`, удаляет фикстуру в finally; Windows-спавн через `cmd.exe /d /s /c` + `windowsVerbatimArguments`
- `package.json` — скрипты: `check-contrast`, `check-theme`; `verify` = `npm run build && npm run check-seo && npm run check-tokens && npm run check-contrast && npm run check-collections && node scripts/check-prohibitions.mjs`
