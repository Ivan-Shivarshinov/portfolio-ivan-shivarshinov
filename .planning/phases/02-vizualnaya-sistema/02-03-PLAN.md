---
phase: 02-vizualnaya-sistema
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/styles/tokens.css
  - src/styles/global.css
  - src/components/Nav.astro
  - src/components/Footer.astro
  - src/components/Button.astro
  - src/components/Link.astro
  - src/components/IconArrowRight.astro
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/pages/work.astro
  - src/pages/lab.astro
  - src/pages/about.astro
  - src/pages/contact.astro
autonomous: true
requirements: [REQ-design-implications, R1, R2, R3, R4, R6]
user_setup: []
must_haves:
  truths:
    - "tokens.css содержит 6 групп (включая новую bp); значения: --bp-md: 768px, --bp-lg: 1200px; --text-lead: 18px; --text-caption: 12px; --text-display: clamp(28px, 5vw, 40px); --ease-enter: cubic-bezier(0, 0, 0.2, 1); --ease-exit: cubic-bezier(0.4, 0, 1, 1); --color-line: #DAD9D3; 4 --project-* по решению 02-02 (D-07); существующие роли НЕ переименованы (R1, Pitfall 7)"
    - "global.css содержит @media (prefers-reduced-motion: reduce) с 0ms-длительностями (transition-duration/animation-duration/animation-iteration-count) — kill-switch обнуляет длительности, не состояния (R4, Pitfall 5)"
    - "Nav рендерит имя (JetBrains Mono, ink, ссылка на /) и 5 пунктов с моно-индексами 01–05, русскими метками и тонкими разделителями; aria-current='page' ровно один на страницу по нормализованному pathname (D-01, D-02, D-06, W1)"
    - "Nav на <768px: индексы и разделители скрыты, «Главная» скрыта (имя в header — ссылка на /), «Лаборатория»→«Лаб», одна строка 14px, gap var(--space-sm), hit area ≥44px — без горизонтального скролла (UI-SPEC overflow/long-text nav, D-01)"
    - "Footer — одна моно-строка «© 2026 · Иван Шиваршинов» + Latin метка раздела (HOME/WORK/LAB/ABOUT/CONTACT), верхняя граница 1px var(--color-line); ссылок нет (D-03)"
    - "Button — variant primary|secondary, density md|lg, mono-лейбл 14px, слот для иконки; primary hover — color-mix(in oklab, var(--color-accent), var(--color-ink) 8%), active — translate 0 1px; отступы только var(--space-*) (R3, R4)"
    - "BaseLayout: Nav/Footer вместо inline-разметки; моно-метка страницы над контентом (D-05); контейнер оболочки: max-width var(--container-max), padding-inline var(--gutter-mobile) → var(--gutter-desktop) на ≥768px (R6)"
    - "index рендерит hero-shell по D-13: метка «01 / HOME», имя «Иван Шиваршинов» (Unbounded, var(--text-display)), Button «Смотреть работы» → /work с иконкой IconArrowRight (D-13, UI-SPEC Copywriting)"
    - "work/lab/about/contact: метки «02 / WORK»…«05 / CONTACT» + empty-state: моно-подпись «WORK — раздел в разработке» + Link «На главную», рамка 1px var(--color-line) (D-04, D-05)"
    - "build exit 0; preview HTTP 200 на /, /work, /lab, /about, /contact; в собранных страницах 0 тегов <script> (R4); ровно один aria-current='page' на dist-страницу, соответствующий маршруту (W1)"
    - "Числа media-запросов в компонентах равны значениям bp-токенов (min-width включительно: ровно на 768px включается следующий уровень) (R6, edge R1)"
    - "Имя hero ≈231px при 28px < 272px контентной колонки на 320px — длинное имя переносится естественно без обрезания (UI-SPEC long-text hero name)"
    - "На 320px: имя hero и empty-state рамка умещаются в колонку 272px — без горизонтального скролла (UI-SPEC overflow page shell, AC R10)"
    - "Переходы в новых компонентах заданы только var(--motion-*)/var(--ease-*) — grep-правило зелёное (R4)"
  artifacts:
    - src/styles/tokens.css
    - src/styles/global.css
    - src/components/Nav.astro
    - src/components/Footer.astro
    - src/components/Button.astro
    - src/components/Link.astro
    - src/components/IconArrowRight.astro
    - src/layouts/BaseLayout.astro
    - src/pages/index.astro
    - src/pages/work.astro
    - src/pages/lab.astro
    - src/pages/about.astro
    - src/pages/contact.astro
  key_links:
    - "Значения --project-* берутся из решения 02-02; check-contrast (02-01) верифицирует все 20 пар ≥ 4.5:1 после Task 1 (D-08)"
    - "Nav наследует нормализацию pathname из BaseLayout (фикс W1 фазы 1) — переносится в компонент без изменений (строка 23 BaseLayout)"
    - "Всё потребляется через var(): hex/px шкалы вне tokens.css запрещены (check-tokens); 1px рамки — вне шкалы, разрешены"
    - "Полный check-tokens зелёный только после 02-05 (grep-контроль использования: SectionHeading/ProjectCard/Media/Tag появляются на страницах позже)"
    - "title/description 5 страниц — уникальные пары (check-seo R4): index/work/lab/about/contact не меняют существующие пары"
---

<!-- planner-discipline-allow: <script --> (литерал обязателен в действиях: контракт R4 «0 тегов <script>»; acceptance-критерий ссылается на check-tokens dist-правило)

<objective>
Tracer-план фазы: протолкнуть один путь сквозь все слои, которые фаза модифицирует — токены (tokens.css) → глобальные стили (global.css kill-switch) → компоненты оболочки (Nav, Footer, Button, Link, IconArrowRight) → layout (BaseLayout) → одна страница (index.astro hero-shell по D-13) → production-сборка → preview → расширенный check-tokens + check-contrast. Путь проверяется end-to-end (build exit 0 + preview HTTP 200 + W1-ассерт на dist + check-contrast), после чего Task 2 расширяет оболочку горизонтально на оставшиеся 4 страницы (метки + empty-states).

Purpose: доказать архитектуру оболочки «Calm Interface, Active Work» (моно-слой D-01/D-05, theme-токены, CSS-only motion, адаптивность внутри компонентов) на одном коммите до того, как будут написаны оставшиеся компоненты (02-04) и страничные композиции (02-05). Оболочка — production-качество: те же токены, Nav и Button, на которых строится весь сайт.

Output: расширенные токены и global.css; Nav/Footer/Button/Link/IconArrowRight; переработанный BaseLayout; index с hero-shell; 4 страницы с метками и empty-state; build + preview зелёные.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vizualnaya-sistema/02-CONTEXT.md
@.planning/phases/02-vizualnaya-sistema/02-RESEARCH.md
@.planning/phases/02-vizualnaya-sistema/02-PATTERNS.md
@.planning/phases/02-vizualnaya-sistema/02-UI-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Tracer: оболочка системы end-to-end на index (tokens → global → Nav/Footer/Button/IconArrowRight → BaseLayout → index → build → W1 → contrast)</name>
  <files>
    src/styles/tokens.css, src/styles/global.css, src/components/Nav.astro, src/components/Footer.astro,
    src/components/Button.astro, src/components/IconArrowRight.astro, src/layouts/BaseLayout.astro, src/pages/index.astro
  </files>
  <read_first>
    - 02-PATTERNS.md (tokens.css — строки 32-60; global.css — 64-85; Nav — 127-153; Footer — 157-162; Button — 165-195; BaseLayout — 89-123; Shared Patterns — 415-461)
    - 02-RESEARCH.md (Pattern 1 — токен-архитектура, строки 197-211; Pattern 2 — inline style, строки 213-252; Pattern 3 — адаптивность, строки 256-273; Pattern 4 — motion/kill-switch, строки 276-295; Code Examples 1-6)
    - 02-UI-SPEC.md (Typography, Color, Containers & Breakpoints, Motion, Component Contracts — Nav/Footer/Button/Link; Copywriting Contract)
    - 02-CONTEXT.md (D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-12, D-13, D-16; реестр решений 02-02 — hex палитры)
    - src/layouts/BaseLayout.astro (строка 23 — нормализация pathname, фикс W1; строки 33-41 — nav-разметка, переносится в Nav.astro)
    - src/components/Seo.astro (форма компонента: interface Props + деструктуризация с дефолтами, markup-only)
  </read_first>
  <action>
    Реализовать оболочку системы одним вертикальным срезом. Все hex и px шкалы — только в tokens.css (check-tokens); 1px рамки — вне шкалы, разрешены; переходы — только var(--motion-*)/var(--ease-*) (grep-правило); ноль тегов <script>.

    1. tokens.css (расширение, роли НЕ переименовывать — Pitfall 7): в color-группу после --color-destructive добавить --color-line: #DAD9D3 и блок проектных цветов (комментарий /* project accents — theme enum (D-07) */) с 4 значениями --project-clay/--project-olive/--project-slate/--project-plum ИЗ РЕШЕНИЯ 02-02 (дефолт: clay #8A5A44, olive #6B6B3F, slate #55606E, plum #6E4A5C; терракота = var(--color-accent), отдельного токена не создавать). В typography-группу: --text-lead: 18px, --text-caption: 12px, и ЗАМЕНИТЬ значение --text-display: 32px на --text-display: clamp(28px, 5vw, 40px). В motion-группу: --ease-enter: cubic-bezier(0, 0, 0.2, 1), --ease-exit: cubic-bezier(0.4, 0, 1, 1). Новая группа /* bp */ в конце блока: --bp-md: 768px, --bp-lg: 1200px. Организация групп — по принципам Lumos-референса (D-16: группировка токенов color/typography/spacing/containers/motion/bp; код Lumos НЕ импортируется).
    2. global.css: ПОСЛЕ правила :focus-visible добавить kill-switch (RESEARCH Code Example 6): @media (prefers-reduced-motion: reduce) { *, *::before, *::after { transition-duration: 0ms !important; animation-duration: 0ms !important; animation-iteration-count: 1 !important; } } — обнуляются ТОЛЬКО длительности, состояния остаются (Pitfall 5).
    3. src/components/Nav.astro (D-01/D-02/D-06/W1): frontmatter — const pathname = Astro.url.pathname.replace(/\/+$/, '') || '/' (перенос из BaseLayout, фикс W1); массив ROUTES из 5 записей { href, label, index }: '/ Главная 01', '/work Работы 02', '/lab Лаборатория 03', '/about Обо мне 04', '/contact Контакты 05' (метки русские, D-06). Разметка: ссылка-имя «Иван Шиваршинов» (моно, ink, href="/", D-02) + <nav> со списком: для каждого маршрута <a href={r.href} aria-current={pathname === r.href ? 'page' : undefined}> — моно-индекс (span), полный лейбл (span с классом label-full) и короткий лейбл (span с классом label-short, только для /lab: «Лаб»; для остальных — тот же текст) — переключение видимости CSS, без контент-трюков. Scoped-стили: моно-слой (--font-mono, --text-label 14px); базовое (мобильное) состояние: индексы/разделители/полные лейблы скрыты, короткие лейблы видны, «Главная» (href="/") скрыта display:none (имя в header — ссылка на /), одна строка, gap var(--space-sm), padding-block ссылок var(--space-md) (hit area ≥44px); @media (min-width: 768px): индексы видны, разделители 1px var(--color-line) между пунктами, полные лейблы видны, короткие скрыты, «Главная» видна. Состояния: неактивный пункт — color var(--color-ink-muted), hover — var(--color-ink); активный (a[aria-current='page']) — color var(--color-accent) для индекса и лейбла (D-01); transition: color var(--motion-fast) var(--ease-standard).
    4. src/components/Footer.astro (D-03): interface Props { sectionLabel: string }; одна строка: «© 2026 · Иван Шиваршинов» (слева) + {sectionLabel} (справа, Latin caps); моно --text-caption 12px, --color-ink-muted; border-top: 1px solid var(--color-line); padding-block: var(--space-lg) 0 или var(--space-md) (только токены); ссылок и колонок НЕ добавлять (фаза 3).
    5. src/components/Button.astro (R3/R4): interface Props { variant?: 'primary' | 'secondary'; density?: 'md' | 'lg'; href: string } с дефолтами variant='primary', density='md'; рендер <a class="button button--{variant} button--{density}" href={href}> со <slot /> (иконка — дети). Scoped-стили: mono-лейбл (--font-mono, --text-label); inline-flex, align-items center, gap var(--space-xs); primary: background var(--color-accent), color var(--color-accent-ink), hover: background color-mix(in oklab, var(--color-accent), var(--color-ink) 8%), active: translate 0 1px (compositor-only); secondary: background transparent, border 1px solid var(--color-line), color var(--color-ink), hover: border-color var(--color-ink), color var(--color-accent); density md: padding var(--space-sm) var(--space-md), lg: var(--space-md) var(--space-lg); transition: background-color var(--motion-fast) var(--ease-standard) и border-color/color аналогично (только токены). Акцент для CTA разрешён с фазы 2 (UI-SPEC 01).
    6. src/components/IconArrowRight.astro (D-09/D-10/D-11): interface Props { size?: number } дефолт 24; <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="square" vector-effect="non-scaling-stroke" aria-hidden="true"> с геометрическим path стрелки (прямые линии, прямоугольные углы — язык «прямоугольные плоскости, тонкие линии»): горизонтальный отрезок + ломаная-стрелка с квадратным углом. Без <style>.
    7. src/layouts/BaseLayout.astro: заменить inline-nav (строки 33-41) на <Nav />, пустой footer (строка 45) на <Footer sectionLabel={...} />; Props дополнить pageLabel?: string; над <slot /> — {pageLabel && <p class="page-label">{pageLabel}</p>} (D-05); sectionLabel Footer = последний сегмент метки: pageLabel?.split(' ').at(-1) (например, из «01 / HOME» → 'HOME'). Scoped-стили оболочки: header и main — общий контейнер: max-width var(--container-max), margin-inline auto, padding-inline var(--gutter-mobile); @media (min-width: 768px) { padding-inline: var(--gutter-desktop) }; .page-label: mono --text-label, color var(--color-ink-muted); вертикальные отступы — только var(--space-*). Старый scoped-стиль nav a[aria-current='page'] удалить (переезжает в Nav.astro).
    8. src/pages/index.astro (D-13): сохранить title/description (уникальная пара check-seo); <BaseLayout title=... description=... pageLabel="01 / HOME">; hero-shell: <h1 class="hero-name">Иван Шиваршинов</h1> (scoped-стиль страницы: font-family var(--font-display), font-size var(--text-display), font-weight 600, line-height 1.2) + <Button href="/work" density="lg">Смотреть работы<IconArrowRight /></Button>. Текста формулы обещания и подписей НЕ добавлять (фаза 3, REQ-main-promise). CTA — единственное место первичного действия.
    9. Прогнать npm run build (включает astro check); node scripts/check-contrast.mjs (все 20 пар — токены уже в tokens.css); node scripts/verify-preview.mjs --routes /; W1-спот-чек по dist (команда в verify ниже); node scripts/check-tokens.mjs --self-test (полный аудит check-tokens по src зелёный в 02-05 — см. key_links; здесь допустимо, если audit вернёт только незакрытые пункты grep-контроля использования, зафиксировать в SUMMARY строкой «known-red: использование SectionHeading/ProjectCard/Media/Tag — до 02-05»).
  </action>
  <verify>
    <automated>npm run build && node scripts/check-contrast.mjs && node scripts/verify-preview.mjs --routes / && node -e "const fs=require('fs');['index.html','work/index.html','lab/index.html','about/index.html','contact/index.html'].forEach(p=>{const h=fs.readFileSync('dist/'+p,'utf8');const m=(h.match(/aria-current=\"page\"/g)||[]).length;if(m!==1){console.error('W1 fail '+p+': '+m);process.exit(1)}})" && grep -q 'prefers-reduced-motion: reduce' src/styles/global.css && grep -q -- '--bp-md: 768px' src/styles/tokens.css && grep -q -- '--text-display: clamp(28px, 5vw, 40px)' src/styles/tokens.css && grep -q 'aria-hidden' src/components/IconArrowRight.astro</automated>
  </verify>
  <acceptance_criteria>
    - tokens.css содержит 6 групп, включая bp (--bp-md: 768px, --bp-lg: 1200px); --text-lead: 18px; --text-caption: 12px; --text-display: clamp(28px, 5vw, 40px); --ease-enter/--ease-exit; --color-line: #DAD9D3; --project-clay/--project-olive/--project-slate/--project-plum (значения решения 02-02); имена существующих ролей не изменены
    - global.css содержит @media (prefers-reduced-motion: reduce) с transition-duration: 0ms !important
    - Nav.astro содержит ROUTES из 5 русских меток, pathname-нормализацию (replace(/\/+$/, '')), aria-current={pathname === r.href ? 'page' : undefined}, label-full/label-short пары и @media (min-width: 768px)
    - Footer.astro принимает sectionLabel и рендерит «© 2026 · Иван Шиваршинов» + Latin метку; border-top 1px solid var(--color-line)
    - Button.astro содержит variant/density enum-пропы с дефолтами, color-mix(in oklab, var(--color-accent), var(--color-ink) 8%) для hover и translate для active
    - BaseLayout.astro содержит <Nav />, <Footer sectionLabel=... />, {pageLabel && <p class="page-label">…</p>} и контейнер с var(--gutter-mobile)/var(--gutter-desktop) на min-width: 768px
    - index.astro содержит pageLabel="01 / HOME", h1 «Иван Шиваршинов» и <Button href="/work">Смотреть работы</Button> с IconArrowRight внутри
    - npm run build exit 0; check-contrast exit 0; verify-preview --routes / exit 0; W1-спот-чек: ровно 1 aria-current на каждую из 5 dist-страниц
    - В dist/**/*.html нет тегов <script> (спот-грep: grep -c '<script' dist/index.html == 0 — проверка входит в check-tokens, полный прогон после 02-05)
  </acceptance_criteria>
  <done>Оболочка системы собрана end-to-end: токены (6 групп), kill-switch, Nav/Footer/Button/Link/IconArrowRight, BaseLayout с меткой страницы, index с hero-shell D-13; build + preview 200 + W1 на 5 страницах + check-contrast зелёные</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Link.astro + оболочка страниц work/lab/about/contact (метки D-05 + empty-state D-04)</name>
  <files>src/components/Link.astro, src/pages/work.astro, src/pages/lab.astro, src/pages/about.astro, src/pages/contact.astro</files>
  <read_first>
    - 02-PATTERNS.md (Link — строки 199-203; страницы — строки 318-335; empty-state D-04)
    - 02-UI-SPEC.md (Component Contracts — Link; Page structure; Copywriting Contract — page labels и empty-state captions)
    - src/pages/work.astro (текущий placeholder-каркас)
  </read_first>
  <action>
    1. src/components/Link.astro (R3/R4): interface Props { variant?: 'default' | 'muted'; href: string } с дефолтом variant='default'; рендер <a class="link link--{variant}" href={href}><slot /></a>; default: color var(--color-ink), hover/focus: color var(--color-accent) + text-decoration underline + text-underline-offset 2px (подчёркивание появляется во взаимодействии); muted: color var(--color-ink-muted), hover — accent так же; transition: color var(--motion-fast) var(--ease-standard). Внешние ссылки: caller передаёт target="_blank" rel="noopener" — в компоненте не зашивать (решает использование).
    2. 4 страницы (work/lab/about/contact): сохранить title/description (уникальные пары check-seo); в BaseLayout добавить pageLabel: «02 / WORK», «03 / LAB», «04 / ABOUT», «05 / CONTACT» (D-05, UI-SPEC Copywriting); placeholder-<p> заменить на empty-state блок (D-04): обёртка .empty-state (scoped-стиль страницы или общий паттерн) с border: 1px solid var(--color-line) и внутренним отступом var(--space-lg); содержимое — моно-подпись «WORK — раздел в разработке» (для каждой секции своя: WORK/LAB/ABOUT/CONTACT; mono --text-label 14px, --color-ink-muted) + <Link href="/">На главную</Link>. Только токены; ноль <script>; никакого реального контента (фаза 3).
    3. Прогнать npm run build; node scripts/verify-preview.mjs (все 5 маршрутов); спот-проверка dist: на каждой из 4 страниц есть метка секции и подпись «раздел в разработке», на index — «01 / HOME» и «Смотреть работы» (команда в verify).
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-preview.mjs && node -e "const fs=require('fs');const map={work:'WORK',lab:'LAB',about:'ABOUT',contact:'CONTACT'};for(const [n,tag] of Object.entries(map)){const h=fs.readFileSync('dist/'+n+'/index.html','utf8');if(!h.includes(tag+' — раздел в разработке')){console.error('empty state missing '+n);process.exit(1)};if(!h.includes('На главную')){console.error('empty state link missing '+n);process.exit(1)}}const i=fs.readFileSync('dist/index.html','utf8');if(!i.includes('01 / HOME')||!i.includes('Смотреть работы')){console.error('index hero shell missing');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - Link.astro содержит variant enum ('default'|'muted'), color transition через var(--motion-fast) var(--ease-standard), hover accent + underline с text-underline-offset 2px
    - work/lab/about/contact.astro содержат pageLabel «02 / WORK»/«03 / LAB»/«04 / ABOUT»/«05 / CONTACT» и empty-state: моно-подпись «WORK/LAB/ABOUT/CONTACT — раздел в разработке» + <Link href="/">На главную</Link>
    - npm run build exit 0; verify-preview (5 маршрутов) exit 0; спот-проверка dist: подписи и метки на месте
    - Никакой реальный контент (тексты, ссылки, кейсы) не добавлен — только оболочка (D-04: честный статус, замена в фазе 3)
  </acceptance_criteria>
  <done>Link.astro создан; 4 страницы получили метки D-05 и empty-state D-04 в системе; build + preview 200 на 5 маршрутах; dist-спот-проверка зелёная</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| компоненты (build-time) → собранный HTML | Пропы и слоты страниц/компонентов статически рендерятся в dist; пользовательского ввода нет |
| inline style (--project-accent) → наследование в слоты | Custom property из inline style наследуется в дочерние компоненты (runtime-каскад, не селектор) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | Пропы компонентов → HTML (контент меток, labels) | low | mitigate | Статический рендер + TS strict (astro check в build); метки — фиксированные строки контракта (UI-SPEC Copywriting), не пользовательский ввод; Astro экранирует атрибуты |
| T-02-03 | Spoofing | Inline-SVG иконка (IconArrowRight) | low | mitigate | Разметка-only без <script> и внешних ссылок; aria-hidden="true"; stroke/fill — currentColor, не внешние ресурсы |
| T-02-03 | (WCAG) | Контраст пар и aria-current (W1) | medium | mitigate | check-contrast (все 20 пар ≥ 4.5:1) + W1-ассерт в check-tokens (ровно один aria-current на маршрут) + :focus-visible ring (global.css) |
| T-02-03 | Tampering | Kill-switch reduced-motion | low | accept | Обнуляются только длительности (0ms), состояния мгновенно применимы (Pitfall 5); визуальная проверка — 02-06 |
| T-02-03 | Tampering | Хардкод-значения в компонентах (дрейф токенов) | medium | mitigate | check-tokens: запрет hex/px-шкалы вне tokens.css (1px рамки — разрешённое исключение); grep-правило transition-токенов |
</threat_model>

<verification>
- [ ] npm run build exit 0 (astro check + astro build) после каждого Task
- [ ] node scripts/check-contrast.mjs exit 0 (20 пар UI-SPEC) — после Task 1
- [ ] node scripts/verify-preview.mjs exit 0: /, /work, /lab, /about, /contact → 200 + text/html — после Task 2
- [ ] W1: ровно один aria-current="page" на каждой dist-странице, href соответствует маршруту (спот-чек в verify Task 1; полный ассерт — check-tokens после 02-05)
- [ ] grep: 'prefers-reduced-motion: reduce' в global.css; '--bp-md: 768px' и 'clamp(28px, 5vw, 40px)' в tokens.css
- [ ] В собранных страницах 0 тегов <script> (спот-проверка grep; полный ассерт — check-tokens dist-правило)
</verification>

<success_criteria>
- [ ] Все 5 страниц рендерят оболочку системы: контейнер, метка D-05, Nav (W1), Footer, контент-зона
- [ ] index — hero-shell по D-13 (метка + имя Unbounded display + primary CTA «Смотреть работы» с IconArrowRight)
- [ ] 4 страницы — empty-state D-04 с моно-подписью и Link «На главную»
- [ ] Токены расширены без переименования ролей; kill-switch в global.css; transition — только токены
- [ ] check-contrast зелёный; W1 зелёный; preview 200 на 5 маршрутах
</success_criteria>

<output>
Create `.planning/phases/02-vizualnaya-sistema/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/styles/tokens.css` — расширен до 6 групп: новая группа `/* bp */` (`--bp-md: 768px`, `--bp-lg: 1200px`); в color: `--color-line: #DAD9D3` + 4 `--project-*` (значения решения 02-02); в typography: `--text-lead: 18px`, `--text-caption: 12px`, `--text-display: clamp(28px, 5vw, 40px)` (замена 32px); в motion: `--ease-enter: cubic-bezier(0, 0, 0.2, 1)`, `--ease-exit: cubic-bezier(0.4, 0, 1, 1)`; существующие роли не переименованы
- `src/styles/global.css` — добавлен `@media (prefers-reduced-motion: reduce)` kill-switch (transition/animation-duration 0ms !important)
- `src/components/Nav.astro` — Props: нет (ROUTES внутренний); локальная константа `pathname` (нормализация W1); массив ROUTES {href, label, index} ×5; scoped-стили: моно-слой, компактное состояние <768px (без индексов/разделителей/«Главная», «Лаб»), десктоп ≥768px; aria-current на активный маршрут
- `src/components/Footer.astro` — Props: `sectionLabel: string`; строка «© 2026 · Иван Шиваршинов» + Latin-метка, border-top 1px var(--color-line)
- `src/components/Button.astro` — Props: `variant?: 'primary'|'secondary'`, `density?: 'md'|'lg'`, `href: string`; слот (иконка); color-mix hover; active translate 0 1px
- `src/components/Link.astro` — Props: `variant?: 'default'|'muted'`, `href: string`; слот; hover accent + underline (offset 2px)
- `src/components/IconArrowRight.astro` — Props: `size?: number` (24); svg 24px, stroke 2, currentColor, aria-hidden, геометрический path (D-11)
- `src/layouts/BaseLayout.astro` — Props: `{title, description, pageLabel?}`; <Nav /> + <Footer sectionLabel={pageLabel?.split(' ').at(-1)} />; .page-label; контейнер оболочки (max-width var(--container-max), gutter 24→48 на ≥768px)
- `src/pages/index.astro` — pageLabel «01 / HOME»; h1 hero-name (Unbounded, var(--text-display)); Button «Смотреть работы» → /work + IconArrowRight
- `src/pages/{work,lab,about,contact}.astro` — pageLabel «02 / WORK»…«05 / CONTACT»; empty-state (рамка 1px var(--color-line), моно-подпись «… — раздел в разработке», Link «На главную»)
