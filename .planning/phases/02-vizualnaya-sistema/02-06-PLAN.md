---
phase: 02-vizualnaya-sistema
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - .planning/phases/02-vizualnaya-sistema/02-VERIFICATION.md
autonomous: false
requirements: [REQ-design-implications, R4, R6]
user_setup: []
must_haves:
  truths:
    - "Полный npm run verify зелёный на финальном состоянии (build + check-seo + check-tokens с W1-ассертом и grep-правилами + check-contrast + check-collections + check-prohibitions) (R8 AC)"
    - "CI зелёный после push: GitHub Actions workflow (lint → check → build) проходит на main (R8 AC)"
    - "Визуальный проход пользователя на 375 / 768 / 1200 зафиксирован в 02-VERIFICATION.md (R6, AC#11)"
    - "На ширине 320px нет горизонтального скролла (AC#10); min-width включительно: ровно на значении bp включается следующий уровень (edge R6)"
    - "prefers-reduced-motion: в эмуляции DevTools длительности переходов 0ms во всех интерактивных состояниях; hover-цвета мгновенно применимы (R4, edge R4, Pitfall 5)"
    - "Нейтральный покой: без взаимодействия интерфейс нейтрален (нет акцентов-заливок и декоративных акцентов); accent появляется только в hover/focus — ссылки, primary CTA, активный nav, focus-ring, заголовок карточки (SC3, R5)"
    - "Главная рендерит выбранный концепт (docs/hero-concept.md): дефолт D-12 — метка «01 / HOME» + имя + CTA; при выборе Figma-варианта — соответствующие элементы в рамках системы (R9)"
    - "SYSTEM DEMO на work.astro и empty-state на lab/about/contact отображаются корректно; метки страниц «02 / WORK»…«05 / CONTACT» на месте (D-04, D-05)"
    - "0 тегов <script> в собранных страницах (проверка check-tokens dist-правила внутри verify) (R4)"
  artifacts:
    - .planning/phases/02-vizualnaya-sistema/02-VERIFICATION.md
  key_links:
    - "02-VERIFICATION.md — вход для /gsd-verify-work (паттерн фазы 1: 01-VERIFICATION.md); фиксирует визуальный проход 320/375/768/1200, эмуляцию reduced-motion, нейтральный покой"
    - "Контраст и W1 уже автоматизированы (check-contrast, W1-ассерт) — визуальный проход проверяет композицию/ритм (R6) и состояние покоя (SC3), которые скриптами не проверяются (RESEARCH Pitfall 8: скриншот-база не выбрана)"
  prohibitions:
    - statement: "Сгенерированные/скачиваемые декоративные медиа НЕ становятся главным визуальным языком (неоновая AI-эстетика, бесконечные градиенты, «ИИ-слоп»); их роль — вторичный слой, контент главенствует"
      status: resolved
      verification: "judgment — визуальный проход пользователя (Task 2); дизайн-направление §10 + REQ-design-implications; фаза 2 изображений не добавляет"
---

<objective>
Финальный гейт фазы: полный npm run verify + push + зелёный CI (Task 1) и визуальный проход пользователя с фиксацией в 02-VERIFICATION.md (Task 2, checkpoint:human-verify). Визуальный проход — единственный слой верификации, который не автоматизируется (композиция/ритм на 375/768/1200, отсутствие скролла на 320, reduced-motion, нейтральный покой) — по контракту фазы скриншот-база НЕ выбрана (RESEARCH Pitfall 8), проверка — живой проход + Stacki.

Purpose: закрыть AC#6 (reduced-motion), AC#10–11 (320/375/768/1200), SC3 (нейтральный покой) и дать пользователю контрольную точку перед /gsd-verify-work. После этого фаза готова к валидации (02-VALIDATION.md nyquist_compliant).

Output: 02-VERIFICATION.md с зафиксированным проходом; зелёный verify и CI; фаза готова к /gsd-verify-work.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vizualnaya-sistema/02-VALIDATION.md
@.planning/phases/02-vizualnaya-sistema/02-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-UI-SPEC.md
@.planning/phases/02-vizualnaya-sistema/02-CONTEXT.md
@.planning/phases/02-vizualnaya-sistema/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: финальный npm run verify + push + зелёный CI</name>
  <files>.planning/phases/02-vizualnaya-sistema/02-VERIFICATION.md</files>
  <read_first>
    - 02-VALIDATION.md (Sampling Rate — «Before /gsd-verify-work: полный npm run verify зелёный + CI зелёный»)
    - 02-SPEC.md (Acceptance Criteria — полный список)
    - .github/workflows/ci.yml (существующий workflow фазы 1: lint → check → build)
  </read_first>
  <action>
    1. Прогнать полный npm run verify на финальном состоянии фазы (build → check-seo → check-tokens → check-contrast → check-collections → check-prohibitions). Все компоненты ожидаемы зелёными: bp-группа и токены (02-03), grep-контроль использования (02-05), W1-ассерт, контраст-пары.
    2. Проверить, что в собранных страницах 0 тегов <script> (входит в check-tokens), и спот-проверить уникальность title/description (check-seo).
    3. Создать .planning/phases/02-vizualnaya-sistema/02-VERIFICATION.md (шаблон — 01-VERIFICATION.md фазы 1): заголовок, статус, перечень проверок с результатами: verify exit 0, 5 маршрутов preview 200, W1, контраст 20 пар, отсутствие скролла 320 (заполняется после Task 2), визуальный проход 375/768/1200 (заполняется после Task 2), reduced-motion (после Task 2), нейтральный покой (после Task 2). Незаполненные секции Task 2 помечаются «pending — визуальный проход пользователя».
    4. git add/commit (если есть незакоммиченные изменения после 02-05) и git push origin main; дождаться завершения GitHub Actions (gh run watch или gh run list) и убедиться, что workflow (lint → check → build) зелёный. При красном CI — исправить и перезапустить до зелёного.
  </action>
  <verify>
    <automated>npm run verify && node -e "const fs=require('fs');const v=fs.readFileSync('.planning/phases/02-vizualnaya-sistema/02-VERIFICATION.md','utf8');if(!v.includes('npm run verify')||!v.includes('pending')){console.error('VERIFICATION.md skeleton incomplete');process.exit(1)}" && git push origin main && gh run list --limit 1 --json status,conclusion --jq '.[0].conclusion' | grep -q success</automated>
  </verify>
  <acceptance_criteria>
    - npm run verify exit 0 (полный набор скриптов фазы 2)
    - 02-VERIFICATION.md создан со скелетом проверок (verify, маршруты, W1, контраст, секции прохода — pending)
    - git push origin main выполнен; последний run GitHub Actions (lint → check → build) имеет conclusion success
    - В dist нет <script>-тегов (проверка внутри check-tokens)
  </acceptance_criteria>
  <done>Финальный verify зелёный; изменения запушены; CI (lint → check → build) зелёный; 02-VERIFICATION.md создан со скелетом прохода</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Визуальный проход: 320/375/768/1200 + reduced-motion + нейтральный покой (R6, R4, SC3)</name>
  <what-built>Вся визуальная система фазы 2: оболочка 5 страниц (Nav с моно-индексами, Footer, метки страниц, контейнер), hero-shell главной (имя Unbounded + CTA «Смотреть работы»), SYSTEM DEMO на work, empty-state на lab/about/contact, компоненты (Button/Link/SectionHeading/ProjectCard/Media/Tag) с CSS-only состояниями и motion-токенами. Автоматизированные проверки (verify, W1, контраст) уже зелёные — проход закрывает неавтоматизируемые слои: композиция/ритм, отсутствие скролла, reduced-motion, нейтральный покой.</what-built>
  <how-to-verify>
    1. Запустить локальный preview: npm run preview (или npm run dev) и открыть http://localhost:4321.
    2. DevTools → responsive mode: проверить ширины 320, 375, 768, 1200 и широкий десктоп (1920):
       - на 320: НЕТ горизонтального скролла; nav — одна строка «Работы / Лаб / Обо мне / Контакты» (без индексов и «Главная»); имя hero «Иван Шиваршинов» помещается (или переносится без обрезания); empty-state рамки влезают; footer-строка влезает;
       - на 375: то же, что 320;
       - на 768 (ровно bp-md): десктоп-состояние nav (индексы + «Главная» + полные лейблы), контейнер с gutter-desktop;
       - на 1200+: контейнер фиксируется на 1200px, сетка карточек SYSTEM DEMO — 2 колонки.
    3. Проверить все 5 страниц: / (hero-shell: метка «01 / HOME», имя, CTA), /work (SYSTEM DEMO: 2 карточки, вторая без мета-строки), /lab, /about, /contact (empty-state «… — раздел в разработке» + «На главную»).
    4. Нейтральный покой (SC3): без наведения интерфейс нейтрален — акцентов-заливок и цветных декоративных элементов нет (кроме активного пункта nav и focus-ring при табе); при hover: ссылки становятся accent + подчёркивание, primary CTA темнеет (color-mix), заголовок карточки становится project-accent (терракота/олива), рамка карточки темнеет, Tag с theme получает цвет; при tab-навигации виден focus-ring (2px accent).
    5. prefers-reduced-motion: DevTools → Rendering → Emulate CSS media feature prefers-reduced-motion: reduce → hover/фокус всех интерактивных элементов: состояния меняются МГНОВЕННО (0ms), цветовые отклики остаются.
    6. При выборе hero-figma (решение 02-02): сверить главную с docs/hero-concept.md — оболочка соответствует выбранному концепту в рамках системы.
    7. Зафиксировать результаты (что проверил, что заметил) в 02-VERIFICATION.md — разделы прохода; при обнаружении дефектов — перечислить их для исправления (план вернётся в исправление, после фиксов проход повторяется).
  </how-to-verify>
  <resume-signal>approved (или описание дефектов для исправления)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-окружение (GitHub Actions) | Выполняет lint → check → build на main; артефакты — только dist и логи |
| браузер пользователя (визуальный проход) | Локальный preview/dev-сервер на localhost:4321 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | CI workflow (lint → check → build) | low | accept | Стандартный workflow фазы 1 (01-07), без изменений в фазе 2; зависимости не обновлялись (фаза не устанавливает пакетов) |
| T-02-06 | Information Disclosure | Логи CI и VERIFICATION.md | low | accept | Публичный репозиторий — осознанное решение (01-01 T3); логи не содержат секретов (нет env-секретов в workflow) |
| T-02-06 | Tampering | 02-VERIFICATION.md (фиксация прохода) | low | accept | Внутренний артефакт планирования; история в git |
</threat_model>

<verification>
- [ ] npm run verify exit 0 на финальном состоянии
- [ ] GitHub Actions: последний run на main — conclusion success (lint → check → build)
- [ ] 02-VERIFICATION.md заполнен: verify, маршруты, W1, контраст + визуальный проход (после Task 2)
- [ ] Чекпоинт Task 2 закрыт пользователем («approved») или дефекты переданы в исправление
</verification>

<success_criteria>
- [ ] Полный verify зелёный + CI зелёный
- [ ] Визуальный проход 320/375/768/1200 зафиксирован в 02-VERIFICATION.md: нет скролла на 320, адаптивность на всех ширинах
- [ ] reduced-motion подтверждён (0ms, состояния остаются); нейтральный покой подтверждён (SC3)
- [ ] Главная соответствует docs/hero-concept.md (R9); фаза готова к /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/02-vizualnaya-sistema/02-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `.planning/phases/02-vizualnaya-sistema/02-VERIFICATION.md` — протокол финального гейта: результаты `npm run verify`, 5 маршрутов preview, W1, контраст (20 пар), визуальный проход 320/375/768/1200 (заполняется в Task 2), reduced-motion, нейтральный покой (SC3), соответствие главной docs/hero-concept.md; вход для `/gsd-verify-work`
- Записи в STATE.md (статус фазы, если применимо по флоу оркестратора)
