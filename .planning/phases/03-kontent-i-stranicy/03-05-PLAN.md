---
phase: 03-kontent-i-stranicy
plan: 05
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - src/pages/about.astro
  - src/pages/lab.astro
  - src/pages/index.astro
  - scripts/check-visual.mjs
autonomous: false
requirements:
  - REQ-positioning-category
  - REQ-main-promise
  - REQ-audience
  - REQ-competency-architecture
  - REQ-tone
  - REQ-growth-trajectory
must_haves:
  truths:
    - "Собранная / содержит формулу обещания из REQ-main-promise дословно (D-13), категорию и подпись; CTA — последний элемент hero (SPEC edge R1 covered)"
    - "На / отсутствуют ярлыки-клише позиционирования (AC R1; проверка — check-tone, scope index.html)"
    - "На / присутствует секция «Избранные работы» с 2–3 карточками из коллекции, ведущими на /work/{slug}/ (SPEC edge R4 covered)"
    - "/about содержит 4 блока: профиль, аудитории, компетенции Build/Improve/Extend, траектория из 3 ступеней (SPEC edge R5 covered)"
    - "/lab содержит 2 блока: направления и процесс; контент не притворяется, что эксперименты существуют (SPEC edge R6 covered)"
    - "Компетенции поданы тремя группами Build/Improve/Extend с иерархией «понятный результат → доказанная специализация → коммерческое усиление → направление роста»; длинный список инструментов не является центром (REQ-competency-architecture)"
    - "check-visual проверяет hover на новых страницах (футер-ссылка), маршрут кейса на скролл и состояние кнопки копирования (UI-SPEC keyboard/error covered)"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "4 блока: PROFILE / AUDIENCES / CAPABILITIES / TRAJECTORY"
      contains: "TRAJECTORY"
    - path: "src/pages/lab.astro"
      provides: "2 блока: DIRECTIONS / PROCESS + честный статус; notes остаётся пустой (D-07)"
      contains: "PROCESS"
    - path: "src/pages/index.astro"
      provides: "Формула обещания (D-13 дословно) + категория + секция «Избранные работы»"
      contains: "Избранные работы"
    - path: "scripts/check-visual.mjs"
      provides: "Hover-цель на футер-ссылку, маршрут кейса в скролл-проверке, шаг копирования email"
      contains: "copy"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/content/projects/*.mdx"
      via: "featured-фильтр + sort по order → 2–3 карточки → /work/{slug}/ (D-08/D-09, Pitfall 10)"
      pattern: "featured"
    - from: "src/pages/about.astro"
      to: "scripts/check-tone.mjs"
      via: "моно-меты PROFILE/AUDIENCES/CAPABILITIES/TRAJECTORY — позитивные ассерты REQUIRED_CONTENT (R5)"
      pattern: "TRAJECTORY"
prohibitions: []
---

<objective>
Контентные страницы фазы: /about с 4 блоками (профиль, аудитории, компетенции B/I/E, траектория), /lab с 2 блоками (направления + процесс, честный статус), / — формула обещания (D-13 дословно) и секция «Избранные работы». Обновление check-visual.mjs под новые страницы (hover-цель, маршрут кейса, шаг копирования).

Purpose: REQ-audience/REQ-competency-architecture/REQ-growth-trajectory/REQ-main-promise/REQ-positioning-category материализуются в страницы по UI-SPEC; тексты — черновик агента + утверждение пользователя (D-04, Claude's Discretion). check-visual обязателен: его hover-проверка опирается на ссылку пустого состояния /lab, которая исчезает с заменой контента.
Output: /about, /lab, / с реальным контентом; обновлённый check-visual.mjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kontent-i-stranicy/03-CONTEXT.md
@.planning/phases/03-kontent-i-stranicy/03-RESEARCH.md
@.planning/phases/03-kontent-i-stranicy/03-PATTERNS.md
@.planning/phases/03-kontent-i-stranicy/03-SPEC.md
@.planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md

# Текущее состояние редактируемых файлов
@src/pages/about.astro
@src/pages/lab.astro
@src/pages/index.astro
@scripts/check-visual.mjs
@docs/positioning_portfolio_ivan_shivarshinov.md
@docs/hero-concept.md
</context>

## Artifacts this phase produces

- `src/pages/about.astro` — 4 блока: PROFILE/«Профиль», AUDIENCES/«Аудитории» (4 ситуации клиента: запустить / привести в порядок / развивать / расширить), CAPABILITIES/«Компетенции» (3 группы Build/Improve/Extend), TRAJECTORY/«Траектория» (3 ступени Product-minded Web Developer → Web Product Developer → Product Engineer); секции — SectionHeading + prose-контракт; без центрирования текста
- `src/pages/lab.astro` — 2 блока: DIRECTIONS/«Направления» (AI-воркфлоу, автоматизации, внутренние инструменты — «что здесь появится») + PROCESS/«Процесс» (как устроены эксперименты) + честная статусная строка; без карточек экспериментов и без ссылок на несуществующие эксперименты
- `src/pages/index.astro` — формула обещания (композиция по 03-UI-SPEC Q5, текст дословно D-13), категория, подпись, CTA последним; секция «Избранные работы» (SectionHeading meta="SELECTED WORK" + grid featured 3 колонки ≥768px)
- `scripts/check-visual.mjs` — hover-цель → футер-ссылка контакта на /lab (логика: изменение цвета), маршрут первого кейса в скролл-проверке, шаг 5 «копирование email» на /contact

<tasks>

<task type="auto">
  <name>Task 1: /about и /lab — черновики контента по UI-SPEC</name>
  <files>src/pages/about.astro, src/pages/lab.astro</files>
  <read_first>
    src/pages/about.astro и src/pages/lab.astro — текущие empty-state (строки 5-17): BaseLayout + класс .empty-state
    src/components/SectionHeading.astro — пропы meta/title/layout
    .planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md — About (R5), Lab (R6), Prose contract, Copywriting Contract
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Pitfall 7 (моно-меты — позитивные ассерты check-tone), Code Example 4 (REQUIRED_CONTENT)
    docs/positioning_portfolio_ivan_shivarshinov.md — PRD §3 (аудитории), §4 (компетенции), §6 (траектория) — материал черновиков
  </read_first>
  <action>
    Черновики текстов — по D-04: файлы пишутся, тексты показываются в чате, коммит — после утверждения Task 2. Оба файла — замена empty-state на контентные блоки (паттерн страниц фазы 2: BaseLayout + плоский список self-closing компонентов, scoped styles только var()-токены).

    1. src/pages/about.astro:
       - BaseLayout: title="Обо мне — Иван Шиваршинов", description — уникальная формулировка под реальный контент (без дублей пар с другими страницами), pageLabel="04 / ABOUT"
       - 4 блока, ритм --space-2xl, каждый — SectionHeading (layout="split" на ≥768px) + проза:
         - meta="PROFILE" title="Профиль" — 2-4 абзаца: путь веб-разработчика, продуктовый подход, честно о зоне роста (без образа гения-одиночки — REQ-tone)
         - meta="AUDIENCES" title="Аудитории" — «с кем работаю»: 4 ситуации клиента из REQ-audience (запустить / привести в порядок / развивать / расширить) + типы команд (технологические компании и SaaS, экспертные фирмы, B2B со сложным контентом, команды, ищущие технического партнёра, агентства и дизайнеры); желаемое восприятие — самостоятельный технический партнёр по веб-направлению
         - meta="CAPABILITIES" title="Компетенции" — иерархия Build / Improve / Extend (REQ-competency-architecture): 3 группы с коротким описанием результата каждой; примеры конкретных задач (Build — сайты и контентные системы; Improve — SEO-санация, перформанс, структура; Extend — веб-инструменты, автоматизации, ИИ в процессах); НЕ список инструментов и НЕ «делаю всё»
         - meta="TRAJECTORY" title="Траектория" — 3 ступени дословно: Product-minded Web Developer → Web Product Developer → Product Engineer (REQ-growth-trajectory), каждая с 1-2 предложениями, что она означает
       - Проза — prose-контракт UI-SPEC (Manrope 16px, ~65ch, заголовки через SectionHeading, ссылки — Link)
    2. src/pages/lab.astro:
       - BaseLayout: title="Лаборатория — Иван Шиваршинов", description — уникальная формулировка, pageLabel="03 / LAB"
       - 2 блока + статусная строка, ритм --space-2xl:
         - meta="DIRECTIONS" title="Направления" — «что здесь появится»: AI-воркфлоу, автоматизации, внутренние инструменты (мост к продуктам — REQ-growth-trajectory, Lab как витрина перехода)
         - meta="PROCESS" title="Процесс" — как устроены эксперименты: от гипотезы до прототипа, критерии, что идёт в работу
         - статусная строка (моно, --text-caption, --color-ink-muted): «Эксперименты в работе — появятся в следующих релизах» (честный статус — R6 AC; точная строка фиксируется, на неё ссылается check-tone REQUIRED_CONTENT)
       - НЕТ карточек экспериментов, НЕТ ссылок на готовые эксперименты, НЕТ притворства (R6)
    3. Тексты показать в чате для утверждения (черновик → правки → коммит, D-04). Моно-меты блоков — латиницей (PROFILE/AUDIENCES/CAPABILITIES/TRAJECTORY/DIRECTIONS/PROCESS) — это позитивные ассерты check-tone.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Собранные /about и /lab открываются с контентными блоками вместо empty-state</human-check>
  </verify>
  <acceptance_criteria>
    - about.astro содержит 4 SectionHeading с метами PROFILE, AUDIENCES, CAPABILITIES, TRAJECTORY; в прозе — 4 ситуации клиента и 3 группы компетенций Build/Improve/Extend и 3 ступени траектории
    - lab.astro содержит 2 SectionHeading с метами DIRECTIONS, PROCESS и статусную строку «Эксперименты в работе — появятся в следующих релизах»; в разметке нет карточек/ссылок на эксперименты
    - В обоих файлах нет классов .empty-state; стили — только var()-токены (check-tokens: нет hex и px-шкалы)
    - `npm run build` — exit 0
  </acceptance_criteria>
  <done>Черновики /about и /lab написаны и показаны в чате; коммит — после Task 2</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Утверждение текстов /about и /lab</name>
  <what-built>Черновики текстов /about (4 блока) и /lab (2 блока + статус) — показаны в чате</what-built>
  <how-to-verify>
    Прочитайте черновики в чате: 1) профиль — без пафоса и образа гения-одиночки (REQ-tone); 2) аудитории — соответствуют REQ-audience; 3) компетенции — иерархия Build/Improve/Extend без длинного списка инструментов (REQ-competency-architecture); 4) траектория — 3 ступени; 5) Lab — направления + процесс, честный статус (R6). Внесите правки формулировок — агент применит их и закоммитит.
  </how-to-verify>
  <resume-signal>Тексты утверждены (или: правки — …)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: / — формула обещания (D-13) + «Избранные работы»; обновление check-visual.mjs</name>
  <files>src/pages/index.astro, scripts/check-visual.mjs</files>
  <read_first>
    src/pages/index.astro — текущий hero-shell (строки 6-21): BaseLayout pageLabel="01 / HOME", h1.hero__name, Button CTA
    docs/hero-concept.md — концепт первого экрана (куда ложится формула, CTA — последний элемент)
    .planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md — Hero «/» (Q5 — композиция top→bottom), Home featured (R4), Copywriting Contract (формула дословно)
    .planning/phases/03-kontent-i-stranicy/03-PATTERNS.md — раздел «src/pages/index.astro» + Shared Patterns (SEO-пары, сортировка)
    scripts/check-visual.mjs — Шаг 3 checkLinkHover (строки 156-170: getByRole('link', { name: ... }) на /lab), Шаг 4 checkReducedMotion (173-181), DEFAULT_ROUTES (22), checkLinkHoverLogic (228-230)
  </read_first>
  <action>
    1. src/pages/index.astro:
       - В hero (существующая колонка, gap var(--space-xl)) под h1.hero__name вставить формулу по композиции 03-UI-SPEC Q5 (top→bottom, ритм --space-md/--space-lg):
         a) «Создаю и развиваю сложные сайты, контентные системы и веб-инструменты.» — роль Lead (--text-lead, --font-sans, --color-ink)
         b) «Product-minded web developer.» — моно-слой (--font-mono, --text-label, --color-ink-muted)
         c) «Соединяю Webflow, код, SEO/AEO и возможности ИИ…» — роль Body (--text-body, --color-ink-muted)
         d) категория «веб-разработчик с продуктовым подходом» — роль Body (--color-ink-muted)
         e) подпись «от идеи и структуры до запуска и дальнейшего развития» — моно (--font-mono, --text-caption, --color-ink-muted)
         f) CTA «Смотреть работы» + IconArrowRight — ПОСЛЕДНИЙ элемент секции (D-13, 02 D-13; не менять существующий Button)
         Тексты a-e — дословно (D-13), без редактирования. Секция hero: при наличии формулы первая строка «Создаю и развиваю…» — Lead (не Label).
       - Ниже hero — секция «Избранные работы»: SectionHeading (meta="SELECTED WORK", title="Избранные работы") + grid карточек (1 колонка <768px / 3 колонки ≥768px): `const featured = projects.filter((p) => p.data.featured).sort((a, b) => a.data.order - b.data.order);` — карточки как в work.astro (03-03): ProjectCard с title/meta/theme/summary/href=`/work/${slug}/` + Media/Image слот + Tags (stack.slice(0, 3)). featured-фильтр поверх той же сортировки по order (Pitfall 10). Секция рендерит все featured-записи (в финале — 3, диапазон 2–3 гарантирует check-collections, D-09).
       - title/description страницы оставить (формула уже в description) или уточнить — уникальная пара сохраняется.
    2. scripts/check-visual.mjs:
       - Шаг 3 (checkLinkHover) и Шаг 4 (checkReducedMotion): заменить цель на футер-ссылку контакта на /lab: `page.getByRole('link', { name: /github/i })` (футер рендерит контакты из коллекции на каждой странице — 03-04); checkLinkHoverLogic: ok = изменился цвет ссылки (before.color !== after.color) — контракт футер-ссылки ink-muted → ink; требование underline для футер-ссылок убрать (подчёркивание — контракт Link-компонента, не футера)
       - Скролл-проверка (Шаг 1): добавить маршрут первого кейса — прочитать src/content/projects/*.mdx (frontmatter-поле order, минимальный порядковый номер), route = /work/{slug}/; ширина 320 и 1200; при отсутствии записей — предупреждение и пропуск (совместимость с фазой 2)
       - Новый Шаг 5 «копирование email»: на /contact (viewport 1200×800) кликнуть `page.getByRole('button', { name: /Копировать/i })` и проверить, что текст статуса (span с aria-live="polite") изменился с «Копировать» на иное значение («Скопировано» или текст ошибки — в headless clipboard может быть недоступен, важен факт срабатывания state machine)
       - Self-test (строки 205-225): обновить assert checkLinkHoverLogic под новую логику (изменение цвета без underline) и добавить assert маршрута кейса (пустой projects → skip)
  </action>
  <verify>
    <automated>npm run build && node scripts/check-visual.mjs --self-test && node scripts/check-seo.mjs</automated>
    <human-check>Глазом: композиция hero по UI-SPEC Q5; сетка избранных; визуальный проход — финальный гейт 03-06</human-check>
  </verify>
  <acceptance_criteria>
    - index.astro содержит дословно: «Создаю и развиваю сложные сайты, контентные системы и веб-инструменты.», «Product-minded web developer.», «Соединяю Webflow, код, SEO/AEO и возможности ИИ…», «веб-разработчик с продуктовым подходом», «от идеи и структуры до запуска и дальнейшего развития»; Button «Смотреть работы» — последний элемент секции hero
    - index.astro содержит SectionHeading meta="SELECTED WORK" title="Избранные работы" и фильтр featured + sort по order
    - dist/index.html (после build): формула присутствует; порядок элементов hero: имя → формула → CTA
    - check-visual.mjs: hover-цель — getByRole('link', { name: /github/i }) на /lab; checkLinkHoverLogic требует изменение цвета; маршрут кейса в скролл-проверке из src/content/projects; Шаг 5 кликает кнопку копирования на /contact и проверяет смену статуса
    - `node scripts/check-visual.mjs --self-test` — exit 0; `node scripts/check-seo.mjs` — exit 0 (ожидание 6 страниц)
  </acceptance_criteria>
  <done>Формула обещания и «Избранные работы» на /; check-visual адаптирован под новые страницы; сборка и self-test зелёные</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| авторский контент (about/lab/index) → сборка | Тексты черновит агент, утверждает пользователь (D-04); статический SSG |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Spoofing | src/pages/index.astro (hero) | low | mitigate | Формула — дословно REQ-main-promise (D-13), без ярлыков-клише; запрет ярлыков проверяется check-tone по index.html |
| T-03-12 | Tampering | src/pages/lab.astro | low | mitigate | Честный статус без притворства (R6 AC); статусная строка — ассерт check-tone; notes остаётся пустой (D-07) |
| T-03-13 | Information Disclosure | src/pages/about.astro | low | accept | Профиль по PRD, без чувствительных данных; утверждение пользователем (checkpoint Task 2) |
</threat_model>

<verification>
- `npm run build` — exit 0
- `node scripts/check-visual.mjs --self-test` — exit 0
- `node scripts/check-seo.mjs` — exit 0 (6 страниц: 5 + 1 кейс)
- check-tone real-run (денлист + позитивные ассерты) — финальный гейт (03-06): на этой волне формула/меты уже есть, но контактные ассерты требуют 03-04
- Полный `npm run verify` (включая check-visual с браузером) — финальный гейт (03-06)
</verification>

<success_criteria>
- /about: 4 блока (профиль, аудитории, B/I/E, траектория) — утверждены пользователем
- /lab: 2 блока + честный статус — утверждены пользователем
- /: формула обещания дословно + категория + «Избранные работы»; CTA последний
- check-visual: hover/скролл/копирование покрывают новые страницы; self-test зелёный
</success_criteria>

<output>
Create `.planning/phases/03-kontent-i-stranicy/03-05-SUMMARY.md` when done
</output>
