---
phase: 03-kontent-i-stranicy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-tone.mjs
  - scripts/check-tokens.mjs
  - scripts/check-seo.mjs
  - package.json
autonomous: true
requirements:
  - REQ-positioning-category
  - REQ-main-promise
  - REQ-audience
  - REQ-competency-architecture
  - REQ-evidence-cases
  - REQ-tone
  - REQ-growth-trajectory
must_haves:
  truths:
    - "Denylist тона непустой (≥10 записей) и имеет self-test с фикстурой-нарушителем: фикстура FAIL, чистый текст PASS (SPEC edge R9 covered)"
    - "Ярлыки-клише позиционирования проверяются только по index.html — «Product Engineer» легален в траектории /about (RESEARCH Pitfall 7, A5)"
    - "Позитивные ассерты: формула обещания на /, моно-меты 4 блоков About, 2 блоков Lab, 3 каналов Contact (AC R1–R7)"
    - "W1-правило допускает 0 aria-current=\"page\" на глубоких страницах work/*/index.html и требует ровно 1 на верхнеуровневых (Pitfall 1)"
    - "Правило <script>: ровно 1 тег суммарно, только на /contact; 0 на остальных страницах (D-10, Pitfall 2)"
    - "check-seo параметризуется фактическим числом записей projects: ожидаемое число страниц = 5 + N (Open Question 1)"
  artifacts:
    - path: "scripts/check-tone.mjs"
      provides: "Denylist ≥10 + позитивные ассерты + self-test (R9); scope ярлыков — только index.html"
      contains: "DENYLIST"
    - path: "scripts/check-tokens.mjs"
      provides: "Правило 7 (W1: 0 на work/*/index.html), правило 9 (1 script на /contact)"
      contains: "work/*/index.html"
    - path: "scripts/check-seo.mjs"
      provides: "Ожидаемое число страниц/url, вычисляемое из коллекции projects (5 + N)"
      exports: ["computeExpectedPages"]
  key_links:
    - from: "scripts/check-tone.mjs"
      to: "scripts/check-prohibitions.mjs"
      via: "паттерн denylist + self-test с фикстурой-нарушителем (exact/prefix + CLI --self-test)"
      pattern: "--self-test"
    - from: "package.json"
      to: "scripts/check-tone.mjs"
      via: "скрипт check-tone; в verify-цепочку добавляется в 03-06 (когда real-run зелёный)"
      pattern: "check-tone"
prohibitions: []
---

<objective>
Валидационный каркас фазы 3 (Wave 0): новый check-tone.mjs (денлист тона ≥10 + позитивные ассерты + self-test, R9), обновление check-tokens.mjs (правило 7 — W1 на страницах кейсов, правило 9 — первый клиентский JS), параметризация check-seo.mjs под фактическое число страниц.

Purpose: контрактные границы фазы получают проверки ДО написания контента (паттерн проекта: строгие проверки с self-test, фикстура-нарушитель обязана падать). Без этих обновлений финальный verify фазы упадёт: страницы кейсов не дают aria-current (Pitfall 1), копирование email — первый тег script (Pitfall 2), число страниц 5 → 10–11 (Pitfall 4).
Output: scripts/check-tone.mjs (новый), обновлённые check-tokens.mjs и check-seo.mjs, скрипт check-tone в package.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kontent-i-stranicy/03-CONTEXT.md
@.planning/phases/03-kontent-i-stranicy/03-RESEARCH.md
@.planning/phases/03-kontent-i-stranicy/03-PATTERNS.md
@.planning/phases/03-kontent-i-stranicy/03-SPEC.md
@.planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md

# Текущее состояние редактируемых файлов (источники паттернов)
@scripts/check-prohibitions.mjs
@scripts/check-tokens.mjs
@scripts/check-seo.mjs
@package.json
</context>

## Artifacts this phase produces

- `scripts/check-tone.mjs` — НОВЫЙ: денлист ≥10 stem-записей из списков «избегать» REQ-tone + группа ярлыков позиционирования (scope — только index.html, Pitfall 7) + REQUIRED_CONTENT (позитивные ассерты формул/мет/каналов) + self-test с фикстурой-нарушителем
- `scripts/check-tokens.mjs` — правило 7: W1 допускает 0 aria-current на `work/*/index.html`; правило 9: ровно 1 тег script суммарно, только на contact/index.html; новые self-test фикстуры (W1_CASE_GOOD, SCRIPT_CONTACT_GOOD, SCRIPT_WORK_BAD)
- `scripts/check-seo.mjs` — EXPECTED_PAGES/EXPECTED_SITEMAP_URLS вычисляются из числа записей projects (5 + N); self-test с временной коллекцией из 2 фикстур
- `package.json` — скрипт `"check-tone": "node scripts/check-tone.mjs"` (в verify-цепочку добавляется планом 03-06, когда real-run зелёный)

<tasks>

<task type="auto">
  <name>Task 1: Новый check-tone.mjs — денлист тона + позитивные ассерты + self-test</name>
  <files>scripts/check-tone.mjs, package.json</files>
  <read_first>
    scripts/check-prohibitions.mjs — эталон паттерна: денлист (exact/prefix), audit-функция, render с exit-кодом, self-test с известной-хорошей/известной-плохой фикстурой, CLI --self-test (строки 15-147)
    scripts/check-seo.mjs — паттерн walk() по dist (строки 37-51) и readText
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Code Example 4 (каркас check-tone: денлист, REQUIRED_CONTENT, матчинг stem-подстроками), Pitfall 7 (scope ярлыков — только /), Assumptions A4 (денлист-старт, ложные срабатывания — сверка с финальным контентом)
    .planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md — Copywriting Contract (строки «Tone», «Copy button states») — дословные тексты для позитивных ассертов
  </read_first>
  <action>
    Создать scripts/check-tone.mjs по паттерну check-prohibitions.mjs (self-test + exit-коды + CLI):

    1. DENYLIST — массив stem-подстрок русского текста (≥10 записей), стартовый состав из списков «избегать» REQ-tone (хайповый AI-first язык, «революционные решения», лишние англицизмы, образ гения-одиночки): 'революционн', 'прорыв', 'инновационн', 'уникальн', 'невероятн', 'взрывн', 'лучший', 'лучших', 'топовый', 'гений', 'гениальн', 'в одиночку', 'сделал всё сам', 'ai-first', 'ии-революция', 'будущее за', 'game changer', 'game-changer', 'полный спектр', 'безграничн', 'хайп', 'эксперт мирового класса'. Правило: если слово встречается в легальном контексте финального контента — удалить запись (сверка в 03-06 при прогоне по dist), но НЕ опускаться ниже 10 записей. Учитывать морфологию: матчинг по html.toLowerCase().includes(entry) — stem-подстроки.
    2. INDEX_LABELS — отдельная группа (scope — ТОЛЬКО dist/index.html, Pitfall 7/A5): 'product engineer', 'ai product developer', 'aeo/geo specialist', 'web systems specialist', 'ai-native developer'. По остальным страницам группа не проверяется (на /about легальна траектория с ролью Product Engineer — REQ-growth-trajectory). 'Product-minded web developer' — разрешённая категория (D-13), в денлист НЕ входит.
    3. REQUIRED_CONTENT — позитивные ассерты (все проверяются по dist; страница — относительный путь от dist/):
       - index.html содержит 'Создаю и развиваю сложные сайты, контентные системы и веб-инструменты' (формула, дословно D-13/REQ-main-promise)
       - index.html содержит 'веб-разработчик с продуктовым подходом' (категория)
       - index.html содержит 'от идеи и структуры до запуска и дальнейшего развития' (подпись D-13)
       - about/index.html содержит 'PROFILE', 'AUDIENCES', 'CAPABILITIES', 'TRAJECTORY' (меты 4 блоков, R5 — структурный ассерт)
       - lab/index.html содержит 'DIRECTIONS', 'PROCESS' (меты 2 блоков, R6) и 'Эксперименты в работе' (честный статус, R6 AC)
       - contact/index.html содержит 'mailto:' (канал email, R7) и НЕ содержит 'LinkedIn' (R7: нет LinkedIn)
       - в dist/work/*/index.html отсутствует маркер демонстрационного блока 'SYSTEM DEMO' и фикстурные названия карточек (R2 AC) — файлы work-кейсов обходятся walk() по dist/work
    4. Real-run: обход dist/ (walk-паттерн из check-seo), денлист по всем .html, ярлыки только по index.html, позитивные ассерты; dist отсутствует → понятное сообщение с exit 1. Self-test (паттерн check-prohibitions): известная-плохая фикстура с хайповым текстом (например, «революционные решения» и «я всё сделал в одиночку») → FAIL; чистый текст → PASS; фикстура-нарушитель ярлыков на index.html → FAIL, тот же текст на about-фикстуре → PASS (scope); позитивные ассерты на фикстурах-страницах с формулой/метами → PASS.
    5. package.json: добавить скрипт `"check-tone": "node scripts/check-tone.mjs"` (по образцу соседних, строки 12-17). В verify-цепочку (строка 18) check-tone НЕ добавлять — это делает план 03-06, когда real-run зелёный.
    Комментарии — на русском, стиль check-prohibitions.mjs.
  </action>
  <verify>
    <automated>node scripts/check-tone.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-tone.mjs содержит массив DENYLIST длиной ≥10
    - scripts/check-tone.mjs содержит группу INDEX_LABELS с проверкой scope по index.html и функцией, исключающей прочие страницы
    - scripts/check-tone.mjs содержит REQUIRED_CONTENT со всеми ассертами из пункта 3 action (формула, категория, подпись, 4 меты About, 2 меты Lab, честный статус, mailto, отсутствие LinkedIn, отсутствие демонстрационного маркера в dist/work)
    - `node scripts/check-tone.mjs --self-test` — exit 0 (нарушитель падает, чистый текст проходит, scope-фикстуры классифицируются верно)
    - package.json содержит `"check-tone": "node scripts/check-tone.mjs"`; verify-цепочка (строка 18) НЕ изменена в этом плане
  </acceptance_criteria>
  <done>check-tone.mjs создан, self-test зелёный, скрипт зарегистрирован в package.json</done>
</task>

<task type="auto">
  <name>Task 2: check-tokens.mjs — правило 7 (W1 на кейсах) и правило 9 (1 script на /contact)</name>
  <files>scripts/check-tokens.mjs</files>
  <read_first>
    scripts/check-tokens.mjs — правило 7 (строки 281-302: W1-ассерт по dist), правило 9 (строки 283-285: тег script), self-test фикстуры W1_INDEX_GOOD/W1_WORK_GOOD/W1_INDEX_BAD/W1_WORK_BAD_HREF/SCRIPT_INDEX_BAD (строки 408-428) и runSelfTest-блоки (e) bad-dist / (g) bad-script (строки 515-548)
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Pitfall 1 (W1 на глубоких страницах), Pitfall 2 (script-правило), Pitfall 3 (правило 10 без изменений)
  </read_first>
  <action>
    В scripts/check-tokens.mjs изменить два правила в блоке аудита dist (строки 281-303):

    1. Правило 9 (тег script, строки 283-285): вместо «любой <script> → нарушение» — подсчёт по страницам:
       - суммарное число тегов <script> по всему dist должно быть ровно 1;
       - единственный скрипт обязан находиться в contact/index.html (контракт D-10: копирование email — первый клиентский JS);
       - на любой другой странице 0 тегов.
       Нарушения: `тег <script> в собранной странице {r} (клиентский JS разрешён только на /contact, D-10)` при script на не-contact странице; «всего тегов <script>: N, ожидалось 1» при сумме ≠ 1.
    2. Правило 7 (W1, строки 287-300): глубокая страница — относительный путь от dist/ вида `work/{slug}/index.html` (regex-проверка: `^work\/[^/]+\/index\.html$`): допускается ровно 0 aria-current="page" (Nav не совпадает ни с одним маршрутом кейса, Pitfall 1; Work НЕ помечать активным). Верхнеуровневые страницы — по-прежнему ровно 1 с верным href. Текст нарушения W1 для глубоких: `W1: {r}: страница кейса не может иметь aria-current="page" (найдено N, R2)`.
    3. Self-test: добавить фикстуры:
       - W1_CASE_GOOD — html глубокой страницы без aria-current (например, `<nav><a href="/work">Работы</a></nav>`) → PASS (0 нарушений);
       - W1_CASE_BAD — глубокая страница С aria-current → FAIL;
       - SCRIPT_CONTACT_GOOD — contact/index.html с одним `<script>` → PASS; work/index.html со `<script>` → FAIL (SCRIPT_WORK_BAD);
       - SCRIPT_TOTAL_BAD — два скрипта (contact + index) → FAIL по сумме.
       Обновить блок (e) bad-dist и (g) bad-script в runSelfTest (строки 515-548) под новые правила; существующие фикстуры верхнеуровневых страниц (W1_INDEX_GOOD и др.) не меняются по смыслу. Правило 10 (голый {expr} в кавычках) — БЕЗ изменений (ловит `data-copy="{email}"` автоматически, Pitfall 3).
  </action>
  <verify>
    <automated>node scripts/check-tokens.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - В коде правила 9: подсчёт скриптов с проверкой «сумма = 1 и только на contact/index.html»
    - В коде правила 7: ветка для глубоких страниц (regex по пути dist) с требованием 0 aria-current
    - Self-test содержит фикстуры W1_CASE_GOOD/W1_CASE_BAD/SCRIPT_CONTACT_GOOD/SCRIPT_WORK_BAD и проверки: глубокая без aria-current → чисто; глубокая с aria-current → нарушение; скрипт на contact → чисто; скрипт на work → нарушение
    - `node scripts/check-tokens.mjs --self-test` — exit 0
  </acceptance_criteria>
  <done>Правила 7 и 9 обновлены под страницы кейсов и первый клиентский JS; self-test зелёный</done>
</task>

<task type="auto">
  <name>Task 3: check-seo.mjs — параметризация числа страниц по коллекции projects</name>
  <files>scripts/check-seo.mjs</files>
  <read_first>
    scripts/check-seo.mjs — константы EXPECTED_PAGES/EXPECTED_SITEMAP_URLS (строки 27-28), auditPages (95-134), auditSitemap (144-168), render (199-207), self-test goodPages/GOOD_SITEMAP_CHILD (строки 231-304)
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Pitfall 4 (self-test разъедется с константами), Open Question 1 (параметризация фактом: 5 страниц + число кейсов)
  </read_first>
  <action>
    В scripts/check-seo.mjs заменить жёсткие константы на вычисляемые:

    1. Убрать `const EXPECTED_PAGES = 5; const EXPECTED_SITEMAP_URLS = 5;` (строки 27-28). Добавить функцию `computeExpectedPages(rootDir)`:
       - считает .md/.mdx файлы (без dot-файлов и .gitkeep) в `${rootDir}/src/content/projects` (walk-паттерн файла, строки 37-51);
       - возвращает `5 + N` (5 фиксированных страниц + N кейсов) — Open Question 1, факт из коллекции;
       - N вне диапазона 5–6: функция возвращает фактическое значение (граница «5–6 записей» — контракт check-collections, 03-01 Task 2; check-seo не дублирует её, но честно ожидает фактическое число страниц).
       Модульный уровень: `const EXPECTED_PAGES = computeExpectedPages(ROOT); const EXPECTED_SITEMAP_URLS = EXPECTED_PAGES;`
    2. auditPages(pages, expected) и auditSitemap(indexXml, readChild, expected) — expected передавать параметром (сейчас читают модульные константы); в auditDist передавать EXPECTED_PAGES/EXPECTED_SITEMAP_URLS.
    3. render (строки 199-207): OK-строка и FAIL-тексты — с фактическими числами (`${EXPECTED_PAGES} уникальных пар`, `${EXPECTED_SITEMAP_URLS} url`), а не «5».
    4. Self-test (runSelfTest, строки 244-304): во временной директории создать `src/content/projects/zz-case-a.mdx` и `zz-case-b.mdx` (пустые тела, только frontmatter-заглушки — сам self-test не собирает проект, важен только подсчёт файлов) → `computeExpectedPages(tmp) === 7`. goodPages дополнить 2 страницами кейсов (уникальные пары, path `/work/zz-case-a/`, `/work/zz-case-b/`) → 7 страниц; GOOD_SITEMAP_CHILD дополнить 2 url кейсов → 7 url. Все assert-ожидания «5» заменить на «7» (или на параметризованное значение). Проверка «4 url → fail» остаётся (заменой `<url>` на пустоту относительно 7).
    НЕ менять: декодирование сущностей, extractMeta, REQUIRED_OG_TAGS, логику дублей пар.
  </action>
  <verify>
    <automated>node scripts/check-seo.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - В check-seo.mjs нет литералов `EXPECTED_PAGES = 5` / `EXPECTED_SITEMAP_URLS = 5`; есть computeExpectedPages(rootDir) с подсчётом .md/.mdx в src/content/projects и формулой 5 + N
    - auditPages/auditSitemap принимают expected параметром
    - `node scripts/check-seo.mjs --self-test` — exit 0 (self-test с 2 фикстурными кейсами: ожидание 7 страниц/url)
    - `node scripts/check-seo.mjs` (real-run по текущему dist: 0 кейсов → ожидание 5) — exit 0
  </acceptance_criteria>
  <done>check-seo параметризован числом записей projects; self-test и real-run зелёные</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist-вывод → check-скрипты | Собранный HTML анализируется проверками (не runtime-граница) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | scripts/check-tone.mjs | low | mitigate | Денлист — статические stem-подстроки без regex-инъекций (входы — константы файла, не пользовательский ввод); self-test с фикстурой-нарушителем |
| T-03-04 | Information Disclosure | scripts/check-seo.mjs | low | accept | Проверка SEO-пар и sitemap по публичному dist — нет чувствительных данных; кейсы проходят K1–K4-утверждение (03-03/03-06) |
</threat_model>

<verification>
- `node scripts/check-tone.mjs --self-test` — exit 0
- `node scripts/check-tokens.mjs --self-test` — exit 0
- `node scripts/check-seo.mjs --self-test` — exit 0
- `node scripts/check-seo.mjs` — exit 0 (real-run по текущему dist — 5 страниц)
- Реальные прогоны check-tone (денлист + ассерты) и правила 7/9 check-tokens по dist станут зелёными по мере наполнения контента (03-03…03-06); полный `npm run verify` — финальный гейт фазы
</verification>

<success_criteria>
- check-tone.mjs создан: денлист ≥10 + ярлыки со scope по / + позитивные ассерты + self-test
- check-tokens.mjs: правило 7 допускает 0 aria-current на кейсах, правило 9 требует ровно 1 script на /contact
- check-seo.mjs вычисляет ожидаемое число страниц из коллекции (5 + N)
- Все три self-test зелёные; package.json получил скрипт check-tone
</success_criteria>

<output>
Create `.planning/phases/03-kontent-i-stranicy/03-02-SUMMARY.md` when done
</output>
