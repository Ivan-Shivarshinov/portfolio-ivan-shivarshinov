---
phase: 03-kontent-i-stranicy
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/data/contacts.json
  - src/pages/contact.astro
  - src/components/Footer.astro
autonomous: false
requirements:
  - REQ-tone
must_haves:
  truths:
    - "На /contact ровно 3 канала (email, Telegram, GitHub), без формы и без LinkedIn (SPEC edge R7 covered)"
    - "mailto-ссылка корректна и работает без JS; копирование email в буфер работает с fallback на execCommand и состояниями idle/copied/error (D-10, UI-SPEC error covered)"
    - "Ссылки на /contact и в футере идентичны — единый источник src/data/contacts.json (D-11, SPEC edge R7 adjacency covered)"
    - "В футере ≥1 ссылка контакта (AC R7); на 320px футер и строки каналов не дают горизонтального скролла (UI-SPEC overflow covered)"
    - "Ровно 1 тег script в dist, только на /contact (D-10, check-tokens правило 9)"
  artifacts:
    - path: "src/data/contacts.json"
      provides: "Единый источник каналов: {id, label, value, href} × 3 (email/telegram/github), значения от пользователя (D-12)"
      contains: '"id": "email"'
    - path: "src/pages/contact.astro"
      provides: "3 канала + кнопка копирования email + inline-скрипт (navigator.clipboard → execCommand fallback)"
      contains: "data-copy"
    - path: "src/components/Footer.astro"
      provides: "Ссылки контактов из коллекции contacts (D-11), hit-area ≥44px"
      contains: "getCollection('contacts')"
  key_links:
    - from: "src/pages/contact.astro"
      to: "src/data/contacts.json"
      via: "getCollection('contacts') → map каналов; email.value в data-copy={email} (без кавычек — правило 10)"
      pattern: "getCollection('contacts')"
    - from: "src/components/Footer.astro"
      to: "src/data/contacts.json"
      via: "та же коллекция — AC R7 «ссылки идентичны» обеспечивается источником, не проверкой"
      pattern: "getCollection('contacts')"
prohibitions: []
---

<objective>
Контактный слой фазы: единый источник каналов `src/data/contacts.json` (D-11), страница /contact с 3 каналами и копированием email (D-10 — первый клиентский JS сайта, без фреймворков), футер со ссылками из того же источника (R7).

Purpose: AC R7 «ссылки на /contact и в футере идентичны» обеспечивается источником, а не проверкой; копирование email — прогрессивное улучшение поверх mailto (бюджет клиентского JS: один скрипт, ноль фреймворков, ADR).
Output: contacts.json (реальные значения от пользователя — D-12), контактная страница, футер со ссылками.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kontent-i-stranicy/03-CONTEXT.md
@.planning/phases/03-kontent-i-stranicy/03-RESEARCH.md
@.planning/phases/03-kontent-i-stranicy/03-PATTERNS.md
@.planning/phases/03-kontent-i-stranicy/03-SPEC.md
@.planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md

# Текущее состояние редактируемых файлов
@src/pages/contact.astro
@src/components/Footer.astro
@src/components/IconMail.astro
@src/components/IconTelegram.astro
@src/components/IconGithub.astro
@src/components/IconCopy.astro
@src/components/IconCheck.astro
</context>

## Artifacts this phase produces

- `src/data/contacts.json` — массив 3 каналов {id, label, value, href}: email (mailto:), telegram (https://t.me/), github (https://github.com/) — значения от пользователя (D-12), уникальные id (strict-json-loader)
- `src/pages/contact.astro` — 3 строки каналов (иконка + моно-лейбл + value-ссылка), кнопка копирования email (Button-контракт secondary, density lg, IconCopy/IconCheck, состояния «Копировать»/«Скопировано»/«Не удалось скопировать» + «выделите адрес вручную»), один inline-скрипт (navigator.clipboard → execCommand fallback, aria-live="polite")
- `src/components/Footer.astro` — строка контактов из коллекции: EMAIL · TELEGRAM · GITHUB (моно Caption 12px, ink-muted → hover ink, hit-area ≥44px), 2 строки на <768px
- Решение: IconLinkedin остаётся в компонентах как задел (не используется — LinkedIn вне контактов, R7; удаление — churn без выгоды)

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Значения контактов от пользователя (D-12)</name>
  <what-built>Запрос фактических значений контактов (D-12) и формулировок страницы /contact</what-built>
  <how-to-verify>
    Сообщите агентe в чате: 1) email-адрес; 2) ник Telegram (например, @ivan_shivarshinov); 3) URL GitHub (например, https://github.com/Ivan-Shivarshinov). При желании — правки формулировок страницы: заголовок секции, подпись, тексты состояний кнопки копирования (по умолчанию — по UI-SPEC Copywriting Contract: «Копировать»/«Скопировано»/«Не удалось скопировать» + «выделите адрес вручную»).
  </how-to-verify>
  <resume-signal>Значения: email …, telegram …, github … — или правки формулировок</resume-signal>
</task>

<task type="auto">
  <name>Task 2: contacts.json, /contact с копированием email, футер со ссылками</name>
  <files>src/data/contacts.json, src/pages/contact.astro, src/components/Footer.astro</files>
  <read_first>
    src/data/services.json — формат JSON-массива с уникальными id (аналог contacts.json, strict-json-loader)
    src/content.config.ts — коллекция contacts (03-01): схема {id, label, value, href}
    src/pages/contact.astro — текущий empty-state (строки 5-17): BaseLayout, pageLabel="05 / CONTACT"
    src/components/Footer.astro — текущий футер (строки 1-17): sectionLabel-проп
    src/components/Nav.astro — паттерн массив → map ссылок (строки 11-17, 24-36)
    src/components/Link.astro — контракт внешних ссылок: caller передаёт target="_blank" rel="noopener"
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Pattern 3 (контакты через strict-json-loader, строки 285-296), Pattern 4 (копирование email, строки 298-339), Pitfall 3 (data-copy={email} БЕЗ кавычек — правило 10), Pitfall 11 (плейсхолдеры не утекают: файл создаётся с реальными значениями)
    .planning/phases/03-kontent-i-stranicy/03-PATTERNS.md — разделы «src/pages/contact.astro», «src/data/contacts.json», «src/components/Footer.astro»
    .planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md — Contact channels, Footer (Q3), Copy button states (Q2)
  </read_first>
  <action>
    Значения каналов — из решения Task 1 (реальные, без плейсхолдеров — Pitfall 11: файл создаётся в задаче, где значения уже сообщены).

    1. src/data/contacts.json — массив из 3 объектов, порядок Email → Telegram → GitHub (решение плана, Claude's Discretion):
       - { "id": "email", "label": "Email", "value": "<email от пользователя>", "href": "mailto:<email>" }
       - { "id": "telegram", "label": "Telegram", "value": "@<ник>", "href": "https://t.me/<ник>" }
       - { "id": "github", "label": "GitHub", "value": "<ник>", "href": "https://github.com/<ник>" }
       Формат — как services.json (массив, 2 пробела, завершающий перенос строки); id уникальны (strict-json-loader: дубль падает сборку).
    2. src/pages/contact.astro — заменить empty-state на 3 канала из коллекции:
       - frontmatter: `const contacts = await getCollection('contacts');` + `const email = contacts.find((c) => c.id === 'email');`
       - BaseLayout: title="Контакты — Иван Шиваршинов", description — реальная формулировка (по тону REQ-tone, без хайпа), pageLabel="05 / CONTACT"
       - SectionHeading (meta="CONTACT", title="Контакты") + список строк каналов: иконка 24px (IconMail/IconTelegram/IconGithub) + моно-лейбл (--font-mono, --text-label, --color-ink-muted) + value как Link (href из коллекции; внешние — target="_blank" rel="noopener", паттерн Link.astro); строки — колонка во всю ширину контейнера, перенос естественный, без nowrap (overflow covered, UI-SPEC)
       - email-строка дополнительно: кнопка `<button type="button" class="copy-btn" data-copy={email.value} aria-describedby="copy-status">` — data-copy БЕЗ кавычек (шаблон: data-copy={email.value}; `data-copy="{email}"` рендерится буквально — Pitfall 3, правило 10). Внутри: `<span id="copy-status" class="copy-status" aria-live="polite">Копировать</span>` + IconCopy. Стили кнопки — по контракту Button secondary density lg (border 1px var(--color-line), hover border-ink + color-accent, padding var(--space-md) var(--space-lg), моно --text-label, transition var(--motion-fast) var(--ease-standard); 2px accent ring на :focus-visible — из UI-SPEC). Кнопка — native button (не Button.astro — тот рендерит <a>)
       - inline-скрипт по RESEARCH Pattern 4 (строки 308-338): navigator.clipboard.writeText в isSecureContext → fallback execCommand('copy') через временный textarea (position: fixed, opacity 0, readOnly); состояния: idle «Копировать» → ok «Скопировано» + IconCheck, авто-сброс ≈2 с; ошибка «Не удалось скопировать» + подсказка «выделите адрес вручную»; статус — через span с aria-live="polite". mailto-ссылка остаётся рабочей без JS (прогрессивное улучшение)
    3. src/components/Footer.astro — добавить контакты из коллекции:
       - frontmatter: `const contacts = await getCollection('contacts');`
       - под текущей строкой (или вторая строка, макет по UI-SPEC Q3): ссылки контактов — text = label в верхнем регистре (CSS text-transform: uppercase), моно Caption 12px (--text-caption, --font-mono), цвет --color-ink-muted → hover --color-ink, transition var(--motion-fast) var(--ease-standard); hit-area ≥44px: padding-block var(--space-md) на <768px (паттерн Nav, 02-UI-SPEC); внешние — target="_blank" rel="noopener"
       - ≥768px: одна строка — слева «© 2026 · Иван Шиваршинов», справа ссылки + sectionLabel; <768px: две строки (контакты переносятся без горизонтального скролла на 320px — overflow covered)
       - НЕ менять: проп sectionLabel, контракт метки футера
  </action>
  <verify>
    <automated>npm run build && node scripts/check-tokens.mjs</automated>
    <human-check>Визуальная сверка /contact и футера по 03-UI-SPEC (3 канала, состояния кнопки копирования) — на финальном визуальном проходе (03-06)</human-check>
  </verify>
  <acceptance_criteria>
    - src/data/contacts.json — массив из ровно 3 записей с уникальными id email/telegram/github и полями label/value/href; href email начинается с mailto:, telegram — https://t.me/, github — https://github.com/; нет example-плейсхолдеров
    - contact.astro: getCollection('contacts'); ровно 3 строки каналов; кнопка `<button type="button" class="copy-btn" data-copy={email.value}` (без кавычек вокруг выражения); span#copy-status с aria-live="polite"; mailto-ссылка в разметке всегда
    - В contact.astro нет упоминаний LinkedIn и нет формы (<form)
    - Footer.astro: getCollection('contacts'); ссылки контактов в разметке; контракт hover ink-muted → ink
    - `npm run build` — exit 0; `node scripts/check-tokens.mjs` — exit 0 (ровно 1 script, только на contact/index.html — D-10)
    - grep dist/contact/index.html: содержит mailto: и data-copy=; не содержит LinkedIn
  </acceptance_criteria>
  <done>Три канала на /contact и в футере из одного источника; копирование email работает (или честный fallback); сборка и правило 9 зелёные</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| контактные ссылки → внешние ресурсы | Клик уводит на внешние домены (t.me, github.com) |
| clipboard-код → браузер | Единственный клиентский JS (без ввода пользователя и сети) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | src/pages/contact.astro (clipboard) | low | mitigate | Скрипт не принимает пользовательский ввод и не ходит в сеть; данные — публичный email из contacts.json; fallback/ошибка с подсказкой |
| T-03-09 | Information Disclosure | src/data/contacts.json | low | accept | Публичные каналы по выбору пользователя (D-12); значения утверждаются до коммита (checkpoint Task 1, Pitfall 11) |
| T-03-10 | Spoofing | внешние ссылки (TG/GitHub) | low | mitigate | target="_blank" rel="noopener" на внешних ссылках (паттерн Link.astro); значения утверждены пользователем |
</threat_model>

<verification>
- `npm run build` — exit 0
- `node scripts/check-tokens.mjs` — exit 0 (правило 9: 1 script на /contact; правило 7: W1 верхнеуровневые)
- grep-проверки по dist/contact/index.html: mailto: присутствует, data-copy= присутствует, LinkedIn отсутствует
- check-tone real-run целиком — финальный гейт (03-06)
</verification>

<success_criteria>
- /contact: 3 канала из contacts.json, копирование email с состояниями, без формы и LinkedIn
- Футер: те же контакты из того же источника (R7), hit-area ≥44px, без скролла на 320px
- Сборка зелёная; правило 9 check-tokens зелёное
</success_criteria>

<output>
Create `.planning/phases/03-kontent-i-stranicy/03-04-SUMMARY.md` when done
</output>
