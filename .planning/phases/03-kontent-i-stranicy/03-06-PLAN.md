---
phase: 03-kontent-i-stranicy
plan: 06
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - src/content/projects/{slug2..slugN}.mdx
  - src/assets/projects/{slug2..slugN}/
  - package.json
  - .planning/phases/03-kontent-i-stranicy/03-VERIFICATION.md
autonomous: false
requirements:
  - REQ-evidence-cases
  - REQ-tone
must_haves:
  truths:
    - "Коллекция projects содержит от 5 до 6 реальных записей; проверка падает при 4 и при 7 (SPEC edge R2 covered)"
    - "Каждый кейс отвечает на 4 вопроса (проблема → ответственность → решение → результат) с разделением личного вклада, команды и эффекта (REQ-evidence-cases, SPEC edge R3 covered)"
    - "Для каждой записи сгенерирована страница work/{slug}/index.html с 4 разделами; все маршруты кейсов в sitemap; check-seo зелёный (SPEC edge R3 covered)"
    - "featured-записей ровно 2–3 (D-09); тема каждого кейса из 4 приглушённых цветов, терракота кейсам не назначается (D-06)"
    - "У каждого кейса ≥1 реальный скриншот в репозитории (файл существует — build-level гарантия cover: image()); дублей файлов между кейсами нет (SPEC edge R8 covered)"
    - "Полный гейт зелёный: lint → check (tokens/collections/contrast/seo/tone) → build; CI зелёный (AC R10)"
    - "К1–K4 подтверждены при утверждении каждого кейса; ручная проверка тона по спискам «использовать/избегать» зафиксирована в 03-VERIFICATION.md (AC R9, SPEC Prohibitions)"
  artifacts:
    - path: "src/content/projects/"
      provides: "5–6 MDX-записей кейсов (после 03-03: 5–6 суммарно)"
      contains: "## Результат"
    - path: "src/assets/projects/"
      provides: "Папки скриншотов по слагам кейсов (D-02)"
      contains: "cover.png"
    - path: "package.json"
      provides: "verify-цепочка с check-tone (финальный состав)"
      contains: "check-tone"
    - path: ".planning/phases/03-kontent-i-stranicy/03-VERIFICATION.md"
      provides: "Журнал утверждения кейсов: K1–K4 по каждому, ручной проход тона"
      contains: "K1"
  key_links:
    - from: "src/content/projects/*.mdx"
      to: "src/assets/projects/{slug}/cover.png"
      via: "frontmatter cover: image() — файл обязан существовать (R8 build-level гарантия, Pitfall 6)"
      pattern: "cover:"
    - from: "scripts/check-collections.mjs"
      to: "src/content/projects/*.mdx"
      via: "аудит границ: 5–6 записей, featured 2–3, theme без terracotta, 4 h2, дублей cover нет (03-01)"
      pattern: "auditCaseBoundaries"
prohibitions:
  - statement: "Кейсы MUST NOT содержать вымышленных проектов и не подтверждённых результатов"
    status: resolved
    verification: judgment — подтверждается пользователем при утверждении каждого кейса (checkpoint Task 2)
  - statement: "Кейсы MUST NOT раскрывать конфиденциальные данные клиентов; при NDA — анонимизация обязательна (тип клиента вместо имени)"
    status: resolved
    verification: judgment — именование каждого кейса утверждает пользователь (D-03)
  - statement: "Кейсы MUST NOT приписывать себе вклад команды — личное/командное/эффект разделены честно"
    status: resolved
    verification: judgment — утверждение контента каждого кейса (checkpoint Task 2)
  - statement: "Заглушки/стоковые изображения MUST NOT выдаваться за реальные интерфейсы кейсов"
    status: resolved
    verification: judgment — скриншоты предоставляет пользователь (D-02, checkpoint Task 2); K4 при утверждении
---

<objective>
Оставшиеся кейсы (2–6 по утверждённому в 03-03 составу): черновики, утверждение пользователем (K1–K4), скриншоты — и финальный гейт фазы: verify-цепочка с check-tone, полный прогон (build → check-seo → check-tokens → check-contrast → check-collections → check-prohibitions → check-tone → check-visual), журнал утверждения 03-VERIFICATION.md (K1–K4 по каждому кейсу, ручной проход тона по спискам REQ-tone).

Purpose: довести коллекцию до контрактной границы 5–6 записей (R2), на которой сходятся все проверки фазы — число страниц (check-seo), границы коллекций (check-collections), тон (check-tone), W1/скрипт-контракт (check-tokens). Фаза запечатывается зелёным полным гейтом.
Output: полная коллекция кейсов со скриншотами, финальный verify-состав package.json, 03-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kontent-i-stranicy/03-CONTEXT.md
@.planning/phases/03-kontent-i-stranicy/03-RESEARCH.md
@.planning/phases/03-kontent-i-stranicy/03-PATTERNS.md
@.planning/phases/03-kontent-i-stranicy/03-SPEC.md
@.planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md

# Текущее состояние
@src/content/projects/
@package.json
@docs/positioning_portfolio_ivan_shivarshinov.md
</context>

## Artifacts this phase produces

- `src/content/projects/{slug2..slugN}.mdx` — оставшиеся записи кейсов (суммарно 5–6 с учётом кейса 03-03): frontmatter по схеме (theme/featured/cover/coverAlt), тело с 4 h2, ~400–700 слов, «Моя зона.»/«Команда.»/«Эффект.»
- `src/assets/projects/{slug2..slugN}/cover.png` — папки и скриншоты пользователя (D-02)
- `package.json` — verify-цепочка: добавлен `npm run check-tone`
- `.planning/phases/03-kontent-i-stranicy/03-VERIFICATION.md` — журнал фазы: утверждение кейсов с отметками K1–K4, ручной проход тона по спискам REQ-tone, итог полного гейта

<tasks>

<task type="auto">
  <name>Task 1: Черновики кейсов 2–6 — записи и папки активов</name>
  <files>src/content/projects/{slug2..slugN}.mdx, src/assets/projects/{slug2..slugN}/.gitkeep</files>
  <read_first>
    src/content/projects/{slug1}.mdx — эталонная запись первого кейса (03-03): формат frontmatter и тела, стиль
    src/content.config.ts — схема projects (03-01)
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Code Example 1 (шаблон записи), Code Example 2 (структурная проверка 4 секций), Pitfall 10 (сортировка/порядок order), Pitfall 12 (рекомендации по размерам скриншотов)
    .planning/phases/03-kontent-i-stranicy/03-PATTERNS.md — раздел «src/content/projects/{slug}.mdx»
    Решение Task 1 плана 03-03 — состав, темы, именование, featured, слаги (в 03-VERIFICATION.md или SUMMARY 03-03)
  </read_first>
  <action>
    По утверждённому в 03-03 составу написать оставшиеся кейсы (суммарно 5–6 записей в коллекции, включая кейс 03-03). Каждая запись — по шаблону RESEARCH Code Example 1 и образцу первого кейса:

    - src/content/projects/{slug}.mdx: frontmatter (кавычки одинарные) — slug (из решения, /^[a-z0-9-]+$/), title, summary (1–2 предложения), role, stack (3–6), year, status: 'active', client-type (реальное имя или тип при NDA — решение 03-03, K2), order (2..6, уникальные), theme (из решения — пул clay/olive/slate/plum, БЕЗ terracotta, D-06), featured (true ровно у 2–3 записей суммарно — D-09), cover: '../../assets/projects/{slug}/cover.png', coverAlt (1 предложение)
    - тело — ровно 4 h2, каждый ровно один раз: «Проблема», «Ответственность», «Решение», «Результат»; в «Ответственности» — strong-лейблы «Моя зона.», «Команда.», «Эффект.»; 400–700 слов; факты из интервью 03-03 (K1); без хайпа и ярлыков (REQ-tone); «До: … После: …»-структура в «Результате» где уместно
    - src/assets/projects/{slug}/.gitkeep — папка под скриншот (D-02)
    Все тексты — черновики по D-04: показать в чате (текст + путь каждого файла) перед коммитом. В чате же напомнить: скриншоты кладёт пользователь файлами (PNG/JPG 1280–1920px, ~1–2 МБ, реальные интерфейсы — K4, Pitfall 12), дубли файлов между кейсами запрещены (R8).
  </action>
  <verify>
    <automated>node scripts/check-collections.mjs --self-test && npm run build</automated>
    <human-check>Build НЕ упадёт только после Task 2 (cover: image() требует файлов скриншотов, Pitfall 6)</human-check>
  </verify>
  <acceptance_criteria>
    - Суммарно в src/content/projects: от 5 до 6 .mdx файлов (с кейсом 03-03)
    - У каждой записи: order уникальный, theme из 4 цветов (не terracotta), featured true ровно у 2–3 записей, cover-путь указывает на ../../assets/projects/{slug}/cover.png, coverAlt непустой
    - Тело каждой записи: 4 h2 ровно по одному разу; «Ответственность» содержит «Моя зона.», «Команда.», «Эффект.»
    - Для каждой записи создана папка src/assets/projects/{slug}/ (.gitkeep)
    - `node scripts/check-collections.mjs --self-test` — exit 0
  </acceptance_criteria>
  <done>Черновики всех оставшихся кейсов написаны и показаны в чате; коммит — после Task 2</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Утверждение кейсов 2–6 + скриншоты (K1–K4)</name>
  <what-built>Черновики кейсов 2–6 (тексты в чате, файлы в src/content/projects/), папки под скриншоты src/assets/projects/{slug}/</what-built>
  <how-to-verify>
    1. Прочитайте каждый черновик в чате и подтвердите K1–K4: K1 — факты реальные (не вымышленные, без не подтверждённых результатов); K2 — именование клиента корректно (реальное или NDA-анонимизация); K3 — личный вклад / команда / эффект разделены честно; K4 — скриншоты будут реальными интерфейсами. Внесите правки формулировок и/или тем (D-07).
    2. Положите скриншоты файлами: src/assets/projects/{slug}/cover.png для каждого кейса (PNG/JPG, реальный интерфейс; 1280–1920px по ширине, ~1–2 МБ; без личных данных клиентов; один файл не используется в двух кейсах).
    3. После ответа агент: применит правки, соберёт сайт, прогонит полный гейт и зафиксирует K1–K4 в 03-VERIFICATION.md, затем закоммитит.
  </how-to-verify>
  <resume-signal>Кейсы 2–6 утверждены (K1–K4) + скриншоты положены — или правки по списку</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Финальный гейт — verify-цепочка, полный прогон, журнал фазы</name>
  <files>package.json, .planning/phases/03-kontent-i-stranicy/03-VERIFICATION.md</files>
  <read_first>
    package.json — verify-скрипт (строка 18) и блок скриптов (строки 12-19)
    src/content/projects/ — все записи (после правок Task 2)
    .planning/phases/02-vizualnaya-sistema/02-VERIFICATION.md — образец журнала фазы (формат: решения, K-отметки, итог гейта)
    .planning/phases/03-kontent-i-stranicy/03-CONTEXT.md — D-04 (K1–K4 при утверждении), Claude's Discretion (ручной проход тона)
    .planning/phases/03-kontent-i-stranicy/03-SPEC.md — AC R9 (ручная проверка тона), AC R10 (полный гейт)
  </read_first>
  <action>
    1. package.json: добавить check-tone в verify-цепочку (строка 18) — после check-prohibitions: `... && node scripts/check-prohibitions.mjs && npm run check-tone && npm run check-visual`. Скрипт check-tone уже зарегистрирован (03-02).
    2. Полный гейт (все команды — exit 0): `npm run lint` → `npm run verify` (build → check-seo → check-tokens → check-contrast → check-collections → check-prohibitions → check-tone → check-visual). check-tone real-run теперь зелёный: денлист по dist без ложных срабатываний (при ложных — скорректировать денлист, НЕ ниже 10 записей, и перепрогнать), позитивные ассерты (формула/меты/каналы/честный статус) на месте. check-collections: аудит границ зелёный (5–6 записей, featured 2–3, theme без terracotta, cover-файлы существуют, 4 h2, дублей скриншотов нет, notes 0). check-seo: ожидаемое число страниц = 5 + фактическое число записей (10 или 11) — все маршруты кейсов в sitemap, пары уникальны. check-tokens: W1 (кейсы — 0 aria-current), ровно 1 script на /contact.
    3. Ручной проход тона (AC R9, Claude's Discretion): пройти все страницы по спискам «использовать/избегать» REQ-tone (docs/positioning_portfolio_ivan_shivarshinov.md) — зафиксировать в 03-VERIFICATION.md (что проверено, что найдено/исправлено).
    4. .planning/phases/03-kontent-i-stranicy/03-VERIFICATION.md — журнал фазы по образцу 02-VERIFICATION.md: состав кейсов (решение 03-03), по каждому кейсу — утверждение с отметкой K1–K4, тема, именование; решения планов (темы, порядок каналов, IconLinkedin-задел, денлист); итог полного гейта (команды, результаты); ручной проход тона; ссылки на дефекты/фиксы, если были.
    5. Если после правок Task 2 build упал (например, cover-файл не найден — Pitfall 6, или zod-ошибка) — исправить (проверить пути скриншотов, поля frontmatter) и перепрогнать гейт до зелёного.
  </action>
  <verify>
    <automated>npm run lint && npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - package.json: verify-цепочка содержит `npm run check-tone`
    - `npm run lint` — exit 0
    - `npm run verify` — exit 0: build зелёный; check-seo (10–11 страниц, sitemap со всеми кейсами, пары уникальны); check-tokens (W1: кейсы 0, верхние 1; 1 script на /contact); check-collections (5–6, featured 2–3, theme ≠ terracotta, cover-файлы, 4 h2, дублей нет, notes 0); check-contrast; check-prohibitions; check-tone (денлист ≥10, ассерты, без ложных срабатываний); check-visual (скролл по 4 ширинам + маршрут кейса, hover CTA и футер-ссылки, reduced-motion, копирование email)
    - 03-VERIFICATION.md содержит: состав кейсов; по каждому кейсу отметки K1–K4 и тему; итоги всех команд гейта; раздел ручной проверки тона по спискам REQ-tone
  </acceptance_criteria>
  <done>Полный гейт фазы зелёный (lint → verify → CI); журнал 03-VERIFICATION.md с K1–K4 и ручным проходом тона; всё закоммичено</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX-контент → сборка | Авторский контент (агент + пользователь), публикуется в public-репозиторий |
| src/assets/projects/ → build pipeline | Скриншоты пользователя публикуются осознанно |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Information Disclosure | src/content/projects/*.mdx | high | mitigate | K1–K4 при утверждении каждого кейса (Task 2): без вымышленных фактов, чувствительных данных клиентов; NDA-анонимизация (D-03, K2); репозиторий public — осознанная публикация |
| T-03-15 | Information Disclosure | src/assets/projects/ скриншоты | medium | mitigate | Реальные интерфейсы (K4), без личных данных клиентов; рекомендации по размеру (Pitfall 12); дубли файлов между кейсами запрещены и проверяются (R8) |
| T-03-16 | Tampering | verify-цепочка package.json | low | mitigate | check-tone добавляется только когда real-run зелёный; цепочка исполняется локально и в CI (01-07) |
| T-03-17 | Tampering | scripts/check-tone.mjs (денлист) | low | accept | Ложные срабатывания корректируются вручную при прогоне (A4); порог ≥10 записей сохраняется (R9) |
</threat_model>

<verification>
- `npm run lint` — exit 0
- `npm run verify` — exit 0 (полный состав: build, check-seo, check-tokens, check-contrast, check-collections, check-prohibitions, check-tone, check-visual)
- CI зелёный после push (workflow 01-07: lint → check → build)
- 03-VERIFICATION.md существует с K1–K4 по каждому кейсу и ручным проходом тона (AC R9)
</verification>

<success_criteria>
- Коллекция 5–6 кейсов утверждена (K1–K4) и закоммичена; у каждого — реальный скриншот без дублей
- Все 6 разделов сайта наполнены (Home/Work/Case/Lab/About/Contact); check-seo ожидает 10–11 страниц
- Полный гейт зелёный: lint → verify → CI; журнал фазы зафиксирован
</success_criteria>

<output>
Create `.planning/phases/03-kontent-i-stranicy/03-06-SUMMARY.md` when done
</output>
