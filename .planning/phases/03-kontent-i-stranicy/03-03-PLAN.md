---
phase: 03-kontent-i-stranicy
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/content/projects/{slug1}.mdx
  - src/assets/projects/{slug1}/.gitkeep
  - src/components/ProjectCard.astro
  - src/pages/work.astro
  - src/pages/work/[slug]/index.astro
autonomous: false
requirements:
  - REQ-evidence-cases
must_haves:
  truths:
    - "Первый утверждённый кейс проходит весь путь: запись в коллекции → карточка на /work → страница /work/{slug}/ с 4 разделами (SPEC R2/R3)"
    - "Каждый кейс отвечает на 4 вопроса (проблема → ответственность → решение → результат) и явно разделяет личный вклад, вклад команды и итоговый эффект (REQ-evidence-cases, SPEC edge R3 covered)"
    - "Карточка кейса рендерит title, meta (role · year), summary и тему; ведёт на /work/{slug}/ (AC R2/R4)"
    - "Страница кейса имеет уникальную SEO-пару title/description и попадает в sitemap (SPEC edge R3 covered)"
    - "Work рендерит карточки из коллекции; фикстурные названия отсутствуют в dist (SPEC edge R2 covered)"
  artifacts:
    - path: "src/content/projects/{slug1}.mdx"
      provides: "Первая реальная запись кейса (frontmatter по новой схеме + 4 h2 в теле)"
      contains: "## Проблема"
    - path: "src/pages/work/[slug]/index.astro"
      provides: "Динамический маршрут кейса: getStaticPaths + render + components-prop (h2 → SectionHeading)"
      contains: "getStaticPaths"
    - path: "src/components/ProjectCard.astro"
      provides: "Props summary и href (карточка-ссылка с summary, AC R2/R4)"
      contains: "href?: string"
    - path: "src/pages/work.astro"
      provides: "Список карточек из коллекции вместо фикстурного демо-блока"
      contains: "getCollection('projects')"
  key_links:
    - from: "src/pages/work/[slug]/index.astro"
      to: "src/content/projects/{slug1}.mdx"
      via: "getStaticPaths по коллекции → params.slug; render(entry) — тело рендерится один раз"
      pattern: "getStaticPaths"
    - from: "src/pages/work.astro"
      to: "src/content/projects/{slug1}.mdx"
      via: "getCollection('projects') + sort по order → ProjectCard с theme/cover"
      pattern: "getCollection"
    - from: "src/pages/work/[slug]/index.astro"
      to: "src/components/SectionHeading.astro"
      via: "components-prop: h2 → SectionHeading с моно-метой SECTION_META (PROBLEM/RESPONSIBILITY/SOLUTION/RESULT)"
      pattern: "components={{"
prohibitions:
  - statement: "Кейсы MUST NOT содержать вымышленных проектов и не подтверждённых результатов"
    status: resolved
    verification: judgment — подтверждается пользователем при утверждении каждого кейса (checkpoint T3)
  - statement: "Кейсы MUST NOT раскрывать конфиденциальные данные клиентов; при NDA — анонимизация обязательна (тип клиента вместо имени)"
    status: resolved
    verification: judgment — именование каждого кейса утверждает пользователь (решение «реальные названия, где можно», D-03)
  - statement: "Кейсы MUST NOT приписывать себе вклад команды — личное/командное/эффект разделены честно"
    status: resolved
    verification: judgment — утверждение контента каждого кейса (checkpoint T3)
  - statement: "Заглушки/стоковые изображения MUST NOT выдаваться за реальные интерфейсы кейсов"
    status: resolved
    verification: judgment — скриншоты предоставляет пользователь (D-02, checkpoint T3); R8 покрывает наличие, K4 — достоверность
---

<objective>
Tracer фазы: один утверждённый кейс проходит весь путь end-to-end — запись MDX в коллекции, скриншот в src/assets/projects/, карточка на /work из коллекции (ProjectCard с новыми props summary/href), динамическая страница /work/{slug}/ с четырьмя разделами. Одновременно фиксируется состав кейсов (D-01/D-03/D-07) и запускается процесс утверждения контента (D-04).

Purpose: доказать вертикальный срез «схема → контент → карточка → страница разбора» на первом кейсе до массового наполнения (tracer-first: архитектурный тупик — после одного коммита, а не после десяти). Страница кейса — единственный новый архитектурный паттерн фазы (getStaticPaths + render + components-prop), его проверка на тонком слайсе обязательна.
Output: первый кейс (MDX + папка активов), ProjectCard с summary/href, /work из коллекции, динамический маршрут /work/{slug}/, утверждённый состав кейсов.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kontent-i-stranicy/03-CONTEXT.md
@.planning/phases/03-kontent-i-stranicy/03-RESEARCH.md
@.planning/phases/03-kontent-i-stranicy/03-PATTERNS.md
@.planning/phases/03-kontent-i-stranicy/03-SPEC.md
@.planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md

# Текущее состояние редактируемых файлов
@src/pages/work.astro
@src/components/ProjectCard.astro
@src/content.config.ts
@docs/positioning_portfolio_ivan_shivarshinov.md
@docs/hero-concept.md
</context>

## Artifacts this phase produces

- `src/content/projects/{slug1}.mdx` — первая запись кейса: frontmatter (slug/title/summary/role/stack/year/status/client-type/order/theme/featured/cover/coverAlt) + тело с 4 h2 («Проблема», «Ответственность», «Решение», «Результат»), ~400–700 слов, кавычки frontmatter — одинарные
- `src/assets/projects/{slug1}/` — папка для скриншотов пользователя (D-02), .gitkeep до появления файла
- `src/components/ProjectCard.astro` — новые опциональные props `summary` и `href` (title → ссылка при href, summary — тело под title)
- `src/pages/work.astro` — карточки из коллекции (SectionHeading «Работы» + grid 2 колонки ≥768px)
- `src/pages/work/[slug]/index.astro` — динамический маршрут: шапка-мета, Media со скриншотом, 4 секции через components-prop, навигация «Все работы»/«Следующий кейс»
- Решения в 03-VERIFICATION.md: состав кейсов, темы, именование, K1–K4 по первому кейсу

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Состав кейсов, темы и именование (D-01/D-03/D-07)</name>
  <decision>Состав кейсов фазы: какие 5–6 проектов войдут в портфолио, их темы (D-07), именование клиентов (D-03) и 3 избранных (D-09)</decision>
  <context>
    По D-01 состав кейсов предлагает агент, утверждает пользователь; тем по D-07 — ассоциация с характером проекта из пула clay/olive/slate/plum (терракота кейсам НЕ назначается, D-06); именование — реальные названия где допустимо, при NDA — тип клиента (K2). Скриншоты для всех кейсов предоставит пользователь (D-02). Слаги должны соответствовать регэкспу схемы /^[a-z0-9-]+$/ (03-01).
  </context>
  <options>
    <option id="six-cases">
      <name>6 кейсов (предложение агента)</name>
      <pros>Покрывает 4 сегмента аудитории PRD; максимум границы 5–6; запас при отказе одного</pros>
      <cons>Больше контента на утверждение; дольше фаза</cons>
    </option>
    <option id="five-cases">
      <name>5 кейсов</name>
      <pros>Меньше объём утверждения; ровно нижняя граница</pros>
      <cons>Меньше доказательной базы для аудиторий</cons>
    </option>
    <option id="custom">
      <name>Пользователь правит состав/темы/именование</name>
      <pros>Состав точно отражает реальные проекты и NDA-ограничения</pros>
      <cons>Дольше интервью</cons>
    </option>
  </options>
  <how-to-verify>
    Агент показывает в чате предложение: 6 кандидатов из PRD-материала и REQ-образцов (например: CMS и мультиязычная структура для новостного портала — olive, медиа; исправление canonical и структуры индексации — slate, корпоративный сайт; контентная система для экспертной фирмы — clay, B2B; редизайн и перенос на Webflow — plum, агентство/дизайнер; веб-инструмент/автоматизация процессов — clay, SaaS; лендинг/многостраничник для SaaS — olive) + предложение 3 избранных (featured) + вопрос о реальных названиях клиентов (NDA). Пользователь выбирает вариант или правит состав; фиксирует slug/темы/именование. Дополнительно агент собирает материал интервью (факты по каждому кейсу: проблема, ответственность, решение, результат, метрики).
  </how-to-verify>
  <resume-signal>Состав утверждён (вариант + правки) — или: «измените …»</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Первый кейс end-to-end — MDX, ProjectCard summary/href, /work из коллекции, маршрут /work/{slug}/</name>
  <files>src/content/projects/{slug1}.mdx, src/assets/projects/{slug1}/.gitkeep, src/components/ProjectCard.astro, src/pages/work.astro, src/pages/work/[slug]/index.astro</files>
  <read_first>
    src/pages/work.astro — текущий демо-блок с фикстурными карточками (строки 8-46): каркас страницы сохраняется (BaseLayout pageLabel="02 / WORK", SectionHeading, grid-классы)
    src/components/ProjectCard.astro — текущий контракт props (строки 8-26), ACCENTS-маппинг (19-25), структура article/card__body (28-42), scoped styles (44-102)
    src/content.config.ts — новая схема projects (03-01): theme/featured/cover/coverAlt/регэксп слага
    .planning/phases/03-kontent-i-stranicy/03-PATTERNS.md — разделы «src/pages/work.astro», «src/pages/work/[slug]/index.astro», «src/content/projects/{slug}.mdx», «ProjectCard»
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Pattern 2 (getStaticPaths + render + components-prop, строки 246-282), Code Example 1 (шаблон frontmatter), Code Example 3 (карточка с Image), Pitfall 5 (image()-объект, не URL), Pitfall 8 (pageLabel кейсов = «02 / WORK»), Pitfall 10 (сортировка по order)
    .planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md — Case page header/sections/bottom nav, Work list, ProjectCard contract (таблицы Component Contracts)
  </read_first>
  <action>
    Первый утверждённый кейс (slug1/тема/имя — из решения Task 1). Все записи — по D-04 как ЧЕРНОВИКИ: файлы пишутся, текст показывается в чате, коммит — только после утверждения Task 3.

    1. src/content/projects/{slug1}.mdx — по RESEARCH Code Example 1:
       - frontmatter (кавычки одинарные, репо-конвенция): slug (из решения Task 1, соответствует /^[a-z0-9-]+$/), title, summary (1-2 предложения), role (напр. «ведущий веб-разработчик»), stack (массив, 3-6 элементов), year (число), status: 'active', client-type (тип клиента — решение Task 1), order: 1, theme (из решения Task 1, НЕ terracotta — D-06), featured (из решения Task 1: true — кейс входит в 3 избранных), cover: '../../assets/projects/{slug1}/cover.png', coverAlt (описание скриншота, 1 предложение)
       - тело — ровно 4 h2 (ровно один раз каждый): «Проблема», «Ответственность», «Решение», «Результат»; в «Ответственности» — strong-лейблы «Моя зона.», «Команда.», «Эффект.» (явное разделение вклада — REQ-evidence-cases); 400–700 слов; без хайпа и без ярлыков-клише (REQ-tone); факты — только из интервью Task 1 (K1)
       - сводка кейса показывается в чате: текст + куда легли данные (D-04)
    2. src/assets/projects/{slug1}/.gitkeep — папка-место для скриншота пользователя (D-02); cover-поле уже указывает на ../../assets/projects/{slug1}/cover.png
    3. src/components/ProjectCard.astro — добавить в interface Props (строки 8-14) и деструктуризацию (16): `summary?: string;` и `href?: string;`. Рендер (строки 28-42): при href — h3.card__title оборачивается в `<a class="card__title-link" href={href}>` (контракт Link: ink → accent на hover + text-decoration underline, text-underline-offset 2px); между title и meta — `<p class="card__summary">{summary}</p>` при наличии (роль Body: font-family var(--font-sans), font-size var(--text-body), line-height 1.5, цвет var(--color-ink-muted), margin 0). CSS: .card__title-link { color: var(--color-ink); text-decoration: none; } .card__title-link:hover, .card__title-link:focus-visible { color: var(--project-accent, var(--color-accent)); text-decoration: underline; text-underline-offset: 2px; }. НЕ менять: ACCENTS-маппинг (19-25), showMetrics-логику, layout split/stacked, обёртку article.
    4. src/pages/work.astro — заменить демонстрационный блок (строки 22-44) на карточки из коллекции: в frontmatter `const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);` + import Image из astro:assets; разметка по RESEARCH Code Example 3: SectionHeading (meta="CASE STUDIES", title="Работы", layout="split" — как в текущем файле) + список карточек: ProjectCard title/meta=`${role} · ${year}`/theme/summary/href=`/work/${slug}/`, слот media — Media ratio="4:3" + Image (src=cover — image()-объект, НЕ URL-путь, Pitfall 5; alt=coverAlt), дефолтный слот — p.data.stack.slice(0, 3) → Tag. Grid-стили списка сохранить (переименовать классы демо-блока, например .system-demo__list → .work__list, без изменения значений). Фикстурные карточки и маркер-комментарий фикстуры удаляются целиком (AC R2).
    5. src/pages/work/[slug]/index.astro — НОВЫЙ маршрут по RESEARCH Pattern 2:
       - getStaticPaths: (await getCollection('projects')).sort((a,b) => a.data.order - b.data.order) → params {slug: entry.data.slug}, props {entry, projects}
       - const { Content } = await render(entry); SECTION_META = { 'Проблема': 'PROBLEM', 'Ответственность': 'RESPONSIBILITY', 'Решение': 'SOLUTION', 'Результат': 'RESULT' }
       - BaseLayout: title={`${title} — кейс · Иван Шиваршинов`}, description={summary}, pageLabel="02 / WORK" (НЕ «…/SLUG» — Pitfall 8: метка Footer из последнего сегмента)
       - шапка: моно-мета (font-mono, --text-label, --color-ink-muted): `${role} · ${year} · ${stack.join(', ')} · ${clientType}` — переносится естественно, без nowrap
       - Media ratio="16:9" caption={coverAlt} + Image (src=cover, alt=coverAlt, class для object-fit: cover)
       - тело: `<Content components={{ h2: (props) => <SectionHeading meta={SECTION_META[props.children] ?? 'SECTION'} title={props.children} /> }} />` — единственный рендер тела (Анти-паттерн: не дублировать h2)
       - нижняя навигация: Link «Все работы» (←, href="/work") + Button secondary md «Следующий кейс» (next = projects[(idx + 1) % projects.length], href=`/work/${next.data.slug}/`)
       - scoped style: только var()-токены; prose-контракт из UI-SPEC (абзацы ≤ ~65ch, --space-2xl между секциями)
  </action>
  <verify>
    <automated>node scripts/check-tone.mjs --self-test && node scripts/check-tokens.mjs --self-test</automated>
    <human-check>Полный build + check-seo/check-tokens по dist — после Task 3 (build требует файла скриншота: cover: image() валидирует существование файла, Pitfall 6)</human-check>
  </verify>
  <acceptance_criteria>
    - src/content/projects/{slug1}.mdx содержит все поля новой схемы (theme/featured/cover/coverAlt) и ровно 4 h2 («Проблема», «Ответственность», «Решение», «Результат»), каждый ровно один раз; в «Ответственности» есть «Моя зона.», «Команда.», «Эффект.»
    - src/assets/projects/{slug1}/.gitkeep существует
    - ProjectCard.astro: interface Props содержит `summary?: string` и `href?: string`; при href title рендерится в `<a class="card__title-link" href={href}>`; .card__summary использует var(--text-body)/var(--color-ink-muted)
    - work.astro: `getCollection('projects')` + sort по order; карточки получают summary/href/theme/cover; фикстурный демо-блок удалён (в src/pages/work.astro нет классов demo-блока и маркера-комментария фикстуры)
    - src/pages/work/[slug]/index.astro: getStaticPaths по коллекции, render(entry), components-prop h2 → SectionHeading, pageLabel="02 / WORK", уникальная SEO-пара, нижняя навигация с «Следующий кейс» по order с замыканием
    - node scripts/check-tone.mjs --self-test и node scripts/check-tokens.mjs --self-test — exit 0
  </acceptance_criteria>
  <done>Черновик первого кейса и весь маршрут написаны и показаны в чате; коммит — после утверждения Task 3</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Утверждение первого кейса + скриншот (K1–K4)</name>
  <what-built>Черновик первого кейса (текст в чате + src/content/projects/{slug1}.mdx), карточка и страница разбора на /work и /work/{slug1}/, папка src/assets/projects/{slug1}/ под скриншот</what-built>
  <how-to-verify>
    1. Прочитайте черновик кейса в чате: факты (K1 — не вымышленные), именование клиента (K2 — NDA-анонимизация где нужно), разделение «Моя зона.»/«Команда.»/«Эффект.» (K3), тема карточки (D-07 — можно поменять).
    2. Положите реальный скриншот проекта файлом в src/assets/projects/{slug1}/cover.png (PNG/JPG, реальный интерфейс — K4; рекомендация: 1280–1920px по ширине, ~1–2 МБ, Pitfall 12). Если имя/формат иные — скажите, агент поправит путь в frontmatter.
    3. После ответа агент соберёт сайт (npm run build) и прогонит check-seo (ожидание 6 страниц) и check-tokens (W1 на странице кейса — 0 aria-current) и закоммитит файлы.
  </how-to-verify>
  <resume-signal>Кейс утверждён (K1–K4) + скриншот положен — или правки формулировок/темы</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX-контент → сборка | Авторский контент (агент + пользователь) компилируется в статику; пользовательский ввод отсутствует |
| src/assets/projects/ → build pipeline | Файлы скриншотов пользователя попадают в публичный репозиторий |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Information Disclosure | src/content/projects/*.mdx | high | mitigate | K1–K4 при утверждении каждого кейса (checkpoint T3): без чувствительных данных клиентов; NDA-анонимизация (D-03, K2); репозиторий public — контент публикуется осознанно |
| T-03-06 | Information Disclosure | src/assets/projects/* скриншоты | medium | mitigate | Скриншоты предоставляет пользователь (D-02, K4) без личных данных клиентов; рекомендация по размеру файлов (Pitfall 12); визуальная сверка при утверждении |
| T-03-07 | Tampering | src/pages/work/[slug]/index.astro | low | accept | Авторский MDX, компилируется на сборке; схемная валидация frontmatter; страница — чистый SSG без инпута |
</threat_model>

<verification>
- После Task 3: `npm run build` — exit 0 (включая astro check; скриншот от пользователя)
- `node scripts/check-seo.mjs` — exit 0 (ожидание 6 страниц: 5 + 1 кейс — параметризация 03-02)
- `node scripts/check-tokens.mjs` — exit 0 (страница кейса: 0 aria-current; верхнеуровневые: ровно 1)
- grep по dist: dist/work/index.html содержит N карточек-ссылок /work/{slug1}/; dist/work/{slug1}/index.html существует
- check-collections (аудит границ) на этой волне не запускается — 1 запись вне диапазона 5–6 (финальный гейт — 03-06)
</verification>

<success_criteria>
- Первый кейс утверждён пользователем (K1–K4) и закоммичен; скриншот в репозитории
- /work рендерит карточки из коллекции; /work/{slug1}/ открывается с 4 разделами
- ProjectCard принимает summary/href; сборка и целевые проверки зелёные
</success_criteria>

<output>
Create `.planning/phases/03-kontent-i-stranicy/03-03-SUMMARY.md` when done
</output>
