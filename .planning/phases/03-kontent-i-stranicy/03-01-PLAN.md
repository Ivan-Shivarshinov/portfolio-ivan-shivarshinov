---
phase: 03-kontent-i-stranicy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - scripts/check-collections.mjs
autonomous: true
requirements:
  - REQ-evidence-cases
must_haves:
  truths:
    - "Контракт «5–6 записей» реализован: check-collections падает при 4 и при 7 записях (SPEC edge R2 covered)"
    - "Схема projects валидирует theme (zod enum terracotta/clay/olive/slate/plum, D-05), featured (boolean, default false, D-08), cover (image()), coverAlt при сборке (SPEC edge R8 covered)"
    - "Коллекция contacts читается через strict-json-loader из src/data/contacts.json (D-11, SPEC edge R7 adjacency covered)"
    - "Фикстура негативного теста «дубликат slug» схемо-совместима с новой схемой (Pitfall 9 закрыт: падение строго от DuplicateContentEntrySlugError)"
    - "D-06 соблюдён на уровне проверки: кейсам запрещена тема terracotta"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Схема projects с полями theme/featured/cover/coverAlt, коллекция contacts"
      contains: "theme: z.enum(['terracotta', 'clay', 'olive', 'slate', 'plum'])"
    - path: "scripts/check-collections.mjs"
      provides: "Границы коллекций: 5–6 записей, featured 2–3, theme без terracotta, cover-файл существует, 4 h2 в теле, дублей скриншотов нет, notes 0"
      exports: ["auditCollections"]
  key_links:
    - from: "scripts/check-collections.mjs"
      to: "src/content.config.ts"
      via: "projectFixture обязан быть схемо-совместим с расширенной схемой (Pitfall 9)"
      pattern: "theme: clay"
prohibitions: []
---

<objective>
Контракты контента фазы 3: схема проектов расширяется полями `theme` (D-05), `featured` (D-08), `cover`/`coverAlt` (R8), вводится коллекция `contacts` (D-11); check-collections получает жёсткие границы коллекций (5–6 записей, featured 2–3, темы без terracotta, скриншоты, 4 раздела в теле).

Purpose: данные — в файлах, не в коде (D-02/D-11); каждая контрактная граница фазы получает check-скрипт с self-test (паттерн проекта, 01-02). Без схемы невозможны ни записи кейсов, ни страницы, ни проверки — это интерфейсный контракт, который потребляют все последующие планы (03-02…03-06).
Output: расширенная `src/content.config.ts`, обновлённый `scripts/check-collections.mjs` с self-test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kontent-i-stranicy/03-CONTEXT.md
@.planning/phases/03-kontent-i-stranicy/03-RESEARCH.md
@.planning/phases/03-kontent-i-stranicy/03-PATTERNS.md
@.planning/phases/03-kontent-i-stranicy/03-SPEC.md
@.planning/phases/03-kontent-i-stranicy/03-UI-SPEC.md

# Источник текущего состояния редактируемых файлов
@src/content.config.ts
@scripts/check-collections.mjs
</context>

## Artifacts this phase produces

- `src/content.config.ts` — поля projects: `theme` (zod enum 5 значений, D-05), `featured` (boolean default false, D-08), `cover: image()` + `coverAlt` (R8), регэксп слага `/^[a-z0-9-]+$/`; новая коллекция `contacts` (strict-json-loader, схема `{id, label, value, href}`, D-11)
- `scripts/check-collections.mjs` — обновлённая фикстура `projectFixture()` (схемо-совместимость, Pitfall 9) + аудит границ: projects 5–6 (FAIL при 4/7), featured 2–3, theme ∈ enum и ≠ terracotta (D-06), cover-файл существует на диске, ровно 4 h2 в теле (по одному разу), дублей файлов скриншотов нет, notes = 0 (D-07)

<tasks>

<task type="auto">
  <name>Task 1: Расширить схему projects и добавить коллекцию contacts в content.config.ts</name>
  <files>src/content.config.ts</files>
  <read_first>
    src/content.config.ts — текущая схема projects (строки 124-146), лоадеры strictProjectId/strictJsonLoader (строки 54-122), экспорт collections (строка 174)
    .planning/phases/03-kontent-i-stranicy/03-PATTERNS.md — раздел «src/content.config.ts» (целевая форма схемы)
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Pattern 1 (схема с image()-хелпером, строки 214-239)
  </read_first>
  <action>
    В src/content.config.ts изменить коллекцию projects (строки 124-146):
    - форму schema заменить с `schema: z.object({...})` на `schema: ({ image }) => z.object({...})` — image()-хелпер Astro (валидация файла при сборке, R8)
    - в z.object добавить поля (порядок не важен, кавычки frontmatter-полей — как в существующем файле):
      - `theme: z.enum(['terracotta', 'clay', 'olive', 'slate', 'plum'])` — обязательное (D-05); комментарий: тема — свойство кейса, рендерится в карточке Work, карточке Home и на странице кейса
      - `featured: z.boolean().default(false)` — обязательное с дефолтом (D-08): флаг отбора секции «Избранные работы»
      - `cover: image()` — обязательное: скриншот кейса, файл существует относительно папки записи (R8, D-02)
      - `coverAlt: z.string()` — обязательное: alt для Image
    - существующему полю `slug` добавить регэксп: `slug: z.string().regex(/^[a-z0-9-]+$/, 'slug: только латиница, цифры и дефисы (URL-сегмент)')` (A2)
    Добавить коллекцию contacts по образцу services (строки 158-162):
    - `const contacts = defineCollection({ loader: strictJsonLoader('./src/data/contacts.json'), schema: z.object({ id: z.string(), label: z.string(), value: z.string(), href: z.string() }) })`
    - зарегистрировать в `export const collections` (строка 174): `{ projects, notes, services, skills, tools, contacts }`
    НЕ менять: strictProjectId, strictJsonLoader, коллекции notes/services/skills/tools, кавычки frontmatter-конвенцию файла.
    ВАЖНО: src/data/contacts.json пока не существует — strict-json-loader логирует ошибку и продолжает сборку (не падает); файл создаётся в плане 03-04.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Сборка зелёная (astro check + astro build), коллекция contacts зарегистрирована — `export const collections` содержит contacts</human-check>
  </verify>
  <acceptance_criteria>
    - src/content.config.ts содержит `schema: ({ image }) => z.object({` для projects
    - src/content.config.ts содержит `theme: z.enum(['terracotta', 'clay', 'olive', 'slate', 'plum'])`
    - src/content.config.ts содержит `featured: z.boolean().default(false)`
    - src/content.config.ts содержит `cover: image()` и `coverAlt: z.string()`
    - src/content.config.ts содержит `slug: z.string().regex(/^[a-z0-9-]+$/`
    - src/content.config.ts содержит `const contacts = defineCollection({` с `strictJsonLoader('./src/data/contacts.json')` и схемой id/label/value/href
    - `export const collections` включает contacts
    - `npm run build` завершается с exit 0 (коллекция projects пуста — пустые коллекции проходят, контракт 01-04)
  </acceptance_criteria>
  <reversibility rating="costly">Схема projects — контракт (D-05): check-collections, Stacki-отображение, frontmatter 5–6 кейсов; удаление поля theme потребует правки всех записей и проверок</reversibility>
  <done>Схема расширена и валидируется сборкой; коллекция contacts зарегистрирована; сборка зелёная</done>
</task>

<task type="auto">
  <name>Task 2: Обновить check-collections.mjs — схемо-совместимая фикстура + границы коллекций</name>
  <files>scripts/check-collections.mjs</files>
  <read_first>
    scripts/check-collections.mjs — текущая фикстура projectFixture (строки 47-61), негативные тесты (130-188), assertTreeClean (194-205), runSelfTest (225-271)
    src/content.config.ts — новая схема projects (Task 1 этого плана): фикстура обязана быть схемо-совместимой
    .planning/phases/03-kontent-i-stranicy/03-RESEARCH.md — Code Example 2 (структурная проверка 4 секций), Pitfall 9 (схемо-совместимость фикстур), Code Example 1 (формат frontmatter кейса)
  </read_first>
  <action>
    В scripts/check-collections.mjs:

    1. projectFixture (строки 47-61) дополнить полями новой схемы, чтобы падение сборки было строго от DuplicateContentEntrySlugError, а не от zod (Pitfall 9):
       - `theme: clay` (валидное значение; терракота кейсам не назначается — D-06)
       - `featured: false`
       - `cover: './zz-check-cover.png'` — относительный путь от файла записи (фикстуры лежат в src/content/projects/)
       - `coverAlt: fixture`
       Тест «дубликат slug» (строки 130-151): перед writeFixture(a/b) записать минимальный PNG по пути PROJECTS_DIR/zz-check-cover.png (1×1 пиксель, base64 `iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==`, Buffer.from(..., 'base64')); удалять его в finally вместе с фикстурами (rmSafe). Ожидаемая ошибка в stderr — DuplicateContentEntrySlugError, а не zod-ошибка поля.

    2. Добавить аудит границ (чистые функции + реальный прогон по src/content/projects без сборки; паттерн walk() уже есть):
       - `auditCaseBoundaries()` — по всем .mdx в src/content/projects:
         - число записей ∈ [5, 6] — иначе FAIL с сообщением «записей: N, ожидалось 5–6» (SPEC edge R2 covered: падает при 4 и при 7, «0 записей = FAIL»)
         - featured-записей ∈ [2, 3] — иначе FAIL (D-09)
         - theme каждой записи ∈ {terracotta, clay, olive, slate, plum} И ≠ terracotta (D-06) — иначе FAIL
         - у каждой записи frontmatter-поле cover непустое и файл по пути (резолв относительно директории записи) существует на диске — иначе FAIL (R8, Pitfall 6)
         - нормализованные абсолютные пути всех cover уникальны (дубль файла между кейсами — FAIL, SPEC edge R8 adjacency covered)
         - тело MDX (после frontmatter) содержит ровно по одному разу каждый из четырёх заголовков: `## Проблема`, `## Ответственность`, `## Решение`, `## Результат` (Research Code Example 2: indexOf === -1 → «отсутствует», indexOf !== lastIndexOf → «повторяется»)
       - `notes`-директория: 0 записей .md (D-07, контракт 01-04) — иначе FAIL
       - Результат — массив проблем; в реальном прогоне (точка входа, строка 290+) после негативных тестов вывести `check-collections: аудит границ` + проблемы (exit 1 при наличии). Парсинг frontmatter — простыми regex-матчами строк (`/^theme:\s*(.+)$/m` и т.п.) в стиле проекта, без YAML-библиотеки.

    3. runSelfTest (строки 225-271) дополнить inline-фикстурами новых аудитов:
       - countBoundaries: 5 → PASS, 4 → FAIL, 7 → FAIL, 0 → FAIL
       - featured 2 → PASS, 3 → PASS, 1 → FAIL, 4 → FAIL
       - theme: terracotta → FAIL (D-06), plum → PASS, невалидное → FAIL
       - bodyAudit: тело с 4 заголовками по одному разу → PASS; без «## Решение» → FAIL; с повторённым «## Проблема» → FAIL
       - dupFiles: две записи с одинаковым cover → FAIL
       - notes 1 запись → FAIL
       - существующие self-test-проверки (создание/удаление фикстур, backup/restore services.json) не ломаются

    НЕ менять: негативные тесты duplicateSlugTest/missingFieldTest/duplicateIdTest (кроме cover-файла в duplicateSlugTest), assertTreeClean, контракт 01-02-структуры. Комментарии файла — на русском, стиль существующих.
  </action>
  <verify>
    <automated>node scripts/check-collections.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - projectFixture содержит theme/featured/cover/coverAlt (cover — путь к zz-check-cover.png)
    - duplicateSlugTest пишет и удаляет zz-check-cover.png в src/content/projects/ (rmSafe в finally)
    - В файле есть функция аудита границ с проверками: число записей 5–6, featured 2–3, theme ≠ terracotta, cover-файл существует, 4 h2 в теле, дубли cover запрещены, notes = 0
    - `node scripts/check-collections.mjs --self-test` — exit 0
    - `npm run build` — exit 0 (после Task 1)
  </acceptance_criteria>
  <done>Фикстура схемо-совместима; границы коллекций реализованы и покрыты self-test; самотест зелёный</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter/JSON → zod-схема | Невалидированные данные контента пересекают границу при сборке (validate на build) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | src/content.config.ts (zod-схемы) | low | mitigate | Контент авторский (агент + пользователь, D-01/D-04); схемы валидируют все поля при сборке; строгие лоадеры (дубль slug/id падает сборку) |
| T-03-02 | Information Disclosure | src/data/contacts.json | low | mitigate | Файл создаётся в 03-04 с реальными значениями от пользователя (D-12) — без плейсхолдеров в финальном коммите (Pitfall 11); контакты — публичные каналы по выбору пользователя |
</threat_model>

<verification>
- `node scripts/check-collections.mjs --self-test` — exit 0 (после Task 2)
- `npm run build` — exit 0 (после Task 1; коллекция projects пуста, contacts.json отсутствует — сборка проходит, strict-json-loader логирует и продолжает)
- Полный `npm run verify` — финальный гейт фазы (03-06); на промежуточных волнах — целевые проверки задач
</verification>

<success_criteria>
- Схема projects расширена (theme/featured/cover/coverAlt + регэксп слага), коллекция contacts зарегистрирована
- check-collections реализует все границы контракта (5–6, featured 2–3, theme, cover, 4 h2, дубли, notes 0) с self-test
- Сборка зелёная; self-test check-collections зелёный
</success_criteria>

<output>
Create `.planning/phases/03-kontent-i-stranicy/03-01-SUMMARY.md` when done
</output>
