---
phase: 01-osnova-proekta
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - tsconfig.json
  - .gitignore
  - eslint.config.mjs
  - .prettierrc
  - scripts/check-prohibitions.mjs
  - src/styles/tokens.css
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/Seo.astro
  - src/pages/index.astro
  - public/fonts/space-grotesk-400.woff2
  - public/fonts/space-grotesk-600.woff2
  - public/fonts/jetbrains-mono-400.woff2
autonomous: true
requirements: [R1, R2, R4, R6]
must_haves:
  truths:
    - "npm ci && npm run build завершаются кодом 0 на чистой установке (R1 smoke)"
    - "Preview отдаёт HTTP 200 с HTML на маршруте / (tracer-срез; полные 5 маршрутов — 01-05)"
    - "Ровно один файл токенов в проекте; 5 групп CSS-переменных (color, typography, spacing, containers, motion) определены в src/styles/tokens.css и потребляются через var() в global.css — node scripts/check-tokens.mjs exit 0 (SPEC R2 concurrency edge)"
    - "Шрифты Space Grotesk 400/600 и JetBrains Mono 400 подключены локально: WOFF2 в public/fonts, @font-face с font-display: swap (D-03)"
    - "BaseLayout рендерит Seo.astro в head с типизированными props title/description; страница index.astro передаёт уникальные title/description"
    - "Навигация содержит 5 коротких меток (Work, Lab, About, Contact), не требующих переноса (UI-SPEC long-text nav)"
    - "Хедер не содержит фиксированных ширин — на узких экранах метки переносятся без горизонтального скролла (UI-SPEC overflow nav)"
    - "package.json содержит engines.node >=22.22.3 и скрипты dev/build/preview/lint/check/verify (R6 engines)"
  artifacts:
    - package.json
    - package-lock.json
    - astro.config.mjs
    - tsconfig.json
    - .gitignore
    - eslint.config.mjs
    - .prettierrc
    - scripts/check-prohibitions.mjs
    - src/styles/tokens.css
    - src/styles/global.css
    - src/layouts/BaseLayout.astro
    - src/components/Seo.astro
    - src/pages/index.astro
    - public/fonts/space-grotesk-400.woff2, space-grotesk-600.woff2, jetbrains-mono-400.woff2
  key_links:
    - "Seo.astro строит canonical/og:url из Astro.site — site задан в astro.config.mjs (Pitfall 4)"
    - "global.css и страницы потребляют только var(--token) — связь токены → стили (R2)"
    - "tokens.css — единственный источник значений: любой hex вне него — нарушение (check-tokens.mjs)"
    - "package-lock.json закоммичен — npm ci в CI (Pitfall 10)"
prohibitions:
  - statement: "Utility-first CSS фреймворк (Tailwind и аналоги) НЕ добавляется в фазе 1"
    status: resolved
    verification: "test — node scripts/check-prohibitions.mjs (GSD_PROHIB_SUBJECT) exit 0; известная-хорошая/известная-плохая фикстуры"
  - statement: "Тяжёлый клиентский фреймворк НЕ становится основой всего сайта"
    status: resolved
    verification: "judgment — в dependencies/devDependencies нет клиентских фреймворков (react/vue/svelte и т.п.); сайт — статический рендер Astro"
---

<objective>
Tracer-план фазы: протолкнуть один путь сквозь все слои, которые фаза будет модифицировать — конфигурация (package.json, astro.config.mjs, tsconfig.json, .gitignore) → установка зависимостей → глобальные стили (tokens.css, global.css) → layout (BaseLayout) → SEO-компонент (Seo.astro) → одна страница (index.astro) → локальные шрифты → production-сборка → preview. Путь проверяется end-to-end (build exit 0 + preview HTTP 200 на / + check-tokens.mjs), после чего остальные планы расширяют каркас горизонтально (контент, страницы, spike, CI).

Purpose: доказать архитектуру каркаса (Stacki-совместимая композиция layout + плоские компоненты с props, контентный слой, токен-дисциплина) на одном коммите до того, как будут написаны десятки файлов. Каркас — production-качество: те же конфиги, токены и SEO-компонент, на которых строится весь сайт.

Покрытие Wave 0 gap 6: этот план создаёт package.json с полным набором скриптов (dev/build/preview/lint/check/verify) и engines — единственный оставшийся пункт списка 01-VALIDATION.md.

Output: работающий Astro-каркас с одной страницей, токенами, layout, SEO и lint-конфигом; сборка и preview зелёные.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-osnova-proekta/01-RESEARCH.md
@.planning/phases/01-osnova-proekta/01-PATTERNS.md
@.planning/phases/01-osnova-proekta/01-UI-SPEC.md
@.planning/phases/01-osnova-proekta/01-CONTEXT.md
@.planning/phases/01-osnova-proekta/01-02-PLAN.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Tracer: каркас Astro + первая страница end-to-end (config → layout → SEO → tokens → build → preview)</name>
  <files>
    package.json, package-lock.json, astro.config.mjs, tsconfig.json, .gitignore,
    src/styles/tokens.css, src/styles/global.css, src/layouts/BaseLayout.astro,
    src/components/Seo.astro, src/pages/index.astro
  </files>
  <read_first>
    - 01-RESEARCH.md (Code Examples: строки 384-417 — package.json/astro.config/tsconfig; строки 419-454 — tokens/global; строки 262-303 — Pattern 2 и 3; Pitfalls 1, 2, 4, 5, 9, 10)
    - 01-PATTERNS.md (секции package.json, astro.config.mjs, tsconfig.json, tokens.css, global.css, BaseLayout.astro, Seo.astro, pages — строки 48-100, 175-275)
    - 01-UI-SPEC.md (Design System, Typography, Color, Containers, Motion — точные значения токенов и пропсов)
  </read_first>
  <action>
    Ручной скаффолд по официальному гайду install-and-setup (create-astro в непустом каталоге с docs/ недетерминирован — Pitfall 5). Перед любым npm install прочитать решение 01-01 (мажор Astro).
    1. npm init --yes в корне репозитория.
    2. Установка по решению 01-01: вариант astro-7: `npm install astro@^7 @astrojs/mdx@^7 @astrojs/sitemap`; вариант astro-5: `npm install astro@^5 @astrojs/mdx@^4 @astrojs/sitemap`. Dev-зависимости в обоих вариантах: @astrojs/check@^0.9, typescript@^6 (НЕ 7 — peer-диапазон @astrojs/check, Pitfall 2), @astrojs/ts-plugin, eslint@^10, eslint-plugin-astro@^3, @typescript-eslint/parser, eslint-plugin-jsx-a11y, prettier, prettier-plugin-astro. Мажоры интеграций обязаны совпадать с мажором astro (Pitfall 1). — MDX-расширение устанавливается сразу: тело кейса — MDX (D-06).
    3. package.json: скрипты dev="astro dev", build="astro check && astro build", preview="astro preview", lint="eslint .", check="astro check"; verify скрипт дополняется в Task 2. engines.node=">=22.22.3" (перекрывает astro >=22.12 и eslint-plugin-astro ^22.22.3 — Pitfall 9).
    4. astro.config.mjs: import defineConfig из astro/config, mdx из @astrojs/mdx, sitemap из @astrojs/sitemap; export default defineConfig({ site: <значение решения 01-01 T2>, integrations: [mdx(), sitemap()] }). Никакого viewTransitions: true (устарел — State of the Art).
    5. tsconfig.json: extends "astro/tsconfigs/strict"; include [".astro/types.d.ts", "**/*"]; exclude ["dist"]; compilerOptions.paths { "@/*": ["./src/*"] }.
    6. .gitignore: node_modules/, dist/, .astro/, *.local, .DS_Store, .env*.
    7. src/styles/tokens.css — ЕДИНСТВЕННЫЙ файл токенов, ровно значения из UI-SPEC/PATTERNS: цвет — --color-bg:#FAFAF7, --color-surface:#F1F0EB, --color-ink:#1F1E1C, --color-ink-muted:#5C5A55, --color-accent:#A84B32, --color-accent-ink:#FFFFFF, --color-destructive:#B3261E; типографика — --font-sans:'Space Grotesk',system-ui,sans-serif, --font-mono:'JetBrains Mono',monospace, --text-body:16px, --text-label:14px, --text-heading:20px, --text-display:32px; отступы — --space-xs:4px, --space-sm:8px, --space-md:16px, --space-lg:24px, --space-xl:32px, --space-2xl:48px, --space-3xl:64px; контейнеры — --container-max:1200px, --gutter-mobile:24px, --gutter-desktop:48px; motion — --motion-fast:150ms, --motion-base:250ms, --motion-slow:400ms, --ease-standard:cubic-bezier(0.4,0,0.2,1). Комментарии-группы /* color */ /* typography */ /* spacing */ /* containers */ /* motion */.
    8. src/styles/global.css: три @font-face — 'Space Grotesk' 400 (src url('/fonts/space-grotesk-400.woff2') format('woff2')), 'Space Grotesk' 600 (/fonts/space-grotesk-600.woff2), 'JetBrains Mono' 400 (/fonts/jetbrains-mono-400.woff2); каждому font-display: swap; :root { color-scheme: light }; body { font-family: var(--font-sans); font-size: var(--text-body); line-height: 1.5; color: var(--color-ink); background: var(--color-bg) }; :focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px }. Только var()-значения (кроме 2px outline — вне шкалы отступов).
    9. src/components/Seo.astro: interface Props { title: string; description: string; canonical?: string; ogImage?: string }; const url = canonical ?? new URL(Astro.url.pathname, Astro.site!); рендерит title, meta name="description", link rel="canonical", meta property="og:title", og:description, og:type content="website", og:url, og:locale content="ru_RU", условно og:image при ogImage.
    10. src/layouts/BaseLayout.astro: imports '../styles/tokens.css', '../styles/global.css', Seo; interface Props { title: string; description: string }; html lang="ru"; head с <Seo title description />; body: header с nav (ссылки на /, /work, /lab, /about, /contact — метки Home/Work/Lab/About/Contact, простые текстовые ссылки без стилизации, без фиксированных ширин), <slot />, footer-заглушка. НЕ включать ClientRouter (только временная страница spike, A7). Композиция страниц: layout-обёртка + плоские self-closing компоненты с props (D-04).
    11. src/pages/index.astro: BaseLayout с title «Иван Шиваршинов — веб-разработчик с продуктовым подходом», description «Создаю и развиваю сложные сайты, контентные системы и веб-инструменты.»; тело — минимальный статичный контент (заголовок + строка «Фаза 2 — визуальная система»), без клиентского JS, без произвольного HTML. Primary CTA «Смотреть работы» (Home → /work, UI-SPEC copy contract) намеренно не рендерится в стабе — реализуется в фазе 2+ визуальной сборки (UI-SPEC Color: акцент и primary CTA — только с фазы 2; фаза 1 — scaffolding-only); зафиксировать эту отсрочку одной строкой в SUMMARY.
    12. Закоммитить package-lock.json сразу (Pitfall 10). Верификация: npm run build (astro check + astro build) exit 0; node scripts/check-tokens.mjs exit 0; preview: node scripts/verify-preview.mjs --routes / (скрипт сам поднимает astro preview на порту 4321, ждёт готовности и проверяет HTTP 200 + text/html на /; полный 5-маршрутный прогон — 01-05) и grep -q 'rel="canonical"' dist/index.html.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-tokens.mjs && node scripts/verify-preview.mjs --routes / && grep -q 'rel="canonical"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - package.json содержит скрипты dev/build/preview/lint/check и engines.node ">=22.22.3"
    - astro.config.mjs содержит site (значение решения 01-01) и integrations [mdx(), sitemap()]
    - tsconfig.json содержит extends "astro/tsconfigs/strict"
    - Плоская структура src/: pages, layouts, components, styles, content, data (D-01: flat src/ tree — pages, layouts, components, styles, content, data)
    - tokens.css содержит все перечисленные переменные 5 групп с точными значениями UI-SPEC
    - global.css содержит 3 @font-face с font-display: swap и url /fonts/space-grotesk-400.woff2, /fonts/space-grotesk-600.woff2, /fonts/jetbrains-mono-400.woff2
    - Seo.astro содержит все 8 тегов: title, meta description, canonical, og:title, og:description, og:type, og:url, og:locale ru_RU
    - BaseLayout.astro содержит nav со ссылками /work /lab /about /contact
    - npm run build exit 0; node scripts/check-tokens.mjs exit 0; node scripts/verify-preview.mjs --routes / exit 0 (HTTP 200 + text/html на /); grep 'rel="canonical"' в dist/index.html найден
    - package-lock.json закоммичен
  </acceptance_criteria>
  <done>Каркас Astro (package.json, astro.config.mjs, tsconfig strict, .gitignore, src/) собран; index.astro рендерится end-to-end через BaseLayout → Seo.astro с токенами; npm run build exit 0; check-tokens.mjs exit 0; preview / отдаёт 200 + text/html (verify-preview.mjs --routes /), canonical в dist/index.html</done>
</task>

<task type="auto">
  <name>Task 2: скачивание и валидация локальных шрифтов (WOFF2)</name>
  <files>public/fonts/space-grotesk-400.woff2, public/fonts/space-grotesk-600.woff2, public/fonts/jetbrains-mono-400.woff2</files>
  <read_first>
    - 01-UI-SPEC.md (Typography — веса: Space Grotesk 400/600, JetBrains Mono 400)
    - 01-CONTEXT.md (D-03: локальные шрифты, font-display: swap)
  </read_first>
  <action>
    Скачать статические WOFF2 в public/fonts с точными именами space-grotesk-400.woff2, space-grotesk-600.woff2, jetbrains-mono-400.woff2. Основной источник — google/fonts GitHub (папки ofl/spacegrotesk и ofl/jetbrainsmono, статические экземпляры); при недоступности — CDN-зеркало того же репозитория (например, cdn.jsdelivr.net/gh/google/fonts или raw.githubusercontent.com/google/fonts, те же пути).
    Валидация каждого файла: первые 4 байта — сигнатура wOF2; невалидный или отсутствующий файл → повторить из запасного источника.
    Если оба источника недоступны — зафиксировать ограничение в файле .planning/phases/01-osnova-proekta/01-03-SUMMARY.md (создать файл при первом обращении, строку не удалять при финализации SUMMARY) строкой ровно «Ограничение: шрифты не скачаны (оба источника недоступны)» — по этой строке verify задачи пропускает проверку файлов (build не зависит от наличия файлов шрифтов: @font-face резолвится браузером; объявления уже в global.css из Task 1) — и перейти к Task 3.
    Закоммитить шрифты отдельным коммитом от tracer-коммита — внешняя загрузка изолирована от каркаса: сбой внешнего источника не останавливает tracer (scope_sanity W4).
  </action>
  <verify>
    <automated>grep -q "Ограничение: шрифты не скачаны" .planning/phases/01-osnova-proekta/01-03-SUMMARY.md 2>/dev/null && exit 0; node -e "const fs=require('fs');['space-grotesk-400','space-grotesk-600','jetbrains-mono-400'].forEach(n=>{const b=fs.readFileSync('public/fonts/'+n+'.woff2');if(b.subarray(0,4).toString('ascii')!=='wOF2'){console.error('invalid WOFF2: '+n);process.exit(1)}})"</automated>
  </verify>
  <acceptance_criteria>
    - public/fonts содержит 3 файла с точными именами: space-grotesk-400.woff2, space-grotesk-600.woff2, jetbrains-mono-400.woff2
    - Каждый файл валиден: первые 4 байта wOF2 (проверка выше exit 0)
    - Шрифты закоммичены отдельным коммитом; при недоступности обоих источников ограничение зафиксировано в SUMMARY
  </acceptance_criteria>
  <done>Три WOFF2 (Space Grotesk 400/600, JetBrains Mono 400) скачаны, валидны (wOF2) и закоммичены; сбой внешней загрузки не блокирует каркас — ограничение фиксируется в SUMMARY</done>
</task>

<task type="auto">
  <name>Task 3: lint-конфиги + check-prohibitions.mjs + wiring npm verify</name>
  <files>eslint.config.mjs, .prettierrc, scripts/check-prohibitions.mjs, package.json</files>
  <read_first>
    - 01-RESEARCH.md (ESLint flat config: строки 472-485; Standard Stack строки 93-103; Prohibition P1 и его verification в 01-SPEC.md)
    - 01-PATTERNS.md (eslint.config.mjs, строки 101-118)
  </read_first>
  <action>
    Создать eslint.config.mjs: flat config — объект для '**/*.astro' с languageOptions { parser: tsParser, parserOptions: { extraFileExtensions: ['.astro'] } }, затем ...eslintPluginAstro.configs.recommended, затем { ignores: ['dist/', 'node_modules/', '.astro/'] }; импорты: eslintPluginAstro из 'eslint-plugin-astro', tsParser из '@typescript-eslint/parser'.
    Создать .prettierrc: JSON c plugins: ['prettier-plugin-astro'] и overrides: [{ files: '*.astro', options: { parser: 'astro' } }].
    Создать scripts/check-prohibitions.mjs: читает package.json по пути из env GSD_PROHIB_SUBJECT (default './package.json'); проверяет dependencies + devDependencies на отсутствие utility-first CSS фреймворков (denylist: tailwindcss, @tailwindcss/*, windicss, unocss, @unocss/*) и тяжёлых клиентских фреймворков как базовых (react, react-dom, vue, svelte, solid-js, preact, @angular/*); наличие любого → exit 1 со списком найденного; фикстуры: известная-хорошая (текущий package.json) → exit 0, известная-плохая (встроенная строка JSON с tailwindcss) → exit 1 через --self-test.
    Обновить package.json: добавить скрипты check-seo="node scripts/check-seo.mjs", check-tokens="node scripts/check-tokens.mjs", check-collections="node scripts/check-collections.mjs", check-spike-doc="node scripts/check-spike-doc.mjs", verify="npm run build && npm run check-seo && npm run check-tokens && npm run check-collections && node scripts/check-prohibitions.mjs" (полный verify зелёный только после 01-04/01-05; частичный прогон допустим на этом шаге).
    Прогнать npm run lint — исправить любые предупреждения/ошибки ESLint в созданных .astro-файлах; прогнать node scripts/check-prohibitions.mjs (exit 0) и --self-test.
  </action>
  <verify>
    <automated>npm run lint && node scripts/check-prohibitions.mjs && node scripts/check-prohibitions.mjs --self-test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.mjs существует и содержит eslintPluginAstro.configs.recommended, tsParser с extraFileExtensions ['.astro'], ignores dist/node_modules/.astro
    - Стек линта/форматтера — ESLint + eslint-plugin-astro + prettier (D-10)
    - .prettierrc существует и содержит plugins: ['prettier-plugin-astro']
    - check-prohibitions.mjs содержит denylist из utility-first фреймворков и чтение GSD_PROHIB_SUBJECT
    - package.json содержит скрипты check-seo/check-tokens/check-collections/check-spike-doc/verify
    - npm run lint exit 0; node scripts/check-prohibitions.mjs exit 0; node scripts/check-prohibitions.mjs --self-test exit 0
  </acceptance_criteria>
  <done>eslint.config.mjs и .prettierrc созданы; check-prohibitions.mjs проверяет denylist через GSD_PROHIB_SUBJECT и self-test; package.json содержит полный набор скриптов + verify; lint и build зелёные</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm-реестр → проект | Зависимости из внешнего реестра попадают в node_modules и исполняются при сборке |
| локальная ФС → сборка | Скачанные WOFF2-шрифты (google/fonts GitHub) попадают в public/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm install (11 пакетов) | high | mitigate | Аудит легитимности выполнен (RESEARCH.md Package Legitimacy Audit — все Approved, постinstall-скриптов нет); версии зафиксированы в package.json по мажору из решения 01-01; package-lock.json коммитится сразу; в CI — npm ci (Pitfall 10) |
| T-01-03 | Tampering | Зависимости (устаревание) | low | accept | Аудит валиден до 2026-08-31; версии явные; обновление — вне scope фазы |
| T-01-03 | Spoofing | SEO/head-контент страниц | low | mitigate | Astro экранирует атрибуты; title/description — строки из пропсов BaseLayout (типизированы TS strict) |
| T-01-03 | Tampering | Скачанные шрифты | low | mitigate | Источник — официальный google/fonts; файлы проверяются на валидность WOFF2 (заголовок) при приёмке |
| T-01-03 | Elevation | Клиентский JS | low | accept | Фаза 1 — ноль клиентского JS (UI-SPEC Interaction NONE); в собранном HTML отсутствуют client:* директивы (проверка — часть check-seo.mjs в 01-05) |
</threat_model>

<verification>
- [ ] npm run build exit 0 (astro check + astro build)
- [ ] node scripts/check-tokens.mjs exit 0 — один файл токенов, 5 групп, нет хардкода
- [ ] npm run lint exit 0
- [ ] node scripts/check-prohibitions.mjs exit 0 (P1/P2)
- [ ] preview: node scripts/verify-preview.mjs --routes / → 200 + text/html на / (полный 5-маршрутный прогон — 01-05)
</verification>

<success_criteria>
- [ ] Каркас Astro (package.json, astro.config.mjs, tsconfig strict, src/, public/fonts) существует и собирается
- [ ] Одна страница (index) рендерится end-to-end: tokens → global.css → BaseLayout → Seo.astro → HTML с canonical/OG
- [ ] Lint-конфиг и check-prohibitions.mjs на месте; P1/P2 задокументированы в must_haves.prohibitions
- [ ] Wave 0 gaps 1-6 закрыты полностью (скрипты — 01-02, package.json scripts — здесь)
</success_criteria>

<output>
Create `.planning/phases/01-osnova-proekta/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `package.json` — npm scripts: dev, build ("astro check && astro build"), preview, lint, check, check-seo, check-tokens, check-collections, check-spike-doc, verify; `engines.node: ">=22.22.3"`; dependencies: astro (мажор по решению 01-01), @astrojs/mdx, @astrojs/sitemap; devDependencies: @astrojs/check@^0.9, typescript@^6, @astrojs/ts-plugin, eslint@^10, eslint-plugin-astro@^3, @typescript-eslint/parser, eslint-plugin-jsx-a11y, prettier, prettier-plugin-astro
- `astro.config.mjs` — `defineConfig({ site, integrations: [mdx(), sitemap()] })`
- `tsconfig.json` — extends `astro/tsconfigs/strict`, include `[".astro/types.d.ts", "**/*"]`, paths `@/* → ./src/*`
- `src/styles/tokens.css` — CSS custom properties: 7 `--color-*`, 6 `--font-*`/`--text-*`, 7 `--space-*`, 3 `--container-*`, 4 `--motion-*` (точные значения из UI-SPEC)
- `src/styles/global.css` — 3 `@font-face` (Space Grotesk 400/600, JetBrains Mono 400, font-display: swap), `:root color-scheme`, `body`, `:focus-visible`
- `src/components/Seo.astro` — Props `{title, description, canonical?, ogImage?}`; теги title/description/canonical/og:title/og:description/og:type/og:url/og:locale/og:image
- `src/layouts/BaseLayout.astro` — Props `{title, description}`; head + nav (Home/Work/Lab/About/Contact) + slot + footer
- `src/pages/index.astro` — маршрут `/`, title «Иван Шиваршинов — веб-разработчик с продуктовым подходом»
- `public/fonts/` — space-grotesk-400.woff2, space-grotesk-600.woff2, jetbrains-mono-400.woff2
- `eslint.config.mjs` — flat config (eslint-plugin-astro recommended + @typescript-eslint/parser + ignores)
- `.prettierrc` — plugins prettier-plugin-astro, parser astro для *.astro
- `scripts/check-prohibitions.mjs` — CLI `node scripts/check-prohibitions.mjs [--self-test]`; env `GSD_PROHIB_SUBJECT` (путь к package.json); denylist: tailwindcss, @tailwindcss/*, windicss, unocss, @unocss/*, react, react-dom, vue, svelte, solid-js, preact, @angular/*
