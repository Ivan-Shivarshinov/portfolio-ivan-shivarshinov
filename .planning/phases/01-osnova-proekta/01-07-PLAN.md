---
phase: 01-osnova-proekta
plan: 07
type: execute
wave: 5
depends_on: [01-03, 01-04, 01-05, 01-06]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [R6]
must_haves:
  truths:
    - "CI workflow выполняет lint ДО build в одном workflow (порядок шагов: lint → check → build) — содержимое .github/workflows/ci.yml (SPEC ordering R6)"
    - "Workflow зелёный на первом push (пустая история репозитория): GitHub Actions run со статусом success (SPEC empty R6)"
    - "git remote -v показывает GitHub-remote origin (SPEC R6)"
    - "package.json содержит engines.node >= 22.22.3 и скрипты lint/check/build (SPEC R6; исполняются CI)"
  artifacts:
    - .github/workflows/ci.yml
    - GitHub-репозиторий (имя/видимость по решению 01-01 T3)
  key_links:
    - "ci.yml ↔ package.json scripts (lint/check/build) — CI исполняет npm-скрипты (D-11)"
    - "npm ci ↔ package-lock.json — lockfile закоммичен в 01-03 (Pitfall 10)"
    - "local main ↔ origin: первый push запускает workflow (empty R6)"
---

<objective>
Закрыть R6: один CI-workflow (GitHub Actions: lint → astro check → build на каждый push), GitHub-remote создан, первый push зелёный, engines зафиксированы. Линт выполняется до build (SPEC R6 ordering edge); workflow зелёный на первом push с пустой историей (SPEC R6 empty edge).

Purpose: CI — внешняя гарантия качества сборки на каждый push (единый источник истины «Astro + AI-агент + Stacki + Git»); зелёный первый push доказывает, что каркас воспроизводим на чистой машине (npm ci из lockfile).

Output: .github/workflows/ci.yml + удалённый репозиторий с успешным первым прогоном Actions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-osnova-proekta/01-RESEARCH.md
@.planning/phases/01-osnova-proekta/01-PATTERNS.md
@.planning/phases/01-osnova-proekta/01-CONTEXT.md
@.planning/phases/01-osnova-proekta/01-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .github/workflows/ci.yml (lint → check → build)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - 01-RESEARCH.md (CI workflow: строки 454-470; Pitfall 9 — node-версии; Pitfall 10 — lockfile)
    - 01-PATTERNS.md (секция .github/workflows/ci.yml, строки 120-140)
    - 01-CONTEXT.md (D-11: один workflow, lint до build)
  </read_first>
  <action>
    Создать .github/workflows/ci.yml по образцу RESEARCH.md (строки 454-470): name "CI"; on: [push, pull_request]; одна job "lint-and-build" на ubuntu-latest: шаг checkout (actions/checkout@v4), шаг setup-node (actions/setup-node@v4) с node-version: 22 и cache: npm, затем три run-шага строго в порядке: npm ci, npm run lint, npm run check, npm run build (линт ДО check/build — требование R6 ordering; check перед build — astro check генерирует типы, Pitfall 6).
    Локальный dry-run полного CI-пайплайна: npm ci (чистая установка из lockfile), npm run lint, npm run check, npm run build — все exit 0. Если npm ci падает на peer-deps — проверить соответствие мажоров интеграций решению 01-01 T1 (Pitfall 1) и исправить package.json до первого пуша.
  </action>
  <verify>
    <automated>npm ci && npm run lint && npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml существует; содержит on: [push, pull_request]; job со шагами checkout, setup-node (node-version 22, cache npm), npm ci, npm run lint, npm run check, npm run build — в этом порядке
    - npm ci && npm run lint && npm run check && npm run build — все exit 0 локально
  </acceptance_criteria>
  <done>ci.yml существует с порядком lint → check → build; локальный dry-run полного CI-пайплайна (npm ci + lint + check + build) зелёный</done>
</task>

<task type="auto">
  <name>Task 2: создание GitHub-remote + первый push + зелёный Actions</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - 01-CONTEXT.md (Specifics: публичный/приватный не зафиксирован — решено в 01-01 T3)
    - 01-RESEARCH.md (Environment Availability: gh CLI 2.95.0, авторизован Ivan-Shivarshinov, scopes repo+workflow)
  </read_first>
  <action>
    Перед push: убедиться, что рабочее дерево чисто (git status: нет незакоммиченных изменений, включая удалённую spike-страницу из 01-06 и фикстуры из 01-04), текущая ветка — main. Все файлы фазы закоммичены с осмысленными сообщениями.
    Создать репозиторий по решению 01-01 T3: gh repo create <имя> --<visibility> --source . --push --description "Портфолио — Иван Шиваршинов: веб-разработчик с продуктовым подходом" (visibility: --public или --private по решению; имя по решению, например portfolio-ivan-shivarshinov).
    Проверить remote: git remote -v показывает origin (push/fetch).
    Дождаться результата первого workflow: gh run list --workflow=CI (или gh run watch) до завершения; убедиться, что статус run = success (lint → check → build зелёные на первом push, пустая история — empty R6).
    Если первый run упал — починить источник (конфиг, версии, порядок), закоммитить фикс и запушить; повторять до зелёного.
  </action>
  <verify>
    <automated>git remote -v | grep -q origin && gh run list --workflow=CI --limit 1 --json status,conclusion | grep -q '"conclusion":"success"'</automated>
  </verify>
  <acceptance_criteria>
    - git remote -v содержит origin с URL созданного репозитория
    - gh run list показывает последний прогон CI со status completed и conclusion success
    - Первый push выполнен из локальной ветки main с полной историей фазы (включая коммит docs/stacki-coverage.md из 01-06)
    - git status чист после push
  </acceptance_criteria>
  <done>GitHub-remote создан по решению 01-01 T3; первый push выполнен; первый прогон Actions завершён со status success (lint → check → build зелёные)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions (внешний сервис) → репозиторий | Workflow исполняет npm ci/lint/check/build на ubuntu-latest |
| локальная машина → GitHub | Первый push публикует историю репозитория (включая docs/) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm ci в CI (зависимости) | high | mitigate | Установка строго из закоммиченного package-lock.json (npm ci) — детерминированный граф; версии зафиксированы по аудиту RESEARCH.md (Package Legitimacy Audit, все Approved); мажоры интеграций согласованы с astro (Pitfall 1) |
| T-01-07 | Information Disclosure | Публикация истории репозитория (docs/, конфиги) | medium | mitigate | Видимость репозитория — явное решение пользователя (01-01 T3); в фазе 1 нет секретов и токенов в файлах; будущие секреты — только через GitHub secrets |
| T-01-07 | Tampering | workflow-файл (вредоносные шаги) | low | accept | Автор — только разработчик проекта; actions с фиксированных репозиториев (actions/checkout@v4, actions/setup-node@v4); секретов в workflow нет |
</threat_model>

<verification>
- [ ] Локальный dry-run: npm ci && npm run lint && npm run check && npm run build — exit 0
- [ ] git remote -v показывает origin
- [ ] Первый прогон GitHub Actions: status completed, conclusion success
- [ ] Порядок шагов в ci.yml: lint до check/build (grep по файлу)
</verification>

<success_criteria>
- [ ] GitHub-remote создан (имя/видимость по решению пользователя)
- [ ] CI зелёный на первом push: lint → check → build (R6 полностью)
- [ ] engines.node >= 22.22.3 зафиксированы (создано в 01-03, подтверждено CI на Node 22)
- [ ] Документ покрытия Stacki (01-06) в истории репозитория — spike до визуальной сборки (P3)
</success_criteria>

<output>
Create `.planning/phases/01-osnova-proekta/01-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `.github/workflows/ci.yml` — workflow "CI": on [push, pull_request]; job lint-and-build (ubuntu-latest): actions/checkout@v4 → actions/setup-node@v4 (node-version 22, cache npm) → `npm ci` → `npm run lint` → `npm run check` → `npm run build`
- GitHub-репозиторий — имя и видимость по решению 01-01 T3; remote origin (push/fetch)
