---
phase: 01-osnova-proekta
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/pages/work.astro
  - src/pages/lab.astro
  - src/pages/about.astro
  - src/pages/contact.astro
  - public/robots.txt
autonomous: true
requirements: [R1, R4]
must_haves:
  truths:
    - "У всех 5 страниц уникальные пары title/description: scripts/check-seo.mjs находит ровно 5 уникальных пар в dist/*.html (SPEC adjacency R1/R4)"
    - "Каждая страница содержит canonical и полный набор OG-тегов (og:title, og:description, og:type, og:url, og:locale): scripts/check-seo.mjs exit 0 (SPEC R4)"
    - "sitemap-index.xml генерируется при сборке и содержит все 5 маршрутов: scripts/check-seo.mjs exit 0 (SPEC R4; техническое уточнение: проверка по sitemap-index.xml — фактический вывод @astrojs/sitemap 3.x, Pitfall 3)"
    - "Отсутствие обязательных SEO-полей у страницы (title/description props) → astro check падает: типизированные Props в BaseLayout/Seo.astro при TS strict (SPEC empty R4)"
    - "Все 5 маршрутов отдают HTTP 200 с валидным HTML: scripts/verify-preview.mjs exit 0 (SPEC R1 + UI-SPEC populated static-content)"
    - "Страницы-заглушки Work/Lab/About/Contact рендерят документированный placeholder-текст «Раздел в разработке — кейсы появятся в фазе 3» (UI-SPEC empty static-content)"
  artifacts:
    - src/pages/work.astro
    - src/pages/lab.astro
    - src/pages/about.astro
    - src/pages/contact.astro
    - public/robots.txt
  key_links:
    - "Страницы → BaseLayout → Seo.astro: title/description передаются через props (R4 unique pairs)"
    - "robots.txt ссылается на /sitemap-index.xml (фактический вывод sitemap-интеграции, Pitfall 3)"
    - "sitemap ← site в astro.config.mjs (Pitfall 4)"
---

<objective>
Создать 4 страницы-заглушки (Work, Lab, About, Contact) на общем BaseLayout с уникальными парами title/description и документированным placeholder-текстом; добавить public/robots.txt со ссылкой на sitemap-index.xml; прогнать полные SEO- и preview-проверки (scripts/check-seo.mjs, scripts/verify-preview.mjs) против собранного сайта. Закрывает R1 (5 маршрутов HTTP 200) и R4 (SEO: уникальные пары, canonical, OG, sitemap).

Purpose: страницы — потребители каркаса из 01-03; их композиция (layout + плоский список компонентов с props) задаёт Stacki-совместимый паттерн для фаз 2-3 (D-04). SEO-контракт проверяется программно по собранному HTML, а не глазами.

Output: 5 работающих маршрутов с полным SEO-хедом; зелёные scripts/check-seo.mjs и scripts/verify-preview.mjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-osnova-proekta/01-RESEARCH.md
@.planning/phases/01-osnova-proekta/01-PATTERNS.md
@.planning/phases/01-osnova-proekta/01-UI-SPEC.md
@.planning/phases/01-osnova-proekta/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 страницы-заглушки + public/robots.txt</name>
  <files>src/pages/work.astro, src/pages/lab.astro, src/pages/about.astro, src/pages/contact.astro, public/robots.txt</files>
  <read_first>
    - 01-RESEARCH.md (Pattern 2 — строки 262-273; Pitfall 3 — строки 340-344)
    - 01-PATTERNS.md (секция src/pages/*.astro, строки 276-289)
    - 01-UI-SPEC.md (Copywriting Contract — точные тексты)
  </read_first>
  <action>
    Создать 4 страницы по образцу Pattern 2 (строки 265-273): импорт BaseLayout из '../layouts/BaseLayout.astro', тело — только BaseLayout с props и плоским контентом (заглушка: один <p> с placeholder-текстом; без произвольного HTML, выражений и вложенных children на уровне страницы — D-04). Точные пары title/description (уникальные, русский язык, D-08):
    - work.astro: title «Работы — Иван Шиваршинов», description «Кейсы веб-разработки: от простых сайтов до контентных систем и веб-инструментов.»; placeholder «Раздел в разработке — кейсы появятся в фазе 3»
    - lab.astro: title «Лаборатория — Иван Шиваршинов», description «Эксперименты, веб-инструменты и мост к продуктам.»; тот же placeholder
    - about.astro: title «Обо мне — Иван Шиваршинов», description «Веб-разработчик с продуктовым подходом.»; тот же placeholder
    - contact.astro: title «Контакты — Иван Шиваршинов», description «Связаться со мной для проекта или консультации.»; тот же placeholder
    Placeholder-текст — дословно из UI-SPEC Copywriting Contract: «Раздел в разработке — кейсы появятся в фазе 3».
    Создать public/robots.txt: три строки "User-agent: *", "Allow: /", "Sitemap: <значение site из решения 01-01 T2>/sitemap-index.xml" — именно sitemap-index.xml (Pitfall 3: файла sitemap.xml не существует).
    Никакого клиентского JS на страницах (UI-SPEC Interaction NONE).
  </action>
  <verify>
    <automated>npm run build && node scripts/check-seo.mjs && node scripts/verify-preview.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Каждый из work/lab/about/contact.astro содержит import BaseLayout и использует его с title/description props
    - Пары title/description четырёх страниц различны между собой и отличны от пары index (из 01-03)
    - Все 4 страницы содержат строку «Раздел в разработке — кейсы появятся в фазе 3»
    - public/robots.txt содержит "User-agent: *", "Allow: /", "Sitemap: …/sitemap-index.xml"
    - npm run build exit 0; node scripts/check-seo.mjs exit 0 (5 уникальных пар, canonical+OG, sitemap-index.xml 5 url); node scripts/verify-preview.mjs exit 0 (HTTP 200 на 5 маршрутах)
  </acceptance_criteria>
  <done>4 страницы-заглушки с уникальными парами title/description и placeholder-текстом UI-SPEC созданы; robots.txt ссылается на /sitemap-index.xml; check-seo.mjs и verify-preview.mjs зелёные</done>
</task>

<task type="auto">
  <name>Task 2: SEO- и preview-проверки зелёные + устранение нарушений</name>
  <files>src/pages/work.astro, src/pages/lab.astro, src/pages/about.astro, src/pages/contact.astro, scripts/check-seo.mjs, scripts/verify-preview.mjs</files>
  <read_first>
    - 01-RESEARCH.md (Pitfall 3: sitemap-index.xml; строки 340-344)
    - 01-02-PLAN.md (контракты check-seo.mjs и verify-preview.mjs)
  </read_first>
  <action>
    Прогнать полный набор проверок R4/R1: npm run build, затем node scripts/check-seo.mjs (5 уникальных пар title/description; canonical + og:title/og:description/og:type/og:url/og:locale на каждой странице; 5 url в dist/sitemap-index.xml) и node scripts/verify-preview.mjs (HTTP 200 + text/html на /, /work, /lab, /about, /contact).
    Если любая проверка падает — починить источник, а не проверку: пары title/description правятся в страницах (Task 1 и index.astro), отсутствующие OG-теги — в Seo.astro (01-03), sitemap-проблемы — в astro.config.mjs (site задан). Изменения скриптов допустимы только для исправления ложных срабатываний (например, фильтрация dist/_spike-*.html, если spike-страница временно в сборке), и каждое изменение фиксируется в SUMMARY.
    Дополнительно проверить отсутствие client:* директив в собранном HTML (UI-SPEC Interaction NONE): grep -r "client:" dist/ не должен находить совпадений.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-seo.mjs && node scripts/verify-preview.mjs && (grep -r "client:" dist/ ; test $? -eq 1)</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/check-seo.mjs exit 0: ровно 5 уникальных пар, все SEO-теги на всех страницах, sitemap-index.xml с 5 url
    - node scripts/verify-preview.mjs exit 0: 5 маршрутов HTTP 200
    - В dist/*.html отсутствуют client:* директивы (grep exit 1)
    - Любое изменение страниц/скриптов в ходе починки задокументировано в SUMMARY
  </acceptance_criteria>
  <done>Полный SEO- и preview-набор зелёный: 5 уникальных пар, canonical + OG везде, sitemap-index.xml с 5 url, HTTP 200 на 5 маршрутах, в dist/ нет client:* директив</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| контент страниц → собранный HTML | title/description/контент страниц рендерятся в dist/*.html |
| dist/ → проверки | check-seo.mjs и verify-preview.mjs парсят собранные артефакты |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing | SEO-теги (canonical, OG) | low | mitigate | canonical строится из Astro.site (задан в конфиге) + Astro.url.pathname; экранирование Astro в атрибутах; проверки check-seo.mjs фиксируют корректность при сборке |
| T-01-05 | Information Disclosure | robots.txt (маршруты) | low | accept | Статичный сайт; публичные маршруты — суть портфолио; скрытых разделов нет |
| T-01-05 | Tampering | Прогон preview (локальный порт 4321) | low | accept | Локальный процесс на время проверки; завершается в finally скрипта verify-preview.mjs |
</threat_model>

<verification>
- [ ] npm run build exit 0 (все 5 страниц)
- [ ] node scripts/check-seo.mjs exit 0 — 5 уникальных пар, canonical + OG, sitemap-index.xml 5 url
- [ ] node scripts/verify-preview.mjs exit 0 — HTTP 200 на / /work /lab /about /contact
- [ ] В собранном HTML нет client:* директив
</verification>

<success_criteria>
- [ ] 5 маршрутов отдают валидный HTML (R1 полностью)
- [ ] SEO-контракт (R4) подтверждён программно: уникальные пары, canonical, OG, sitemap
- [ ] robots.txt корректен (sitemap-index.xml — техническое уточнение из решения 01-01 T4)
- [ ] Страницы Stacki-совместимы: layout + плоские компоненты с props, без клиентского JS
</success_criteria>

<output>
Create `.planning/phases/01-osnova-proekta/01-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/pages/work.astro` — маршрут /work; title «Работы — Иван Шиваршинов»
- `src/pages/lab.astro` — маршрут /lab; title «Лаборатория — Иван Шиваршинов»
- `src/pages/about.astro` — маршрут /about; title «Обо мне — Иван Шиваршинов»
- `src/pages/contact.astro` — маршрут /contact; title «Контакты — Иван Шиваршинов»
- `public/robots.txt` — User-agent: * / Allow: / / Sitemap: <site>/sitemap-index.xml
