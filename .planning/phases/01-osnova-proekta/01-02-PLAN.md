---
phase: 01-osnova-proekta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-preview.mjs
  - scripts/check-tokens.mjs
  - scripts/check-seo.mjs
  - scripts/check-collections.mjs
  - scripts/check-spike-doc.mjs
autonomous: true
requirements: [R1, R2, R3, R4, R5]
must_haves:
  truths:
    - "Пять валидационных скриптов существуют и проходят синтаксическую проверку и внутренние self-test (node --check + --self-test exit 0)"
    - "verify-preview.mjs проверяет HTTP 200 на маршрутах /, /work, /lab, /about, /contact против preview-сервера (R1)"
    - "check-tokens.mjs проверяет: ровно один файл токенов (src/styles/tokens.css), 5 групп переменных, отсутствие хардкод-hex вне tokens.css (R2)"
    - "check-seo.mjs проверяет: 5 уникальных пар title/description, canonical + OG на каждой странице, 5 url в dist/sitemap-index.xml (R4, Pitfall 3)"
    - "check-collections.mjs проверяет негативные фикстуры: дубликат slug, дубликат id, отсутствие обязательного поля → сборка падает; фикстуры временные и удаляются (R3)"
    - "check-spike-doc.mjs проверяет docs/stacki-coverage.md: 8 конструкций + 2 краевых случая с вердиктами «проверено/ограничение» (R5)"
  artifacts:
    - scripts/verify-preview.mjs
    - scripts/check-tokens.mjs
    - scripts/check-seo.mjs
    - scripts/check-collections.mjs
    - scripts/check-spike-doc.mjs
  key_links:
    - "Скрипты вызываются npm-скриптом verify (wiring в 01-03) и используются как <automated>-проверки планов 01-04/01-05/01-06"
---

<objective>
Создать валидационный каркас фазы — 5 Node-скриптов из Wave 0 Gaps (01-VALIDATION.md строки 63-70), которые проверяют требования R1-R5 на уровне сборки/артефактов. Скрипты создаются ДО кода, который они проверяют (Nyquist: verify-команды последующих планов ссылаются на них).

Purpose: фаза не содержит юнит-логики; валидация — на уровне сборки и собранных артефактов (dist/*.html, sitemap-index.xml, docs/stacki-coverage.md). Скрипты — plain Node.js ESM без фреймворков, запускаются через npm-скрипты.

Покрытие Wave 0 gaps: скрипты 1-5 (verify-preview, check-tokens, check-collections, check-seo, check-spike-doc) создаются здесь; gap 6 (package.json scripts + engines) покрывается планом 01-03 (tracer), который создаёт package.json с полным набором скриптов dev/build/preview/lint/check/verify.

Output: 5 скриптов в scripts/, каждый с режимом --self-test для проверки логики без собранного проекта.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-osnova-proekta/01-VALIDATION.md
@.planning/phases/01-osnova-proekta/01-RESEARCH.md
@.planning/phases/01-osnova-proekta/01-PATTERNS.md
@.planning/phases/01-osnova-proekta/01-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-preview.mjs + scripts/check-tokens.mjs</name>
  <files>scripts/verify-preview.mjs, scripts/check-tokens.mjs</files>
  <read_first>
    - 01-VALIDATION.md (Per-Task Verification Map, rows R1/R2)
    - 01-RESEARCH.md (Validation Architecture, строки 566-605)
    - 01-PATTERNS.md (раздел scripts/*.mjs, строки 300-310)
  </read_first>
  <action>
    Создать scripts/verify-preview.mjs: ESM-скрипт (node --test-совместимый, без фреймворков). Логика: спавнит `npx astro preview --port 4321` (порт из флага --port, default 4321), ждёт готовности (polling GET / до 200, таймаут 60s), затем GET по маршрутам /, /work, /lab, /about, /contact; для каждого маршрута проверяет HTTP-статус 200 и content-type text/html; печатает таблицу маршрут-статус; exit 1 при любом отличии или недоступности сервера; завершает preview-процесс в finally. Флаг --self-test: прогоняет логику построения списка маршрутов и сравнения статусов на встроенных фикстурах без сети (фикстура good: 5 маршрутов 200 → pass; фикстура bad: один маршрут 404 → логика фиксирует fail), exit 0 при корректной работе проверок.
    Создать scripts/check-tokens.mjs: (1) единый файл токенов: ищет файлы с CSS-переменными (паттерн --[a-z-]+:) в src/styles и src/components; допускается ровно один файл — src/styles/tokens.css; любые другие файлы с переменными или несколько файлов токенов → exit 1 с перечислением нарушителей; (2) 5 групп: в tokens.css должны присутствовать префиксы --color-*, --font-* или --text-*, --space-*, --container-*, --motion-* (каждая группа — не менее 1 переменной); (3) запрет хардкода: в src/styles/** и src/components/** вне tokens.css hex-литералы цветов (#RGB/#RRGGBB/#RRGGBBAA) → нарушение; px-значения, совпадающие с величинами шкалы отступов (4, 8, 16, 24, 32, 48, 64 px) → нарушение; НЕ нарушение: 1-2px (outline/focus, вне шкалы), сам tokens.css. Выводит список нарушений; exit 1 при любом нарушении. Флаг --self-test: встроенные фикстуры (good: 1 файл с 5 группами, нет hex вне; bad: hex в component-файле; bad: второй файл с переменными) — логика должна классифицировать их верно, exit 0.
  </action>
  <verify>
    <automated>node --check scripts/verify-preview.mjs && node --check scripts/check-tokens.mjs && node scripts/verify-preview.mjs --self-test && node scripts/check-tokens.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-preview.mjs существует и содержит массив маршрутов из 5 элементов: /, /work, /lab, /about, /contact
    - scripts/verify-preview.mjs содержит обработку флага --self-test
    - scripts/check-tokens.mjs содержит: whitelist-путь src/styles/tokens.css, проверку 5 групп (--color-, --font-/--text-, --space-, --container-, --motion-), hex-детектор
    - node --check и --self-test обоих скриптов завершаются кодом 0
  </acceptance_criteria>
  <done>verify-preview.mjs и check-tokens.mjs существуют, проходят node --check и --self-test с кодом 0; контракты R1/R2 реализованы в логике скриптов</done>
</task>

<task type="auto">
  <name>Task 2: scripts/check-seo.mjs + scripts/check-collections.mjs + scripts/check-spike-doc.mjs</name>
  <files>scripts/check-seo.mjs, scripts/check-collections.mjs, scripts/check-spike-doc.mjs</files>
  <read_first>
    - 01-VALIDATION.md (Per-Task Verification Map, rows R3/R4/R5)
    - 01-RESEARCH.md (Pitfall 3 sitemap-index.xml, Pitfall 8 дубликат slug, строки 340-344 и 366-370)
    - 01-SPEC.md (Edge Coverage: adjacency/empty R3, adjacency/empty R4, R5; Prohibitions P1)
  </read_first>
  <action>
    Создать scripts/check-seo.mjs: парсит dist/*.html (все собранные страницы), извлекает title и meta name=description с каждой страницы; формирует Set пар (title, description); assert ровно 5 уникальных пар → иначе exit 1 с перечислением дублей. Для каждой страницы проверяет наличие: link rel="canonical", meta property="og:title", property="og:description", property="og:type", property="og:url", property="og:locale" — отсутствие любого → exit 1. Парсит dist/sitemap-index.xml (НЕ sitemap.xml — фактический вывод @astrojs/sitemap 3.x, Pitfall 3): 5 элементов url → иначе exit 1. Флаг --self-test: встроенные HTML-фикстуры (5 уникальных пар + все OG-теги → pass; дубль пары → детект; отсутствие og:url → детект; sitemap-фикстура с 5 url → pass).
    Создать scripts/check-collections.mjs: негативные тесты с временными фикстурами. (a) дубликат slug: создаёт в src/content/projects/ два временных .mdx (например, .check-dup-a.mdx и .check-dup-b.mdx) с одинаковым frontmatter-полем slug; запускает `npm run build`; ожидает exit != 0 (DuplicateContentEntrySlugError); в finally удаляет оба файла; если build завершился кодом 0 → exit 1. (b) отсутствие обязательного поля: создаёт временный .mdx в src/content/notes/ без поля title; ожидает exit != 0; удаляет в finally. (c) дубликат id в JSON: делает резервную копию src/data/services.json, добавляет запись с дублирующимся id, запускает build, ожидает exit != 0, восстанавливает файл в finally. После всех тестов проверяет, что рабочее дерево чисто от фикстур (не осталось .check-* файлов и services.json восстановлен). Флаг --self-test: логика создания/удаления фикстур на временной директории без запуска build.
    Создать scripts/check-spike-doc.mjs: проверяет существование docs/stacki-coverage.md; наличие всех 8 конструкций (компоненты, props, variants, scoped styles, CSS-переменные, JSON-коллекции, frontmatter, View Transitions) и 2 краевых случаев (scoped styles + переменная одновременно; коллекция с 0 записей) — по ключевым словам в тексте; каждая конструкция имеет вердикт «проверено» или «ограничение»; при отсутствии любого пункта или вердикта → exit 1 с перечислением пробелов. Флаг --self-test: встроенные markdown-фикстуры (полный документ → pass; документ без View Transitions → детект; пункт без вердикта → детект).
  </action>
  <verify>
    <automated>node --check scripts/check-seo.mjs && node --check scripts/check-collections.mjs && node --check scripts/check-spike-doc.mjs && node scripts/check-seo.mjs --self-test && node scripts/check-collections.mjs --self-test && node scripts/check-spike-doc.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - check-seo.mjs содержит: Set-проверку уникальных пар title/description, проверку 6 SEO-тегов (canonical, og:title, og:description, og:type, og:url, og:locale), путь dist/sitemap-index.xml
    - check-collections.mjs содержит: создание временного .mdx с дубликатом slug, ожидание ненулевого exit build, удаление фикстур в finally, восстановление services.json
    - check-spike-doc.mjs содержит список из 8 конструкций и 2 краевых случаев, проверку вердиктов «проверено»/«ограничение»
    - node --check и --self-test всех трёх скриптов завершаются кодом 0
  </acceptance_criteria>
  <done>check-seo.mjs, check-collections.mjs и check-spike-doc.mjs существуют, проходят node --check и --self-test с кодом 0; контракты R3/R4/R5 реализованы</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Скрипты выполняются локально на машине разработчика и в CI. Входные данные скриптов — собственные файлы проекта (dist/, src/, docs/), не пользовательский ввод.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | scripts/*.mjs (исполнение node) | low | mitigate | Скрипты не содержат postinstall/инъекций: чистые чтения файлов + spawn npm run build для негативных фикстур; фикстуры удаляются в finally и не коммитятся |
| T-01-02 | Information Disclosure | Вывод скриптов (пути, содержимое dist) | low | accept | Локальный вывод для разработчика и CI-логов; чувствительных данных в фазе 1 нет |
</threat_model>

<verification>
- [ ] `node --check` и `--self-test` всех 5 скриптов зелёные
- [ ] Скрипты не оставляют артефактов: git status не содержит временных фикстур после self-test
- [ ] Полный прогон каждого скрипта против реального проекта выполнен в планах-потребителях (01-04/01-05/01-06)
</verification>

<success_criteria>
- [ ] Все 5 скриптов существуют, синтаксически валидны, self-test зелёный
- [ ] Проверяемое поведение каждого скрипта соответствует строке Per-Task Verification Map в 01-VALIDATION.md (R1-R5)
- [ ] Wave 0 gaps 1-5 закрыты; gap 6 (package.json scripts) явно закреплён за планом 01-03
</success_criteria>

<output>
Create `.planning/phases/01-osnova-proekta/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `scripts/verify-preview.mjs` — CLI: `node scripts/verify-preview.mjs [--port N] [--self-test]`; exit 0/1; спавнит `astro preview`
- `scripts/check-tokens.mjs` — CLI: `node scripts/check-tokens.mjs [--self-test]`; exit 0/1; whitelist `src/styles/tokens.css`; группы `--color-*`, `--font-*`/`--text-*`, `--space-*`, `--container-*`, `--motion-*`
- `scripts/check-seo.mjs` — CLI: `node scripts/check-seo.mjs [--self-test]`; exit 0/1; читает `dist/*.html` + `dist/sitemap-index.xml`
- `scripts/check-collections.mjs` — CLI: `node scripts/check-collections.mjs [--self-test]`; exit 0/1; временные фикстуры `.check-*.mdx` + резервная копия `src/data/services.json`
- `scripts/check-spike-doc.mjs` — CLI: `node scripts/check-spike-doc.mjs [--self-test]`; exit 0/1; читает `docs/stacki-coverage.md`
