---
phase: 01-osnova-proekta
plan: 06
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - src/pages/_spike-vt.astro
  - docs/stacki-coverage.md
autonomous: false
requirements: [R5]
user_setup:
  - service: Stacki
    why: "Spike R5 — живой прогон desktop-приложения на скаффолде (D-13): установка и прохождение чек-листа конструкций"
    steps: "Скачать установщик v0.1.3 (NSIS, Windows) с https://github.com/flowtricks/stacki-releases/releases/tag/v0.1.3 и установить; при невозможности — fallback-путь из задачи 2"
must_haves:
  truths:
    - "docs/stacki-coverage.md закоммичен и содержит по одному пункту на каждую из 8 конструкций (компоненты, props, variants, scoped styles, CSS-переменные, JSON-коллекции, frontmatter, View Transitions) + 2 краевых случая, каждый с пометкой «проверено» или «ограничение»: scripts/check-spike-doc.mjs exit 0 (SPEC R5)"
    - "Зафиксировано наблюдаемое поведение компонента, использующего scoped style и CSS-переменную одновременно (SPEC adjacency R5)"
    - "Зафиксировано наблюдаемое поведение коллекции с 0 записей (notes) в Stacki (SPEC empty R5)"
    - "Перечислены обнаруженные ограничения Stacki (например, отсутствие визуальной работы с JSON/стилями — вне README, A1)"
    - "Временная страница _spike-vt.astro удалена после прогона; npm run build остаётся зелёным (A7)"
  artifacts:
    - src/pages/_spike-vt.astro (временный, удаляется)
    - docs/stacki-coverage.md
  key_links:
    - "Stacki (desktop) читает те же файлы src/ через astro dev — связь инструмент ↔ структура проекта (D-04)"
    - "docs/stacki-coverage.md ↔ git-история: коммит spike-документа предшествует визуальной сборке (Prohibition P3)"
prohibitions:
  - statement: "Визуальная сборка НЕ начинается до коммита spike-документа Stacki"
    status: resolved
    verification: "judgment — git log: коммит docs/stacki-coverage.md предшествует любым коммитам визуальной сборки (фаза 2); в фазе 1 визуальной сборки нет, документ коммитится здесь"
---

<objective>
Spike Stacki (SPEC R5): живой прогон desktop-приложения Stacki v0.1.3 на скаффолде — открытие проекта, чтение компонентов/props/variants, scoped styles, CSS-переменных, JSON-коллекций, frontmatter и View Transitions — с документом покрытия docs/stacki-coverage.md, закоммиченным до начала визуальной сборки (ADR Этап 0, Prohibition P3). Гибридный прогон (D-13): агент готовит чек-лист и временную spike-страницу, пользователь запускает Stacki и проходит сценарий, агент фиксирует вердикты.

Purpose: подтвердить совместимость рабочего цикла «Astro + AI-агент + Stacki + Git» ДО визуальной сборки. README Stacki не упоминает 6 из 8 конструкций — их поведение познаётся только эмпирически; результат — документ с вердиктами «проверено / ограничение» (D-14).

Output: docs/stacki-coverage.md (8 конструкций + 2 краевых случая с вердиктами и ограничениями), закоммиченный; временная spike-страница удалена.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-osnova-proekta/01-RESEARCH.md
@.planning/phases/01-osnova-proekta/01-CONTEXT.md
@.planning/phases/01-osnova-proekta/01-PATTERNS.md
@.planning/phases/01-osnova-proekta/01-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: подготовка spike-артефактов — временная страница View Transitions + чек-лист покрытия</name>
  <files>src/pages/_spike-vt.astro, docs/stacki-coverage.md</files>
  <read_first>
    - 01-RESEARCH.md (Code Example View Transitions: строки 487-499; D-14: 8 конструкций + 2 edge; A7)
    - 01-CONTEXT.md (D-13, D-14)
    - 01-UI-SPEC.md (Interaction NONE — spike-страница единственное исключение)
  </read_first>
  <action>
    Создать src/pages/_spike-vt.astro — ВРЕМЕННАЯ страница для проверки View Transitions в Stacki (A7, удаляется после прогона): импорт BaseLayout из '../layouts/BaseLayout.astro' и ClientRouter из 'astro:transitions'; тело: <ClientRouter /> + ссылка на /work с атрибутом transition:name="page". Никаких других client-скриптов.
    Создать docs/stacki-coverage.md — чек-лист покрытия (D-14): заголовок с датой и версией Stacki (v0.1.3, дистрибутив flowtricks/stacki-releases), таблица из 8 строк конструкций — «Компоненты», «Props», «Variants», «Scoped styles», «CSS-переменные», «JSON-коллекции», «Frontmatter», «View Transitions» — с колонками: Конструкция | Как проверить (путь в проекте) | Вердикт (проверено/ограничение) | Наблюдение. Колонки «Вердикт» и «Наблюдение» пустые — заполняются после прогона (Task 3). Плюс 2 краевых случая отдельными строками: (1) компонент со scoped style и CSS-переменной одновременно (проверяется на BaseLayout — навигация использует var(--color-accent) для активной ссылки, UI-SPEC резервирует accent под active nav); (2) коллекция с 0 записей (notes — 0 записей, src/content/notes). Раздел «Ограничения» пустой — заполняется после прогона.
    Убедиться, что сборка зелёная с spike-страницей: npm run build exit 0 (Rust-компилятор строг к HTML — страница должна быть валидной).
  </action>
  <verify>
    <automated>npm run build && test -f docs/stacki-coverage.md</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/_spike-vt.astro существует и содержит import ClientRouter из 'astro:transitions' и ссылку с transition:name="page"
    - docs/stacki-coverage.md содержит таблицу с 8 конструкциями и 2 краевыми случаями, колонки «Вердикт»/«Наблюдение» и раздел «Ограничения»
    - npm run build exit 0 (включая spike-страницу)
    - Заголовок docs/stacki-coverage.md содержит версию Stacki v0.1.3 и дату
  </acceptance_criteria>
  <done>Spike-артефакты готовы: _spike-vt.astro с ClientRouter собран (build exit 0), чек-лист docs/stacki-coverage.md покрывает 8 конструкций + 2 краевых случая</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: установка Stacki и живой прогон чек-листа (пользователь)</name>
  <action>
    Установить Stacki v0.1.3 (NSIS-установщик, Windows) с https://github.com/flowtricks/stacki-releases/releases/tag/v0.1.3, запустить astro dev в проекте, открыть проект в Stacki и пройти чек-лист из docs/stacki-coverage.md, фиксируя для каждой из 8 конструкций и 2 краевых случаев наблюдаемое поведение: работает визуально / доступно только через кодовый режим / недоступно (ограничение).
    Fallback-цепочка (порядок из RESEARCH.md Open Question 2): (1) установщик v0.1.3 из stacki-releases; (2) если установка невозможна — сборка из исходников: git clone https://github.com/flowtricks/stacki && npm install && npm run dist:win (выполняет агент по запросу); (3) если оба пути не работают — зафиксировать ограничение окружения в docs/stacki-coverage.md (D-13/D-14) и продолжить кодовую проверку конструкций; решение о продолжении — за пользователем (подтверждается в resume-signal).
  </action>
  <instructions>
    Уже автоматизировано агентом: проект собран и зелёный (npm run build), временная страница _spike-vt.astro для проверки View Transitions создана, чек-лист docs/stacki-coverage.md подготовлен (8 конструкций + 2 краевых случая с пустыми колонками «Вердикт»/«Наблюдение»).
    Единственное действие пользователя — установить Stacki (или запросить fallback-сборку из исходников) и пройти чек-лист в приложении: открытие проекта; чтение компонентов (BaseLayout.astro, Seo.astro) и их props; variants; scoped styles; CSS-переменные (tokens.css, var(--color-accent) в навигации); JSON-коллекции (services.json/skills.json/tools.json); frontmatter; View Transitions (_spike-vt.astro); краевой случай 1 — компонент со scoped style + переменной (активная ссылка навигации); краевой случай 2 — коллекция с 0 записей (notes). Записывать наблюдения по каждой строке чек-листа.
  </instructions>
  <verification>Агент после resume: проверяет, что для всех 8 конструкций и 2 краевых случаев получены вердикты («проверено»/«ограничение») и наблюдения; если прогон не состоялся — что зафиксирована причина по fallback-пути 3 с решением пользователя о продолжении.</verification>
  <resume-signal>Прогон завершён. Вердикты: <для каждой из 8 конструкций и 2 краевых случаев: проверено / ограничение + наблюдение>; ограничения: <список></resume-signal>
</task>

<task type="auto">
  <name>Task 3: финализация docs/stacki-coverage.md + удаление spike-страницы + commit</name>
  <files>docs/stacki-coverage.md, src/pages/_spike-vt.astro</files>
  <read_first>
    - 01-CONTEXT.md (D-14: структура документа)
    - 01-SPEC.md (AC R5: вердикты «проверено/не проверено/ограничение», наблюдаемое поведение, перечисленные ограничения)
  </read_first>
  <action>
    Сначала — контроль изменений после живого прогона (T-01-06): git status + git diff --stat по рабочему дереву; любые изменения в src/, внесённые Stacki во время прогона, явно принять или откатить (git checkout -- <путь>) с фиксацией решения по каждому изменённому файлу в SUMMARY. Затем заполнить docs/stacki-coverage.md по наблюдениям пользователя из Task 2: для каждой из 8 конструкций и 2 краевых случаев — вердикт («проверено» или «ограничение») и наблюдение (что реально увидели); в раздел «Ограничения» — все обнаруженные ограничения Stacki (например, отсутствие визуальной работы с JSON-коллекциями/стилями, если README-описания не подтвердились — A1). Если прогон не состоялся (fallback-путь 3) — пометить каждую конструкцию «ограничение» с формулировкой «прогон не выполнен: <причина>» и зафиксировать это как ограничение окружения.
    Удалить src/pages/_spike-vt.astro (временная страница — A7, UI-SPEC Interaction NONE).
    Прогнать: npm run build exit 0 (сборка без spike-страницы), node scripts/check-spike-doc.mjs exit 0 (8 конструкций + 2 edge + вердикты), node scripts/check-seo.mjs exit 0 (5 страниц — spike-страница не должна попасть в проверку пар).
    Закоммитить docs/stacki-coverage.md отдельным понятным коммитом (Prohibition P3: spike-документ закоммичен до визуальной сборки; коммит удаления _spike-vt.astro — отдельным коммитом).
  </action>
  <verify>
    <automated>npm run build && node scripts/check-spike-doc.mjs && node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - docs/stacki-coverage.md содержит вердикт и наблюдение для всех 8 конструкций и 2 краевых случаев (ни одной пустой ячейки «Вердикт»)
    - Раздел «Ограничения» заполнен (перечислены обнаруженные ограничения; если их нет — явная запись «ограничений не обнаружено»)
    - src/pages/_spike-vt.astro удалён из рабочего дерева
    - npm run build exit 0; node scripts/check-spike-doc.mjs exit 0; node scripts/check-seo.mjs exit 0
    - git log содержит коммит docs/stacki-coverage.md (spike-документ в Git — AC R5)
    - Проверен git status/diff после прогона: изменения Stacki приняты или откачены, решение по каждому файлу зафиксировано в SUMMARY (T-01-06)
  </acceptance_criteria>
  <done>docs/stacki-coverage.md заполнен вердиктами и ограничениями и закоммичен; _spike-vt.astro удалён; build, check-spike-doc и check-seo зелёные</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub (stacki-releases) → машина пользователя | Скачивание и установка исполняемого файла (NSIS-установщик v0.1.3) |
| Stacki (desktop) → файлы проекта | Приложение читает/пишет файлы src/ проекта |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | Установщик Stacki v0.1.3 (NSIS) | medium | mitigate | Источник — официальный дистрибутивный репозиторий flowtricks/stacki-releases (README главного репозитория указывает на него как единственный канал); релиз подписан GPG; альтернатива — сборка из исходников flowtricks/stacki (MIT, аудируемый код) |
| T-01-06 | Tampering | Запись Stacki в файлы проекта | medium | mitigate | Принцип D-04: Astro-исходники — источник истины; Stacki не перезаписывает деструктивно (README: code fallback, «ничего не перезаписывается»); после прогона проверяется git status/diff — изменения либо принимаются, либо откатываются; spike-страница удаляется |
| T-01-06 | Information Disclosure | docs/stacki-coverage.md (ограничения инструмента) | low | accept | Внутренний инженерный документ; чувствительных данных не содержит |
</threat_model>

<verification>
- [ ] node scripts/check-spike-doc.mjs exit 0 — 8 конструкций + 2 edge с вердиктами
- [ ] npm run build exit 0 (после удаления _spike-vt.astro)
- [ ] node scripts/check-seo.mjs exit 0 — spike-страница не исказила проверку 5 пар
- [ ] git log содержит коммит docs/stacki-coverage.md
- [ ] git status/diff проверен после прогона Stacki — изменения приняты или откачены (T-01-06); дерево чисто после удаления spike-страницы
</verification>

<success_criteria>
- [ ] docs/stacki-coverage.md закоммичен: 8 конструкций + 2 краевых случая, вердикты «проверено/ограничение», ограничения перечислены (R5)
- [ ] Живой прогон выполнен пользователем (или зафиксировано ограничение окружения по fallback-пути с решением пользователя)
- [ ] Временная spike-страница удалена, сборка зелёная
- [ ] Коммит spike-документа предшествует любой визуальной сборке (P3)
</success_criteria>

<output>
Create `.planning/phases/01-osnova-proekta/01-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/pages/_spike-vt.astro` (временный) — `<ClientRouter />` из `astro:transitions`, ссылка с `transition:name="page"`; удаляется в Task 3
- `docs/stacki-coverage.md` — документ покрытия: таблица 8 конструкций (Компоненты, Props, Variants, Scoped styles, CSS-переменные, JSON-коллекции, Frontmatter, View Transitions) + 2 краевых случая (scoped style + переменная; коллекция с 0 записей), колонки Конструкция/Как проверить/Вердикт/Наблюдение, раздел «Ограничения»
