---
phase: 01-osnova-proekta
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/content.config.ts
  - src/data/services.json
  - src/data/skills.json
  - src/data/tools.json
  - src/content/projects/.gitkeep
  - src/content/notes/.gitkeep
autonomous: true
requirements: [R3]
must_haves:
  truths:
    - "Сборка проходит при пустых коллекциях: projects/ и notes/ без записей, npm run build exit 0 (SPEC empty R1/R3)"
    - "Дубликат slug/id в коллекции → сборка падает: scripts/check-collections.mjs exit 0, фикстуры временные и удаляются (SPEC adjacency R3)"
    - "Отсутствие обязательного поля в записи → сборка падает (zod required): scripts/check-collections.mjs exit 0 (SPEC empty R3)"
    - "npm run check (astro check) exit 0 при TS strict с полными и пустыми коллекциями"
    - "Схемы коллекций содержат задел на EN-версии: необязательные поля локали (titleEn и аналоги) без обязательной семантики (D-08)"
    - { statement: "Детерминированный порядок записей при равных датах обеспечен полем order (z.number().int().default(0)) в схеме projects (D-09) — held-out edge test фазы", verification: backstop }
  artifacts:
    - src/content.config.ts
    - src/data/services.json
    - src/data/skills.json
    - src/data/tools.json
    - src/content/projects/.gitkeep
    - src/content/notes/.gitkeep
  key_links:
    - "file()-loader ↔ src/data/*.json: валидация JSON при сборке, типы из схемы (D-02, RESEARCH Pattern 1)"
    - "glob()-loader ↔ src/content/projects|notes: валидация frontmatter MDX/MD при сборке"
    - "zod-схемы — единственный источник типов коллекций (.astro/types.d.ts генерируется сборкой)"
---

<objective>
Создать контентный слой фазы: src/content.config.ts с 5 коллекциями (projects, notes, services, skills, tools), JSON-данные в src/data/, пустые директории коллекций. Схемы валидируются при сборке (content layer, Zod 4 через astro/zod), падение на дубликатах и отсутствии обязательных полей, проход на пустых коллекциях — закрывает R3 целиком.

Purpose: контентный слой — единый источник типов и валидации для всех последующих фаз (кейсы в фазе 3, реальные данные в фазе 5). Схема projects закладывает полную структуру кейсов (D-05) и EN-задел (D-08) без миграций схем в будущем.

Output: валидирующийся контентный слой; негативные фикстуры (дубликат slug/id, отсутствие поля) проверены автоматически.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-osnova-proekta/01-RESEARCH.md
@.planning/phases/01-osnova-proekta/01-PATTERNS.md
@.planning/phases/01-osnova-proekta/01-CONTEXT.md
@.planning/phases/01-osnova-proekta/01-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/content.config.ts + JSON-данные + пустые коллекции</name>
  <files>
    src/content.config.ts, src/data/services.json, src/data/skills.json, src/data/tools.json,
    src/content/projects/.gitkeep, src/content/notes/.gitkeep
  </files>
  <read_first>
    - 01-RESEARCH.md (Pattern 1: строки 227-258; Pitfall 8; строки 229-260 — точный код content.config.ts)
    - 01-PATTERNS.md (секция src/content.config.ts, строки 142-173)
    - 01-CONTEXT.md (D-02, D-05, D-07, D-08, D-09)
  </read_first>
  <action>
    Создать src/content.config.ts по образцу RESEARCH.md Pattern 1 (новый API content layer: импорты defineCollection из 'astro:content', glob и file из 'astro/loaders', z из 'astro/zod'):
    - projects: loader glob({ base: './src/content/projects', pattern: '**/*.{md,mdx}' }); schema z.object: slug z.string(), title z.string(), summary z.string(), role z.string(), stack z.array(z.string()), year z.number().int(), status z.enum(['active', 'archived']), 'client-type' z.string(), order z.number().int().default(0), titleEn z.string().optional() (EN-задел D-08; четыре вопроса кейса — в MDX-теле, НЕ в frontmatter — D-05)
    - notes: loader glob({ base: './src/content/notes', pattern: '**/*.md' }); schema z.object: title z.string(), date z.coerce.date(), summary z.string().optional() — 0 записей намеренно (D-07, edge «пустые коллекции»)
    - services: loader file('./src/data/services.json'); schema z.object: id z.string(), title z.string(), description z.string()
    - skills: loader file('./src/data/skills.json'); schema z.object: id z.string(), title z.string(), description z.string() (допустимо дополнить полем категории по discretion — все поля в zod-схеме)
    - tools: loader file('./src/data/tools.json'); schema z.object: id z.string(), title z.string(), description z.string() (допустимо дополнить)
    - export const collections = { projects, notes, services, skills, tools }
    Создать src/data/services.json, skills.json, tools.json: массивы записей с уникальными id (строки), title и description на русском; 2-3 записи на файл (реальный контент — фаза 3, здесь минимальные валидные данные). Все поля записей обязаны соответствовать zod-схемам.
    Создать src/content/projects/.gitkeep и src/content/notes/.gitkeep — git не отслеживает пустые директории; glob() на пустой директории валиден и вернёт [] (Pitfall 6; страницы фазы 1 коллекции не рендерят).
    Верификация: npm run build (astro check + build exit 0 — валидация схем при сборке); npm run check exit 0; затем node scripts/check-collections.mjs (негативные фикстуры: дубликат slug, отсутствие обязательного поля, дубликат id — каждая ожидает падение сборки; фикстуры удаляются в finally); после прогона git status не содержит .check-* файлов.
  </action>
  <verify>
    <automated>npm run check && npm run build && node scripts/check-collections.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/content.config.ts содержит 5 коллекций: projects (glob MDX), notes (glob MD), services/skills/tools (file JSON)
    - Схема projects содержит все поля D-05: slug, title, summary, role, stack, year, status, client-type, order (default 0), titleEn (optional)
    - Каждый из services.json/skills.json/tools.json — массив записей с уникальным id и полями из zod-схемы
    - src/content/projects/ и src/content/notes/ существуют (через .gitkeep) и не содержат записей
    - npm run check exit 0; npm run build exit 0; node scripts/check-collections.mjs exit 0
    - После прогона git status не содержит временных фикстур (.check-* файлов, изменённый services.json)
  </acceptance_criteria>
  <done>Контентный слой работает: 5 коллекций валидируются при сборке, пустые коллекции проходят, дубликат slug/id и отсутствие обязательного поля падают сборку (доказано check-collections.mjs), git status чист</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| файлы контента → сборка | frontmatter MDX/MD и JSON-данные валидируются zod-схемами при сборке |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Контент коллекций (MDX frontmatter, JSON) | low | mitigate | zod-валидация при сборке (V5 Input Validation); невалидная запись падает сборку — до публикации не дойдёт; Astro экранирует вывод в шаблонах |
| T-01-04 | Tampering | Негативные фикстуры check-collections.mjs | low | mitigate | Фикстуры временные, создаются в finally-блоке и удаляются; git status чист после прогона (проверка в verify) |
| T-01-04 | Spoofing | SEO-поля в схемах (title/description) | low | accept | Собственный контент (не пользовательский ввод); экранирование Astro достаточно для ASVS 1 |
</threat_model>

<verification>
- [ ] npm run check exit 0 (TS strict + полные/пустые коллекции)
- [ ] npm run build exit 0 (валидация схем при сборке)
- [ ] node scripts/check-collections.mjs exit 0 — негативные фикстуры падают ожидаемо и удаляются
- [ ] git status не содержит артефактов фикстур
</verification>

<success_criteria>
- [ ] Все 5 коллекций определены и валидируются при сборке
- [ ] Сборка падает на дубликате slug/id и на отсутствии обязательного поля (доказано негативными фикстурами)
- [ ] Сборка проходит на пустых коллекциях (projects/notes без записей)
- [ ] Поле order в схеме projects зафиксировано (D-09) — backstop-маркер для фазы 3
</success_criteria>

<output>
Create `.planning/phases/01-osnova-proekta/01-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/content.config.ts` — `defineCollection` x5; loaders: `glob` (projects: `**/*.{md,mdx}`; notes: `**/*.md`), `file` (services/skills/tools: `./src/data/*.json`); `z` из `astro/zod`
  - Схема projects (поля): slug: string, title: string, summary: string, role: string, stack: string[], year: number(int), status: enum(active|archived), client-type: string, order: number(int, default 0), titleEn?: string
  - Схема notes: title: string, date: coerce.date, summary?: string
  - Схемы services/skills/tools: id: string, title: string, description: string
- `src/data/services.json` / `skills.json` / `tools.json` — массивы записей `{ id, title, description }` (русский, 2-3 записи)
- `src/content/projects/.gitkeep`, `src/content/notes/.gitkeep` — пустые коллекции (0 записей, edge «пустые коллекции проходят сборку»)
